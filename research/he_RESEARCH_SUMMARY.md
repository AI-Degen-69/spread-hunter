# סיכום מחקר — Maker

תצוגה מתומצתת ומתוארכת של [RESEARCH_LOG.md](RESEARCH_LOG.md). החדש בתחתית.

נקודה אחת לכל דבר קונקרטי שנעשה, נוסה, נמצא או נשבר.

---

## 21/07/2026

* משכנו **56,768 עסקאות של @powerwinner** בשוקי BTC/ETH ל-5 דקות (14–21 ביולי, 2,970 שווקים) כדי לבדוק אם עקומת ההון החלקה שלו נובעת מביצוע טוב יותר של האסטרטגיה שלנו.

* מצאנו שהוא עושה את **ההפך**: נכנס ב-0.30–0.70 (אנחנו ב-0.80–0.99), ב-40% **הראשונים** של החלון (אנחנו ב-40% האחרונים), ויש לו **אפס** עסקאות ב-0.98+.

* אחוז ההצלחה שלו ברמת שוק הוא **41.4%** מול נקודת איזון של 56.1% — על כיוון בלבד הוא מפסיד. הוא לא חוזה.

* פירקנו את הרווח שלו: ברוטו **+$39,884 לשבוע**, אבל **−$32,501** אם היה משלם את עמלת ה-taker שלנו. כל ההבדל הוא שהוא לא משלם עמלת taker — הוא מניח פקודות לימיט. הנפח שלו מתרכז בדיוק היכן ש-`fee ∝ p(1−p)` מגיע לשיא.

* **בדקנו ודחינו** את ההשערה שקניית שני הצדדים שלו היא ארביטראז' נעול: הזוג עולה 0.9990 עבור תשלום של $1.00 והוא רווחי רק ב-51.1% מהמקרים. תפיסת המרווח היא ~68% מהברוטו שלו.

* קודם לכן תפסנו טעות בניתוח שלי עצמי: כיסוי ההכרעות היה 59.1% ל-BTC מול 98.2% ל-ETH (כשלי הגבלת קצב, שנשמטו באופן לא אקראי), מה שהפיק תוצאה סבירה אך שגויה של −$23,828. אחרי השחזור, התשלום המחושב תאם את הפדיונות בשרשרת ברמת **0.1%**.

* בנינו סימולציית maker עם **מודל מילוי מודע-תור**. הניסיון הראשון הסתמך על שדה ה-`side` בסרט העסקאות, אבל 194 מתוך 200 שורות הראו "BUY" — ה-API מדווח על הצד של כל משתתף, לא של היוזם. נבנה מחדש על **הפרשי ספר**, שם תנועת התור נצפית ישירות.

* תיעדנו את ההטיות האופטימיות של המודל בתוך הקוד במקום להסתיר אותן; הפלט הוא חסם עליון. שבע בדיקות יחידה מכסות קדימות תור, סחיפות וגלישת מילוי.

* מצאנו ש**האיזון הוא המניע המרכזי** של הרווח על פני 44 שווקים: מאוזן לחלוטין +$30.70 לשוק, לא מאוזן קשות −$50.95. סה"כ מאוזן +$409, לא מאוזן −$848 — הפער הזה הוא כל ההפסד. שווקים לא מאוזנים מפסידים **באופן עקבי** (סטייה 7.3), וזו בחירה שלילית ולא מזל.

* תיקנו שני מנגנונים שחסמו את עסקת האיזון: תקרת העלות החזירה אפס ציטוטים (×1043), ותקרת מחיר-הזוג דחתה את הגידור ב-$1.00.

* **רגרסיה פתוחה:** התיקון הזה מאפשר כעת למחיר הזוג לעבור את $1.00 (1.09, 1.07, 1.03) עבור תשלום של $1.00 — הפסד מובטח על החלק המגודר. במכוון לא תוקן במהלך העברת הריפו; דורש שינוי נפרד והרצה נקייה.

* הוספנו שמירה על מופע יחיד אחרי שגילינו **ארבעה בוטים במקביל** כותבים לאותו מסד נתונים, ומסכמים מלאים עצמאיים לנתונים לא תקפים בשקט.

* פיצלנו את הריפו מזה של ה-taker, עם מבנה זהה ומחקר EN/HE משלו.

* עלה לאוויר: שירות Railway משלו, אחסון קבוע משלו ב-`/data`, ודומיין משלו. ה-preflight אימת את האזור (Binance נגיש) ושהאחסון ניתן לכתיבה לפני שהבוט מתחיל.

## 22/07/2026

* רגרסיית מחיר-הזוג שהייתה OPEN **אושרה וכומתה** על פני 53 שווקים שהוכרעו: מחיר זוג חציוני **1.0419** עבור תשלום של $1.00, רק **4%** מהזוגות מתחת ל-$1.00, הפסד ממומש **−$1,172.07**, תשואה **−4.1%**.

* הוכחה שהמספרים תואמים: תשלום 1.0419 עבור $1.00 הוא הפסד מובטח של ~4.2% על החלק המגודר, והתשואה הנמדדת היא −4.1%. זו **אינה** בחירה שלילית ואינה שונות — הבוט משלם ביתר עבור הגידור באופן שיטתי.

* כל השאר עובד: שיעור מילוי **37.6%** מול עומק תור אמיתי (חציון 72 מניות לפנינו), **0.48 סנט** נתפסו מול חצי-מרווח תיאורטי של 0.50, ואיזון מלאי **0.99** מול יעד 0.92. הביצוע תקין; משמעת המחיר אינה.

* שורש הבעיה הוא התיקון הקודם שאיפשר לצד המאזן לעקוף את תקרת מחיר-הזוג. הוא השיג איזון (0.99) במחיר משמעת המחיר (4% מהזוגות מתחת ל-$1.00). כיוון התיקון: להגביל את הגידור למחיר ששומר על הזוג מתחת ל-$1.00, ולוותר על הגידור כשאין מחיר כזה — לקבל חוסר איזון מסוים במקום הפסד מובטח.

* לוח הבקרה מדווח על רמות ביטחון 90/95/99 **שהושגו** ב-n=53 (ממוצע −$22.11, סטיית תקן $16.01). נכון אריתמטית, אבל מה שהוכח הוא ש**הבאג מפסיד כסף** — דבר שכבר היה ידוע. זה לא אומר דבר על השאלה אם market making עובד כאן. **הכרעה: DEAD לתצורה הזו; האסטרטגיה עצמה נותרת OPEN** עד להרצה נקייה אחרי התיקון.

## 22/07/2026 (אחר הצהריים)

* **תיקנו את באג תקרת מחיר-הזוג** שגרם להפסד ב-53 השווקים. ב-`strategy/quotes.py` פטור הגידור הוסר — `max_pair_cost = 0.995` חל כעת על **כל** צד, ומחסום שוק טרי מונע פתיחת עמדה אלא אם שני הצדדים מתמלאים לזוג תת-$1.00 ב-ask−1tick. הבוט מדלג על שווקים רחבים במקום לקחת רגל מפסידה מובטחת.

* **הוספנו את מפקד ההכרעה**. טבלה `hedge_census` רושמת, לכל שוק, האם היה זמין זוג תת-$1.00 ממלא בטאץ'. `kpi` מצרף את שיעור המילוי + מחיר-הזוג החציוני בטאץ' — זה המספר שלא נמדד מעולם על נתונים נקיים.

* **הגדרנו היכן הניסוי מסתיים** ב-`strategy/config.py`: שלב א — מפקד של 60 שווקים; אם שיעור תת-$1.00 הממלא `< 50%`, עצירה (DEAD); אחרת שלב ב מכריע 120 שווקים וקורא את סימן הרווח + הביטחון. לוח הבקרה מציג את שלב הניסוי ואת התקדמות המפקד בזמן אמת.

* **הפעלנו ריצה נקייה** (מופע יחיד, `hunter.db` נקי, בוט + לוח מקומיים). שוק המפקד הראשון חזר **ממלא ב-0.99** — כלומר ניתן לעשייה — בסתירה למספר 4% מהריצה המזוהמת. זו ההשערה הנבדקת כעת.

* **תיקנו באג מדידה:** דליפת `PYTHONPATH` של Hermes הצלילה את ה-venv של הפרויקט, כך ש-`pip` "סיפק" תלויות ל-site-packages של Hermes בעוד הייבוא בזמן ריצה כשל. טופל על ידי הפעלה עם `env -u PYTHONPATH ./.venv/Scripts/python.exe ...`. לקח כללי במארח Windows הזה: תמיד לנקות `PYTHONPATH` לפני הפעלת venv של פרויקט.

## 22/07/2026 (לילה)

* **בנינו את אוכף האיזון** — מניע ההפסד האמיתי היה **מילוי חלקי** (צד אחד מתמלא, השני אף פעם לא לפני סגירת חלון ה-5 דקות → הפסד חד-צדדי ברזולוציה), לא באג תקרת הכניסה. ב-`strategy/main.py`, כאשר `t_remaining ≤ 20s` והאיזון `< 0.92`, הבוט מבטל הצעות ו**חוצה את הספר** לקנות את הרגל החסר, בדיוק בהתאמה לצד המוחזק. כל שוק שהוכרע נסגר כעת מאוזן מבנית.

* **מילויים חוצים מסומנים** `crossed=1` ב-`store` (עמודה מתקנת-עצמה) כך ש-`kpi`/`settlements` מבדילים בין גידור סגירה למילוי maker. לוח הבקרה קיבל כרטיס **HEDGE X** (מספר חציות סגירה) — המחוון המוביל לשאלה אם מילוי-חלקי היה המניע.

* **ריצה נקייה מחדש אחרי שינוי הקוד** (AGENTS.md: שינוי פרמטרים מבטל את המדגם). תעדנו את 4 הריצות ה-partial-DB, מחקנו `hunter.db`, הפעלנו מחדש. אומת: הון $5,000, ממומש $0, גידורים 0, Phase A_CENSUS, מדיאן זוג 0.995 (תיקון התקרה מחזיק).

* **שני באגים נתפסו:** (1) `kpi.report()` הפנתה ל-cursor חשוף `c` מחוץ לבלוק → לוח הבקרה החזיר בשקט `{"error": ...}` תחת HTTP 200; תוקן דרך `_rows()`. (2) PIDs של MSYS/`kill` מתורגמים ו-`taskkill` דוחה אותם — נתיב ההרג האמין הוא `powershell Get-CimInstance Win32_Process` ל-PID ה-*native*, ואז `taskkill /F /PID <native>`.

## 28/07/2026

* **שיעור המילוי הופרז פי 16, והמספר הישן לא מדד דבר.** נבנו `scripts/record_books.py` (ספרים חיים גולמיים ל-`books.db` נפרד) ו-`scripts/measure_fill_rate.py` (הרצה מחדש של כל כלל ציטוט מול אותם ספרים). מודל הספר-בלבד דיווח על **50%** שיעור מילוי — ו**100% מהמילויים הגיעו מענף אחד**: "הרמה התרוקנה, אז נזכה את כל יתרת הפקודה". מדלתות צד-ההצעה, ביטול המוני אינו ניתן להבחנה מעסקה המונית, ולכן הענף הזה היה הנחה שנשאה את כל התוצאה.

* **תוקן על ידי מדידה במקום הנחה.** `scripts/fetch_trades.py` ממלא רטרואקטיבית את סרט העסקאות (~1,800 הדפסות לחלון של 5 דקות); `QueueFillEngine.on_book` מקבל כעת מפת `traded`. דלתת ספר = עסקאות + ביטולים (נראית רק כסכום); הסרט נותן עסקאות בלבד. שניהם מקדמים בתור, רק עסקאות ממלאות אותנו. **שיעור מילוי מאושר-סרט: 3.1%** מול 50.0% ספר-בלבד, אותם חלונות, אותם ספרים, אותה אסטרטגיה.

* **גידור האיזון מעולם לא עבד.** הוא הציב **הצעה ב-ask** — פסיבי, לא חצייה. שוחזר: **0 מתוך 150 מניות** מתמלאות גם כשהספר נסחר היישר מטה דרך הרמה. הוא נראה כעובד רק בגלל באג המילוי המדומה. כך שכל שוק לא מאוזן נשאר לא מאוזן, ומילויים חלקיים הם מניע ההפסד המרכזי המתועד.

* **התיקון עולה יותר מהיתרון שהוא מגן עליו.** נוסף `QueueFillEngine.cross()` (הולך על עומק ask אמיתי, מילוי חלקי הוא תוצאה אמיתית, `max_price` מגביל את ההליכה). אך חצייה היא פקודת **taker**: `taker_fee = shares * 0.07 * p * (1-p)` = **1.75 סנט למניה ב-p=0.50, מול יתרון זוג של ~1 סנט** — העמלה בשיא בדיוק היכן שהאסטרטגיה סוחרת. `kpi.py` חייבה אפס, ולכן ה-P&L היה נקרא גבוה מדי; כעת היא מחייבת את העמלה, מוציאה מניות חצויות ממונה שיעור המילוי, ואינה משלמת עליהן החזר maker.

* **נוספו שני הכללים החסרים של powerwinner** — רצועת מחיר 0.30-0.70 וציטוט רק ב-40% הראשונים של החלון — כל אחד מאחורי מתג נפרד כדי שיימדדו אחד בכל פעם.

* **לוח הבקרה מציג כעת מדדי maker:** שיעור מילוי מול עומק תור, מקור המילוי (מאושר-סרט מול מוסק מול חצוי), התפלגות עלות זוג, זמינות ציטוט + סיבות דילוג מובילות, חשיפת מילוי חלקי, עמלות taker, ולכידת מרווח למניה. `fills.reason` נשמר כך שהמקור נעקב לשורה אמיתית.

* **שני באגים בכלים של המפגש הזה עצמו, שנתפסו לפני שהפיקו ממצא:** מסגרת ההרצה-מחדש הפסיקה לצטט צד אחרי מילוי מלא (פקודה שהתמלאה גמורה, לא נחה); סמן הסרט התקדם אחרי מסלולי `continue` והיה מזכה מחדש הדפסות. בדיקות 8 -> 31; המסגרת מאומתת מול ספרים מתוסרטים עם תשובות מחושבות ביד לפני מגע בנתונים אמיתיים.

## 31/07/2026

* **צינור הבוט הבודד של פורט 8788 עבר לחנייה** בענף git אח
  `archive/legacy-bot-8788`. הצי על פורט 8800 הפך את קוד השוק הבודד
  המורשת למת פונקציונלית. הועברו חמשת הקבצים ל-`archive/legacy-bot-8788/`
  תוך שמירה על תת-הנתיבים המקוריים. `strategy/main.py` שומר רק על
  `full_book` + `recent_trades` — שני העוזרים ש-`strategy.fleet` מייבא;
  הזנב המת שמור בנתיב הארכיון. בדיקת לוח הבקרה מאמתת כעת את דף הצי
  בלבד. `.gitignore` שוחרר כדי לעקוב אחר `archive/legacy-bot-8788/` בעוד
  שאר `archive/` נשאר נתוני ריצה. שני הענפים `feat/live-readiness` ו
  `archive/legacy-bot-8788` מצביעים על אותו commit יחיד, כך שהענף החי
  נקי והגרסה ההיסטורית ניתנת לשחזור.

* **הוכנה הרצת נייר חדשה עם ארנק של $1,000 ולוח בקרה פשוט יותר.** הוגדרו ארנק מדומה של $1,000 עם מרווח הקצאה של $900, תקרת התחייבות של $1,000 ותקרת סיכון חד-צדדי של $400. לוח הבקרה מציג בראש P&L בפירוק, תגמול חזוי, הון מחויב, סיכון חד-צדדי, P&L ממומש ובריאות heartbeat; התשואה החזויה משתמשת בכל ההון המחויב ולא רק בהצעות. סקריפט ההפעלה קיבל `-FreshRun` שמעביר לארכיון את מסד הנתונים/קבצי הצד ואת קובץ המצב הישן לפני האתחול.

* **נתפסה ונחסמה חריגה של $1,036.80 מתקרת ההתחייבות.** המחזור הראשון שמר פקודות בגודל ישן והקצה הצעות חדשות מול סכום ישן. כעת מתבצעת שמירה אחרי ביטול, שינוי גודל לפקודות קיימות והגבלת גידורי חירום; באימות ההתחייבות ירדה ל-$367 ואז ל-$256, מתחת לתקרת $1,000.

* **הוגדר במפורש תחום התקרה ברמת הצי.** `visit()` מקבלת כעת מ־`main()` את כל רשימת מצבי השווקים עבור יכולת גידור חירום ושמירת פקודות, בעוד שקריאות ישירות לשוק יחיד נשארות מוגבלות בבטחה למצב יחיד; באג השם המקומי הלא־מוגדר נעלם.

* **סנכרון בריאות לוח הבקרה לקצב הסריקה שנמדד.** סף הישן הועלה מ־45 ל־120 שניות, משום שסריקה רגילה של 20 שווקים נמשכת בערך 50–70 שניות; גיל קובץ המצב וגיל מסד הנתונים עדיין מוצגים לאבחון.

* **מחזור הביטולים נשמר בספר החשבונות.** פקודות מדומות שומרות כעת את מזהי הצעות המחיר שלהן, כך ששחרור לפי תקרת ההתחייבות ותמחור מחדש מסמנים את שורות מסד הנתונים כבוטלו; גם שארית לא־מולאה בגידור חירום רדוד נסגרת בשורת ההצעה.

* **מילויי maker קושרו חזרה לשורות ההצעה.** מילויים מאושרי־טייפ של פקודות נחות נושאים כעת את מזהה ההצעה המקורי, וביטול שומר על כמות המילוי החלקי תוך סגירת מחזור החיים שנותר.

* **המשגר הוקשח למסירה בטוחה.** `fleet-start.ps1` מחשב את ה-checkout מתוך `$PSScriptRoot` וממתין שתהליכי-הבן ופורט 8800 יתפנו לפני הפעלה מחדש, ונכשל-סגור אם אחד מהם נשאר. נוספה בדיקת רגרסיה לשימור מזהה ההצעה המקורי במילויי טייפ מאושרים.

* **התנהגות ביטול הצעה שמולאה חלקית קיבלה כיסוי ישיר.** בדיקת store מוודאת ש־25 מניות שמולאו נשארות ב־`filled` כאשר שורת ההצעה מסומנת `cancelled = 1`.

## 03/08/2026

* **הקשחת בעלות תהליכים, טיפול בשחזור מסד נתונים והרצת דירוג מקבילית.** עודכן `fleet-procs.ps1` להתאמה מדויקת של טיקים בזמן התחלה. נוספו קבצים זמניים ייחודיים לכל הרצה, ניקוי ב-finally ובדיקת בעלות על marker ב-`rank_markets.py`. עודכן המעקב ב-`store.py` להשוואת מזהה קובץ (stat identity) כך שפעולת יצירה מחדש מפעילה את אתחול ה-schema. הופקדה פונקציית עזר `_atomic_write_json` ב-`fleet.py` ונחשף `stale_after_sec` ב-`fleet_dash.py`.

* **נוספו עמודות רווח/הפסד לא ממומש וממומש ללוח הבקרה.** חושב `unrealized_pnl` לכל שוק ב-`server/fleet_dash.py` (שילוב ציפה מזווגת וציפת יציאה לא מגודרת) ונוספו עמודות מפורשות של רווח/הפסד לא ממומש וממומש בטבלת לוח הבקרה למעקב מדויק אחר הפסדים צפים.

## 05/08/2026

* **בוצעה ביקורת של snapshot ה־P&L הלא־מגודר.** הדוח לקריאה בלבד כיסה 23 שווקים חיים, 57 מילויים, 10,853 הצעות, 9 סגירות, 16 הכרעות, נוטיונל מילויים של $2,063.56 והיסטוריה של 40.9 שעות. ה־P&L הממומש היה **+$116.33**, הציפה המזווגת **−$32.61**, הציפה הלא־מגודרת **−$223.32** ו־Total Liquidation P&L **−$139.59**; הזליגה הלא־מגודרת התרכזה ב־**3 שווקים** וההפסד הגדול ביותר היה **−$190.26**.

* **הופרדה מוסכמת הסימון מהתוצאה הכלכלית.** הדשבורד מסמן ספר בלתי קריא ב־$0, בעוד שההכרעה הבינארית היא $1/$0. עלות חשופה של $345.33 נעה בין **+$168.59 אם הכול זוכה** ל־**−$345.33 אם הכול מפסיד**, לכן בקרות הסיכון צריכות להשתמש ב־`naked_cost`, בגיל ובקריאות הספר — לא ב־`unhedged_float` לבדו. הגייט סימן **23/23** שווקים כ־WIDENED, כולל **18 ללא מלאי**.

* **אושרה תבנית אדוורס־סלקשן בצורת זנב.** ה־Drift הממוצע נע מ־**−1.21c ב־5 דקות** ל־**−5.86c בשעה** ול־**−12.83c ב־6 שעות** (`n=5` בשש שעות); **9/52** מילויים גרועים מ־−20c תרמו **−$165.07** ב־Drift משוקלל לפי גודל לעומת **−$29.94** בכלל. המרכז ברובו בריא; הזנב השמאלי נושא את ההפסד.

* **סומנה רצועת 40–60c.** היו בה `n=19`, Drift ממוצע **−10.68c** ו־**−$122.00** ב־Drift משוקלל לפי גודל; שאר הרצועות היו חיוביות או קרובות לאפס. זה תומך בניסוי מבוקר של תקרת נוטיונל/offset, לא במסקנה אוניברסלית.

* **אותר כשל המלאי.** **7/18** שווקים עם מילויים מעולם לא זיווגו, בעוד ששווקים שהצליחו הגיעו לכ־**79–92%** זיווג. ב־`lol-maz-mg1` החשיפה גדלה מ־**98** ל־**233 מניות UP** משום שה־skew היה רק כ־**0.6c** ב־98 מניות חשופות. כל **57/57** המילויים אושרו מול הטייפ, **0** חצו, ולכידת המרווח הממוצעת הייתה **+2.40c למניה**.

* **הכרעת הביקורת: OPEN.** יש לבדוק קודם, בנפרד, שני בקרות — סינון ספר דו־צדדי קריא בכניסה ואיסור הוספה לצד הכבד — ואז למדוד בנפרד את תקרת 40–60c ואת גייט ה־markout המשוקלל/מבוסס־זנב. יש להחנות cutoff של זמן עד שיצטברו יותר תצפיות שש־שעתיות; אין להשתמש כעת בהרחבת offset גורפת או ביציאה ב־market כתיקון.

* **נוספו פרימיטיבים של סיכון בדולרים (`strategy/risk.py`).** `naked_usd` מתמחר את הרגל העודפת לפי עלות ממוצעת ומשחזר את **$190.26** שנצפו על 233.40 מניות UP ב־0.8152 — פוזיציה שתקרה של 360 מניות מעולם לא סימנה. `book_health` דוחה ספרים חד־צדדיים, מוכרעים (0.999/0.001), רחבים מדי (0.26/0.42) ודקים מדי, ומדווח על עומק כ"לא נבדק" במקום כ"עבר" כשלא הוקלט עומק. תצורה חדשה: `max_naked_usd: 120.0`, `decided_price: 0.02`, `max_book_spread: 0.06`, `min_book_depth_sh: 200.0`. מודול טהור ללא קוראים, ולכן ההתנהגות החיה ללא שינוי. 344 בדיקות עוברות.

* **חוברו תקרת הדולר וגייט צד הגידור לנתיב הציטוט החי.** `risk.hard_block` מחליף כעת את תקרת המניות ב־`_decide_quotes_rewards`: בריאות אסימון הגידור, בריאות הספר שלנו, ואז `max_naked_usd` — בסדר הזה, כשהצד שמקטין חשיפה פטור. שוק ב־**$114.80 מתוך $120** אינו מניח דבר על הצד הכבד ומניח גודל מלא על הקל; ספר בריא מול שותף בלתי בר־גידור של **0.999 bid ללא ask** אינו מניח כעת דבר על אף צד. `max_naked_shares` הוסר ולא הושאר לצד תקרת הדולר, וטריגר עצירת החירום נוסח מחדש בדולרים. `enable_hard_blocks` מבודד את השינוי. 359 בדיקות עוברות, לעומת 344.

* **הוחלפו מדרגת הגודל וה־skew מבוסס־המניות בגרסאות מונעות־דולר.** `risk.size_for` מדעיך את הגודל הנח כ־`base * (1 - utilization)^2`, מוגבל ביתרת התקציב חלקי המחיר ומאופס מתחת למינימום של 50 מניות בזירה; הצד הקל לעולם אינו מצטמצם. `risk.skew_offset` מותח את הקפיץ לפי `risk_utilization`, כך ש־100 מניות חשופות בממוצע **0.85** נדחפות רחוק יותר מה־mid מאשר אותן 100 מניות ב־**0.15** — הקריאה שהרמפה הקבועה של 240 מניות לא יכלה לייצר, והסיבה שהיא עדיין טיפסה ב־233 מניות כש־**$190.26** היו על הכף. `skew_full_shares` הוסר. 385 בדיקות עוברות, לעומת 359.

* **סומן ל־replay:** עם בסיס 120 מניות ורצפה של 50, הסולם מגיע לאפס בערך ב־**35% ניצול (כ־$42 מתוך $120)**, כך שהצד הכבד מפסיק להניח הצעות הרבה לפני התקציב הנומינלי. האם זה חותך זרימה רעילה או רווחית היא שאלת dollar gate replay validation, לא תוצאה מוכרעת.

* **רצועת המחירים ותקרת עלות הזוג הפכו נגישות בנתיב `rewards` החי.** שתיהן ישבו מתחת לשורה שבה `_decide_quotes_rewards` מחזירה ומעולם לא רצו — המילויים היו בממוצע **0.8152** מול רצועה נומינלית של **0.30-0.70**, ו־`wta-kalinsk-kessler` קנתה 14 זוגות ב־**$1.0200** מול תקרת **$0.995**. כעת הן זרועות של `risk.hard_block`, בערכים ללא שינוי. שוק **0.95/0.96** אינו מניח דבר ומציין את הרצועה; הצעה ב־0.52 מול ממוצע מוחזק של 0.49 נדחית ב־**$1.01**.

* **נוסף טיפול סיכון תלוי־מחיר (price-dependent risk treatment).** `risk.band_risk_factor` מקטין גודל לעבר הטלת המטבע (`coinflip_size_cut: 0.10`, `coinflip_halfwidth: 0.20`) ומרחיב את ה־offset עם המחיר ששולם (`price_risk_widen: 0.010`). בהתאם ל־KTD3, ה־offset שנקטע בידי חלון 4.5c מומר בהקטנת גודל יחסית, כך שלשנאת הסיכון יש לאן ללכת גם תחת `WIDENED`. 408 בדיקות עוברות, לעומת 385.

* **תוקן באג קלמפ רדום.** offset שנקלמפ בדיוק ל־`max_spread_from_mid` נזרק בשל חריגה ממנו (`0.525 - 0.48 = 0.04500000000000004`). בלתי נגיש כל עוד הקלמפ לא נגע; שגרתי כעת.

* **ה־markout הפך משוקלל לפי גודל, עם גודל מדגם אפקטיבי של Kish (size-weighted markout with Kish effective sample size).** הממוצע הלא משוקלל איפשר לשתי ההדפסות שנשאו **233 מניות** להצביע במשקל של שתי הדפסות בנות 50. `_stats_from_rows` מחזירה כעת ממוצע משוקלל ו־`n` = `sum(w)^2 / sum(w^2)`, כשהספירה הגולמית נשמרת כ־`n_rows`; Kish שווה בדיוק לספירת השורות בגדלים שווים, ולכן `markout_min_sample` וכלל ההכפלה שומרים על משמעותם ו־`strategy/gate.py` לא נדרש לשינוי. המקרה המכריע: עשרה מילויים של 200 מניות ב־−5c מול עשרה של 10 מניות ב־+1c קוראים **בדיוק −2.00c** ללא שקלול — נוחתים *על* הסף הקטסטרופלי, כך שה־`<` המחמיר השאיר את השוק בספר — ו־**−4.71c** עם שקלול, מה שמפעיל את עקיפת העוצמה. 415 בדיקות עוברות, לעומת 408.

* **נוסף מפסק זרם ברמת הצי (fleet-level circuit breaker).** `gate.fleet_posture` גוזרת NORMAL / WIDENED / HALTED מהקריאה המאוגדת בלבד, בנפרד מ־`next_state`. הקריאה המאוגדת שהוקלטה, **‎−0.052375 על n=52** — פי 2.6 מהסף הקטסטרופלי, שנענה היום בהרחבת ציטוטים ב־1.5c — מחזירה כעת **HALTED**, החוסמת את הצד הכבד בכל שוק בעוד הצד הקל מניח בגודל זהה, שוק מאוזן עדיין מצטט בשני הצדדים, וחציית החירום עדיין נורית. נגזרת מחדש בכל סבב ואינה נשמרת, ולכן מוסרת בהתאוששות; קריאה שנכשלה משמרת את התנוחה הקודמת במקום להסיר עצירה חיה בשקט. אותו מאגר עדיין מגביל הכרעה מושאלת ברמת שוק ל־WIDENED, וזו הבחנת KTD5. 433 בדיקות עוברות, לעומת 415.

* **בוצע replay לשערי הסיכון בדולרים מול מילויי נייר שנרשמו (replay dollar risk gates).** נוסף `scripts/replay_risk_gates.py` לקריאה בלבד, יחד עם בדיקות fixture למקרי תקרת הדולר ועלות הזוג שנצפו, לזרימה בריאה, לעומק חסר, למסדים ריקים או חסרים, ולייחוס P&L ברמת שוק. מול `run/fleet.db` (67 מילויים, 23 שווקים), המסלול החי סירב ל־15 מילויים (22.4%), מנע **$729.88** עלות חשיפה לא מגודרת שנוספה, וייחס **$26.19** P&L ממומש שוויתר עליו; בדיקת השווקים הרווחיים סירבה ל־6/40 (15.0%), ולכן נשארה נקייה. עומק ו־P&L לכל מילוי נשארים במפורש OPEN כי הרשומה אינה מכילה אותם. 440 בדיקות עוברות.

* **נוספה טלמטריית פעולות למפעיל.** אירועים עמידים לכל שוק מכסים כעת מילויים, ציטוטים, חסימות, גידורים, יציאות, מיזוגים, המתנות ושגיאות, עם קודי סיבה יציבים לשערי הסיכון. לוח הבקרה מציג את הפעולה האחרונה ושני אירועים קודמים, ניצול סיכון חשוף ברמת הצי, יחס שווקים שמצטטים בפועל, מוני סירובים מובנים וסמן מחיר אמצע זהוב ובולט. 444 בדיקות עוברות.

* **נוספה טבלת יציאות ממומשות.** כל רשומת `closes` מוצגת מתחת לטבלת השווקים החיה עם שיטת מכירה/מיזוג, מניות, עלות ממוצעת, מחיר יציאה אפקטיבי, תשואה על עלות הבסיס, P&L נטו, עמלה/גז ופירוט הרגליים. 447 בדיקות עוברות.

* **נוסף סלקטור קשיח לשוק ראשי.** שווקי משנה של eSports ושמות Live/Handicap נדחים; מועמדים חייבים זיהוי מפורש של Moneyline/Main Line/Outright או Politics/Macro/Economics, נפח של לפחות **$250,000** ב־24 שעות, עומק נוטיונל של **מעל $5,000** בשלוש רמות ביד בנפרד ב־YES וב־NO, ומרווח ספר של **עד 4 סנט**. הצי חוזר על הבדיקה ומבטל ציטוטים סימולטיביים ישנים. כל החבילה: **452 בדיקות**.

* **הוכן מדגם נייר נקי.** המשגר המנוהל הקיים מארכב את בסיס הנתונים ואת קבצי SQLite הנלווים במקום למחוק ראיות, ואז מפעיל צי, דשבורד ו־ranker מול `run/fleet.db` חדש. ההרצה הנקייה נשארת OPEN עד לאימות שעץ התהליכים ויקום השווקים המסונן אכן חיים.

* **אופק הפקיעה הורחב ל־30 יום (resolution horizon extension).** עודכן `select_max_days_to_resolve` מ־7 ל־30 יום ב־`strategy/config.py`. ה-ranker בחר 3 שווקים ראשיים נזילים (לעומת 1 קודם), תוך הכנסת טורנירים נזילים ואירועי מאקרו. 452 בדיקות עוברות.

* **אובחן ותוקן באג שורות הדשבורד הקפואות ביקום ריק (פרסום מצב יקום-ריק).** אירועי 5-6 באוגוסט שהצי החזיק הוסרו מהפלטפורמה — בדיקה חיה מאשרת שה־CLOB מחזיר 404 עבור ה־tokens שלהם ו־`closed: true` במטא־דאטה — ולכן ה־404 היה נכון; הבאג האמיתי היה שצי שהופעל מחדש לאפס שווקים לעולם אינו משלים סבב ולכן לעולם אינו שותף את `run/fleet_state.json`, והדשבורד המשיך להגיש את תמונת־המצב שלפני ההפעלה (שש שורות מתות, קפואות) לכל חיי התהליך. חולץ `_publish_state(states)` כאתר כתיבת המצב האטומי היחיד, ונוספה `_idle_empty()` שמפרסמת `[]` במעבר ליקום ריק, כך שהדשבורד מתנקה ל־"אין שווקים מדווחים" במקום שורות זומבי. 6 בדיקות חדשות ב־`tests/test_fleet_state_publish.py`; כל החבילה 467/467 עוברת. מעבר הסדרה באתחול לשווקים שכבר ב־`resolutions` (הסדרת שווקים מוכרעים באתחול) וכותרת הסבב הריקה נותרו פתוחים.

* **קוצר קצב ה-rerank מ-60 ל-10 דקות (אופטימיזציית קצב re-rank).** גם `INTERVAL_SEC` ב־`scripts/rerank_loop.py` וגם `rerank_interval_sec` ב־`strategy/config.py` הונמכו מ־3600 ל־600 שניות. הצי כבר מאמץ את `run/markets.json` בתוך ~1 שנייה משינוי mtime, ולכן המחולל היה צוואר הבקבוק האמיתי; ההמתנה הגרועה ביותר לשוק חדש יורדת משעה ל־~10 דקות בתוספת מעבר ה-ranker של ~2 דקות. המפקד החי מאשר שיקום ספורט־בלבד הוא עובדת נזילות, לא דעה קדומה של הסלקטור: 997 שווקים ממומנים, 199 נוקדו, 199 נדחו (98 בעומק YES, 17 בעומק NO, ~27 במרווח, 53 בנפח, 6 במילת מפתח), בהתאמה למפגשים 11/13. 467 בדיקות עוברות.

* **מעבר הסדרה באתחול לשווקים שכבר הוכרעו (הסדרת שווקים מוכרעים באתחול).** `MarketState.__init__` בונה מלאי מפנקס ה־fills, שלעולם אינו לומד על הכרעות — לכן שוק שהוכרע בזמן שהצי היה מושבת התחיל מחדש עם מניות רפאים והון משועבד עד לרוטציה הראשונה (או לנצח, אם ה־ranker הוריד אותו קודם, דרך כלל השמירה "עדיין מחזיק מלאי"). נוספה `_settle_startup_resolved()` הקוראת ל־`_settle_resolved` הקיימת עבור כל שוק שכבר ב־`resolutions`, חוברה ל־`main()` אחרי טעינת `resolved_cids` באתחול, עם שורת לוג `STARTUP SETTLE`. 6 בדיקות חדשות ב־`tests/test_fleet_startup_settle.py`; כל החבילה 473/473. אומת מול `run/fleet.db`: 8 שווקים מוכרעים מחזיקים fills (WTA ו־White Sox ביניהם); רק השניים (~$104 מתוך ~$306) ניתנים לשחרור — לארבעה האחרים אין שורת הכרעה.

* **סקריפט התחלה אחד, רקע בלבד (איחוד משגרי רקע).** נמחקה גרסת ה־foreground של `fleet-start.ps1` וה־`scripts/fleet-start.ps1` היחיד הפך למשגר רקע בלבד (הגוף הישן של `fleet-bg.ps1`, שעוצר כפילויות מתועדות דרך בעלות ב־`fleet-procs.ps1`, מזהיר על עריקים, ומסרב על 8800 תפוס). עודכנו כל ההפניות (fleet-procs.ps1, rerank_loop.py, supervisor.py, שני עותקי SKILL.md, maker-instincts.yaml). איחוד בלבד — אין שינוי בהתנהגות זמן־ריצה.

* **תוקן ה-sweep הפנטום של 49 דקות על יקום ריק (תיקון שעון סריקה).** `_Pulse.idle()` מגלגל את שעון ה-sweep ב-in-progress בכל מעבר יקום-ריק בלי לרשום sweep נמדד (`sweep_sec` נשאר None); `_idle_empty` קורא לו בכל מעבר. לפני כן `sweep_elapsed` מדד מהפעלת התהליך (רק `sweep_done()` גלגל אותו) כך שצי סרק עם 0 שווקים דיווח "sweep מלא אורך 49 דק' 35 שנ'" דרך באנר הדשבורד. הצי היה בריא לאורך כל הדרך (`fleet_stale: False`). 3 בדיקות חדשות; 476/476 עוברות.

* **תוקנה תוצאת-הלוואי של הביקורת שלי (ביקורת יקום שווקים).** ביקורת ברים מקילים שהרצתי דרסה את `run/markets.json` עם בחירות ניסוי (ללא `--dry-run`). הצי תיקן את עצמו ב-rerank הקפדני הבא (~דקה לאחר מכן); מחקתי את 30 שורות ה-`reward_samples` המזוהמות (גיבוי נשמר), אימתתי אפס שאריות, והחזרתי את היקום הריק הקפדני. אין fills/markouts/P&L שנפגעו; שני שווקי הניסוי מעולם לא ניקדו (0/2) ולכן שום דבר לא צוטט. ה-config לא שונה.

* **watch_universe.py הוא כעת child מנוהל (צופה יקום מנוהל).** הוספתי "watch" ל-`CHILDREN` ב-`strategy/supervisor.py` כך שצופה סלאט הערב (מספר נבחרים, מפקד ranker, עומק/ספרד ספרי esports כל 5 דק' ל-logs/universe_watch.log) מופעל מחדש אוטומטית עם הערימה ושורד אתחולים. הרגתי את המופע המנותק; הסופרבייזר החדש (9112) אומת כריצה את כל ארבעת הילדים כולל watch (17940); דגימה מנוהלת ראשונה 16:45:36. 2 בדיקות חדשות; 478/478 עוברות.

* **market pipeline selection funnel — תצוגת צינור השוק (סדרה 20):** הדרג שומר עכשיו את כל משפך הסינון ל-`run/pipeline.json` בכל ריצה (`_write_pipeline_snapshot`: בריכות גולמיות, דליים לפי שער עם דוגמאות, ספירות scored/eligible/picked, שורות צנזוס ושערים); הלוח קיבל מתג Fleet / Market scan וקנבן של ארבעה מסלולים (RAW → FILTERS → FINAL → GRADUATED) שמגיש `/api/pipeline` עם מצב חי במסלול האחרון. דליים של שער הספרד קרסו מערך-לפי-ערך ל-"YES spread"/"NO spread". אומת חי: RAW 1010 → scored 198 → rejected 198 → eligible 0 → picked 0 על היקום הדליל של שבת. 483/483 בדיקות.

* **book gate hysteresis window (2026-08-08):** הבהובי ספר חולפים כבר לא מבטלים ציטוטים ולא מהבהבים STALE/ERROR — שוק הלול SK/NV צלל מתחת לבר העומק של 1,000$ ל-~שנייה לכל סקר, וכל צלילה ביטלה הזמנות, ניקתה את ה-bids האחרונים וחתמה err, כך שהדשבורד הבהב STALE/ERROR + הפסד מזויף של 66.34$- כל 4-5 שניות. נוסף חלון אישור `BOOK_GATE_CONFIRM_SEC = 15.0` לשער העומק/ספרד ולמסלול ההבאה ב-`visit`: הכישלון הראשון רושם חותמת זמן וחוזר מוקדם מבלי לגעת בהזמנות/סימנים/err; רק כישלון מתמשך מקבל את הביטול+חותמת המלא; ספר שהתאושש מאפס את השעון. 5 בדיקות חדשות (`tests/test_book_gate_hysteresis.py`), כל הסוויטה 488 עברה.

* **allocator marginal return verdict (2026-08-08):** פסק דין מקצה על תצוגת Market scan — `reallocate` שומר עכשיו פסק דין לכל שוק (`_alloc_verdict`, טהור: תשואה שולית %/יום בגודל המוחלט, תשואת הדולר הראשון, ממוצע תחרות, רף 2%/יום, פוט, ומחרוזת סיבה שנגזרת מאותה השוואה שעשה המים-פיל), נישא על `_live["alloc"]` דרך fleet_state.json אל `/api/pipeline` ומצויר על כל כרטיס GRADUATED ("funded 135 shares · marginal 2.07%/day · competition 46,969 · floor 2%/day" / "unfunded: below 2.00%/day floor"). k==0 מגודר נגד NaN. 5 בדיקות חדשות ב-`tests/test_allocator_verdict.py` + אישור נקודת קצה; 493/493 עברו; אומת חי על 8800.

* **filters lane allocator verdict (2026-08-08):** פסק דין מקצה על מסלול FILTERS — כל שוק שנדחה נושא עכשיו הערכה של מה המקצה היה אומר לו הרנקר היה מאמצו. `_if_adopted` ב-`scripts/rank_markets.py` עושה שימוש חוזר במתמטיקת הקבלה המדויקת של הצי (תשואה שולית של דולר ראשון %/יום = pot/T, `T = their_score/k` מקריאת הציון של ה-venue, פוטי ספרד משוחזרים דרך `spread_capture_daily`), מקובעת לנתיב הסירוב של `_alloc_verdict` על ידי בדיקה; דיקט דחיית הספר רושם עכשיו את קריאת הציון שלו כך שגם דחיות עומק/ספרד ניתנות להערכה (לדחיות זהות אין). `_write_pipeline_snapshot` מצרף את פסק הדין לכל דוגמה וספירת `would_fund` לכל דלי; `gateCard` מצייר "if adopted: ~2.45%/day · would clear the floor" (ירוק) או "below floor" (אדום) לכל דוגמה ועוד שורת סיכום חטאות-קרובות לכל דלי. 6 בדיקות חדשות (`tests/test_if_adopted_verdict.py`) + בדיקת משפך מורחבת; 500/500 עוברות; אומת חי על 8800.

* **near-miss tracker (2026-08-08):** מעקב חטאות-קרובות — הרנקר מצרף עכשיו `run/near_misses.jsonl` (שורה אחת לכל rank, ללא dry-run: scored/rejected + רשימת הירוקים המלאה עם סיבה, marg, pot, תחרות, ועומק top-3 נמדד+בר שחולצו מהסיבה), ו-`near_miss_stats()` ב-fleet_dash.py צוברת אותו לפסק דין READY_TO_TRIAL / COLLECTING / NO_DATA מול ארבעה ברים מתועדים (≥3 ימים, ≥25 שווקים ייחודיים, ≥5 שווקי עומק small-margin שנמדדו ≥50% מהבר, ≥50% יציבות על 72 ה-ranks האחרונים). ממוסגר כרישיון לניסוי מבוקר, לא לשינוי שער — הלוג מאמת עקביות האומדן; הניסוי מודד רווחיות. מוגש ב-/api/pipeline ומצויר כפאנל NEAR-MISS TRACKER בתצוגת Market scan. 8 בדיקות חדשות; 508/508 עוברות. אומת חי: 2 ranks נזרעו, 20 ירוקים ייחודיים, פוט של 183$/יום על השולחן, יציבות 100%, COLLECTING — והממצא האמיתי הראשון: כל דחיית עומק ירוקה נמדדה ב-434$ ומטה (מתחת ל-50% מבר ה-1,000$), כך שהרפיה ל-750$ לא הייתה מכניסה אף אחת מהחטאות-הקרובות של היום.

* **המשך near-miss tracker (2026-08-08):** אריח הפוט במעקב מסכם עכשיו רק את הקבוצה האמינה — `_is_trap` (עומק < 50% מהבר, או marg > 10%/יום) מחליף את מיראזות החלון-הריק לפי הקריאה האחרונה של כל cid, כך שמלכודת היאנקיז (15,768$/יום על 22$) כבר אינה מנפחת את המספר: האריח מציג 3$/יום (לעומת 16,124$) עם אריח נלווה "excl. traps 29 ($16,121/d)" ששומר על הפער גלוי. 3 בדיקות נוספו/עודכנו; 510/510 עוברות; אומת חי על 8800.

* **empty-book mirage filter (2026-08-09):** מיראזת הספר הריק מתויגת, לא נספרת. התשואה השולית של הדולר הראשון היא pot/T, כך שספר עם כמעט כלום בחלון התגמול מחלק בכמעט-אפס תחרות ומדווח הערכות אבסורדיות (890%/יום Dem-retirees, 4,938%/יום אינפלציית בריטניה) — אותה צורת ספר-ריק כמו מלכודת היאנקיז 15,768$/יום, לא הזדמנות חסומה. `_if_adopted` מסווג עכשיו `trap` (עומק < 50% מהבר, או marg > 10%/יום); `would_fund` סופר פסקי דין אמינים בלבד עם ספירת traps לכל דלי; מעקב החטאות-הקרובות מבסס כל בר החלטה (ימים/ייחודיים/small-margin/יציבות) על הקבוצה האמינה, מצייר מיראזות בענבר "EMPTY-BOOK MIRAGE", ומראה "mirages seen: N (not counted)". חי: 34 תצפיות אמינות על פני 6 שווקים ייחודיים מול 406 מיראזות מוחרגות; אריח הפוט 23$/יום אמין (גולמי 21,010$/יום); would_fund של YES-depth ירד ל-0 עם 82 מלכודות מסומנות. 5 בדיקות חדשות (3 trap + 1 ברים + 1 zero-depth); 515/515 עוברות; אומת חי על 8800.

* **mirage structural audit (2026-08-09):** ביקורת מבני-מול-מדידה של מיראזות הספר הריק. הנוסחה מדויקת ל-venue (אומתה 37.04, מסמכים מאשרים דגימה לדקה על פני אפוקים של 7 ימים). שחזור ספר חי משחזר את קריאת הרנקר (אינפלציית בריטניה 0.9 = 0.9); החלונות ריקים באמת מציטוטים הדוקים (הזמנות חונות 3.8–4.3c החוצה שם המשקל הריבועי ~1%, או הצעות פני 13c החוצה). "פוטים גדולים = ספרים ריקים" שקרי — הפוך: Spearman(rate, their_score) = +0.59 על 50 שווקים, 0% ריקים מעל פוט 30$/יום, 20% מתחת; המיראזות הן זנב קטן-פוט ($3–54/יום). אבל תצלום יחיד מוטה לשעת-שפל: תחרות אינפלציית בריטניה מתנדנדת 0.1→99 בין 19:00 ל-22:00 UTC (פי 1000), יציבה לאורך 60 שניות אך שעת-שפל. הערכת 4,938%/יום היא פנטזיית שעת-שפל; ה-epoch ממצע את כל הדגימות. הצעד הבא (מדידה, לא שינוי שער): למצע כמה דגימות ספר לכל rank או להזין קריאות מצטברות כך שההערכה תשפוט את הממוצע היומי, לא את התצלום בר-המזל.

* **sweep module extraction (2026-08-10):** החלק הראשון של חילוץ הסוויפ (issue #11) — שלב ה-settle-and-cancel הועבר מהפונקציה בת 630 השורות של המנוע אל מודול חדש `strategy/sweep.py` מאחורי ממשק קטן (`settle_resolved` / `cancel_live_orders` / `record_event` / `settle_startup_resolved`), גופים ככתבם, ואתרי הקריאה ב-`visit` שונו. רשת ביטחון: בדיקות הצי הקיימות עוברות ללא שינוי; 4 בדיקות חדשות ב-`tests/test_sweep.py` מניעות את השלב ישירות. כל הסוויטה 524/524. עבודת ארכיטקטורה ללא שינוי פרמטרי אסטרטגיה — אין צורך בדגימה חדשה. החלק הבא: שלבי ה-gate/decide (issue #12).

* **unified sweep interface (2026-08-10):** כל הסוויפ מאחורי ממשק אחד (issue #12) — `strategy/sweep.py` חושף עכשיו `sweep(state, ctx) → SweepOutcome` על שלבי ה-gate/decide (שער זהות, טעינת שוק, שער ספר עם היסטרזיס, פילס, קידום שער, יציאות, requote, דגימת תגמול), כל יציאה מוקדמת ממופה לסטטוס (SETTLED / IDENTITY_BLOCKED / COOLDOWN / UNLOADABLE / BOOK_HOLDING / BOOK_FAILED / QUOTING / BLOCKED / WAITING) עם `prev_gate`/`gate`/`why`/`fills`/`requoted`/`released`. `visit` הוא אליאס דק תואם-לאחור; `fleet.py` התכווץ מ-1,983 לכ-1,100 שורות. קריאות `now` חשופות בעזרי השלבים הומרו ל-`ctx.now` (19 אתרים); הבדיקות הועברו למרחבי השמות של מודול הסוויפ ו-4 בדיקות חדשות מניעות את הממשק האחד (settled משחרר עלות, identity-blocked מבטל ציטוטים, book-holding→failed בשני שלבים, quoting מציב שתי רגליים). כל הסוויטה 528/528. ארכיטקטורה בלבד, ללא שינוי פרמטרי אסטרטגיה — אין צורך בדגימה חדשה. תוכנית #14 עודכנה: sweep.py מייבא עכשיו את ה-fetchers.

* **KPI read-side module (2026-08-10):** מודול קריאה אחד ל-KPI של הצי (issue #13) — `strategy/stats.py` חדש מחזיק בכל שאילתת קריאה שחיה בעבר ב-`kpi.py` (דוח), `fleet_dash.py` (דף, כולל חיבור ה-RO והקאש+המנעול המצטברים של maker-rebate) וב-`fleet.py` (`inventory_from_db`). `kpi.py` שמר על המתמטיקה הטהורה וקורא ל-state reader; `fleet_dash.py` שמר על HTTP + HTML ו-`fleet()` שולף את כל מטען הקריאה עם `snapshot()` אחד; לא נשאר SQL באף אחד מהם. הבדיקות עברו לייבוא מ-`strategy.stats`; 4 בדיקות חדשות קושרות את המשטח. כל הסוויטה 532/532. פרשנות קריטריון 4 תועדה: כלי `scripts/` שומרים על מסדי הנתונים שלהם. ארכיטקטורה בלבד, ללא שינוי פרמטרי אסטרטגיה — אין צורך בדגימה חדשה.

* **entry point modularization (2026-08-10):** איחוד נקודת הכניסה (issue #14) -- `full_book` ו-`recent_trades` הועברו ככתבם מ-`strategy/main.py` שפרש אל `strategy/markets.py` (עם `TRADES_API`/`BOOK_TIMEOUT`/`TAPE_TIMEOUT` ו-logger מודולי); `sweep.py` ו-`live_test.py` מייבאים מ-`strategy.markets`, ו-sweep מייצא מחדש את אותם אובייקטים כך שתפרי ה-monkeypatch הקיימים מחזיקים. חמש הערות עודכנו; `strategy/main.py` נמחק. 2 בדיקות חדשות קושרות את ההעתקה (ייבוא + זהות האובייקטים המיוצאים מחדש); כל הסוויטה 534/534. ארכיטקטורה בלבד, ללא שינוי פרמטרי אסטרטגיה -- אין צורך בדגימה חדשה.

* **scorer gate offline test harness (2026-08-10):** תפר ה-fetch מתחת לנוקד השוק (issue #15) -- `evaluate` כבר מקבל את ה-HTTP session מעבר לתפר כמו ה-fetchers של היקום; מה שחסר היה כיסוי אופליין. `tests/test_scorer_gates.py` החדש (14 בדיקות) מניע כל שער של הנוקד מול session CLOB מדומה שמגיש ספרים קבועים ומסרב לכל בקשה אחרת: נעילת תפר (monkeypatch של `requests.Session`/`requests.get` מוכיח שהנוקד לא פותח חיבור משלו, בדיוק שני book GET), הפלות ללא פסק דין (כשל fetch, ספר חד-צדדי, mid קרוב להכרעה, ספירת tokens, חלון צר מהאופסט), דחיות עם פסק דין (זהות ללא הבאה, עומק+קריאה, ספרד, נפח, אופק, רצפת תשלום rewards, פטור מקור spread), ומסלול הקבלה עם הכנסה/הון מחושבים מחדש מנוסחת הציון. כל הסוויטה 548/548. בדיקות בלבד -- אין קוד ייצור, אין דגימה חדשה.

* **volume reader offline test harness (2026-08-10):** כיסוי אופליין לקורא הנפח (המשך של issue #15) -- `gamma_volume` קיבל את אותו טיפול stub session כמו הנוקד: 4 בדיקות חדשות ב-`tests/test_selection.py` קובעות חלוקה לנתחים (45 מזהה → 3 בקשות של 20/20/5 עם `limit` תואם, URL מקובע ל-GAMMA), סבילות שגיאות (נתח שנכשל מוריד רק את המזהים שלו), סבילות לצורת תגובה (`{"data":[...]}` עטוף, שורות ללא מזהה מדולגות, נפח null → 0), וקצה המועמדים הריק (ללא בקשות, תוצאה ריקה). כל הסוויטה 552/552. בדיקות בלבד.

* **venue book parser (2026-08-10):** תפר ניתוח מטען ה-venue (ביקורת ארכיטקטורה C1+C2) -- `strategy.markets.parse_book` החדש הוא המקום היחיד שבו שורות /book של ה-venue הופכות לרמות טיפוסיות. חוזה: אשפה ברמת שורה מדולגת ונספרת ב-`malformed`; מטען שגוי מבנית זורק ככשל בצורת fetch. חמישה אתרים התכנסו: `full_book` (רמה פגומה כבר לא יכולה להתחזות לכשל רשת ולבטל ציטוטים על venue בריא), `recent_trades` (שורות טייפ פגומות מדולגות + הגנת non-list; קודם לכן מחיר פגום אחד העלים שוק מכל סבב בשקט), ה-`evaluate` של הרנקר (fail-closed על `malformed > 0` -- מתחרה שלא נספר מנפח הכנסה; קודם לכן שורה פגומה אחת הפילה את כל ריצת הדירוג), ו-`watch_universe`/`record_books`. `resolve.py` אושר שכבר fail-closed, ללא נגיעה. 9 בדיקות חדשות; כל הסוויטה 561/561. CONTEXT.md קיבל את המונח Book adapter.

* **per-worker HTTP session pool (2026-08-10):** session אחד לכל thread עובד (ביקורת ארכיטקטורה C3) -- `requests.Session` מתועד כלא thread-safe, ובריכת 12 העובדים של הרנקר ניזונה מ-SESSION אחד משותף מ-`main`. נוספו פקטוריית `_worker_session()` איטית thread-local (pool keep-alive אחד לכל thread) ותפר `score_pool(jobs, *, session_factory, max_workers=12)` ש-`main` קורא לו במקום לולאת ה-`ex.map` המוטבעת; `evaluate` עדיין מקבל session לפי #15. שתי בדיקות קובעות את זה: 12 threads מסונכרני-מחסום מקבלים אותו session בתוך thread ו-12 sessions שונים בין threads; ומסלול הבריכה האמיתי עם Session מונה (ה-`get` המדומה של כל עבודה ישן כדי להחזיק את כל 12 העובדים בחיים -- ה-executor אף פעם לא מבטיח thread אחד לכל עבודה, ועובד שמסיים ברגע היה הופך פנוי ומצמצם את הספירה) יוצר בדיוק session אחד לכל עובד, לעולם לא אחד משותף. כל הסוויטה 563/563.

* **depth-gate trial (2026-08-10):** ניסוי שער העומק שטרקר ה-near-miss מרשה (READY_TO_TRIAL: 29/25 ייחודיים, 19/5 עומק בשוליים, 69% יציבות) -- שער ניסוי שלעולם לא נוגע בקונפיג הקבוע: `select_min_top3_depth_usd_trial` (env HUNTER_DEPTH_TRIAL_USD) + `--trial-depth` ברנקר (CLI > קונפיג > קבוע), `evaluate`/`score_pool` מקבלים `min_depth_usd`, שווקים מאומצים בניסוי מתויגים `trial_depth_usd` ב-run/markets.json, ו-pipeline.json מתעד `depth_gate_usd`/`trial_depth_usd`. `scripts/trial_depth_gate.py` החדש מרצה מחדש את לוג ה-near-miss המוקלט (214 דרגות, 3,605 ירוקים, 98 דחיות-עומק) מול שער ניסוי עם חוק התעתוע שנגזר מחדש לשער זה. תוצאה כנה: ב-$750 בדיוק שוק אחד בוגר -- mlb-cle-cws-2026-08-09 ($760 עומק, $1,667.47 ליום פוט, 2.57%/יום marginal) -- אבל בדיקת ה-slug מראה שהמשחק כבר נפתר: הירוקים המוקלטים לא נושאים תאריך סיום, ו-$750 כרגע לא מאמץ שום דבר בר-פעולה. סוויפ $500 מאמץ 6 שווקים ($1,913.47 ליום) — 5 חיים (~$246 ליום: ראשוני מושל MN-02 $97, בית WI-01 $55, השקת Extended-FDV $50, ספירת הסנאטורים הרפובליקנים $41, השקת $2B $3) בתוספת משחק ה-MLB המיושן; $1,000 → 0. אוכלוסיית דחיות העומק היא בעיקר תעתועי ספר-ריק (91/98 בשער הנוכחי; 89 ב-$750, 83 ב-$500), ובסיס הקריאה-האחרונה מחמיר מספירת ה-any-reading של הדשבורד (19), כך ש-READY_TO_TRIAL מנפח את מה שניסוי מאמץ היום. 10 בדיקות חדשות; כל הסוויטה 573/573.

* **mirage triage and live trial (2026-08-10):** טריאז' תעתועים + ניסוי ה-$500 החי. `scripts/mirage_triage.py` החדש עורך טריאז' לאוכלוסיית דחיות העומק המוקלטת (220 דרגות, 110 שווקים): תעתועים הם בעלי עומק זעיר/נפח כמעט אפסי (חציון ~$71 עומק, $232 נפח לעומת $7,183 ל-near-miss); אות ה->= 2 קריאות מסנן 18/104 תעתועים עם אפס אובדן near-miss (רצפי נפח היו מאבדים 5/6); והממצא הדומיננטי הוא שער הנפח — 83/110 דחיות עומק (וגם 5/6 near-miss, 5/6 בוגרים מוקלטים ב-$500) נכשלות בשער הנפח החי של $250k/24h. הניסוי החי מאשר: `--trial-depth 500` אימץ בדיוק שוק אחד (אותו שוק spread שהשער הקבוע בוחר; spread פטור מעומק) ואפס שווקי rewards — "הבוגרים" המוקלטים נכשלים חי על נפח בפער של 1–3 סדרי גודל. העומק מעולם לא היה השער המחייב; הנפח הוא הגבול הבא. הניסוי נשאר מבוים; השער הקבוע של $1,000 לא נגעו בו. 5 בדיקות חדשות; כל הסוויטה 578/578.

* **volume near-miss tracker (2026-08-10):** טרקר ה-near-miss הנפחי — האוכלוסייה שטרקר העומק היה עיוור לה. `_log_rank_volume_near_misses` החדש ברנקר מצרף ל-run/volume_near_misses.jsonl (שורה אחת לכל דרגה, כמראה לוג העומק) ומתעד כל שוק שנדחה על נפח ושהסיבה שלו נושאת קריאה נמדדת "24h volume $X < $Y" (unknown נספר כפער ומדולג), עם פסק הדין של האלוקטור (פוט/תחרות/marg) מצורף. `volume_near_miss_stats()` החדש בדשבורד משקף את `near_miss_stats()` עם אותם שערים — 3 ימים / 25 ייחודיים / 5 small-margin (נפח >= מחצית שער ה-$250k) / 50% יציבות על 72 דרגות — בתוספת `watched` (כל האוכלוסייה הנמדדת, ה-~80/דרגה שהשער דוחה) ורשימת `closest` (5 הראשונות לפי יחס קריאה אחרונה); במתכוון ללא זרוע תעתוע-מרג', כי דחיית-נפח כבר עברה עומק ולכן הספר שלה אמיתי. תצוגת Market scan מקבלת פאנל VOLUME NEAR-MISS TRACKER מתחת לזה של העומק. הלוג החל להתמלא באותו אחר-צהריים: הרנקר המפוקח מזמן תהליך חדש לכל מחזור, כך שדרגת 16:34 הרצה את הקוד החדש — דרגה אחת, 55 שווקים נצפים, הקרוב ביותר ב-42.2% מהשער (House-control $106k/24h) וכל חמשת הקרובים ארוכי-אופק (50–143 ימים, כך ששער האופק של 30 יום היה דוחה אותם בשלב הבא). small_margin_volume (>= $125k) 0/5 — COLLECTING, השער המחייב באפס כפי שנחזה. ניסוי מחצית השער שהוא היה מרשה אינו מיושם — זה משנה מה הפליט מצטט חי. 7 בדיקות חדשות; כל הסוויטה 585/585.

* **pairs-only completion and exit rule (2026-08-10):** חוק ה-pairs-only — פסק הדין של כלכלת הפילים (סשן 36) הפך לכלל התנהגותי מבוים. על פיל חד-צדדי בתוך חלון של 15 דקות (שם הסחיפה הנמדדת עדיין ~0, +0.09c למניה ב-5 דקות), הסוויפ עכשיו או משלים את ה-pair — חציית הרגל החסרה באסק דרך `engine.cross`, עלות ה-pair תקרה `max_pair_cost` 0.995 ורווח מחויב — או יוצא מהרגל הערומה ב-best bid (ה-~3c חצי-הספר במקום הסחיפה של −18.5c למניה ב-1h); חלונות שתפו מתעדים PAIR_WINDOW_EXPIRED פעם אחת לכל פיל. `record_hedge_census` (תפר שנבנה ומעולם לא הופעל, המודד P(השלמה מתחת ל-$1) ברגע המגע) רץ כעת בכל סוויפ. closes נושאים צד (יציאת ערום סוגרת רגל אחת), כך שחשבונאות realized/settled/inventory נבנית מחדש לפי צד במקום להניח שכל close מסיר את שתי הרגליים. סיכומי הדשבורד זוכים לאריח PAIRS-ONLY RULE כשנוסחת ה-EV היא ה-KPI — completion_rate × 16.3¢ − exit_rate × 3.0¢ לכל פיל חד-צדדי — כך שהמדגם הבא מכריע אם היא חיובית. בלוק קונפיג ברירת מחדל ON, env `HUNTER_PAIRS_RULE` משבית. 8 בדיקות חדשות (השלמה/יציאה/תפוגה/מושבת/census/סטטיסטיקות לפי צד); כל הסוויטה 593/593. באג אחד שנתפס: מחרוזת ה-tooltip של ה-EV נסגרה במרכאה לא תואמת — בדיקת פריסת הדף תפסה אותו כשגיאת דף-ריק לפני ששוחרר אי פעם.

* **volume-gate trial (2026-08-10):** הרחבת היקום היא בעיה של שערים, לא של שליפה — הצינור מנקד 205 שווקים חיים ובוחר 2. ביקורת משפך מלאה + ריצות יבשות הראו שהקלת העומק המורשית ($1,000→$750/$500) מאמצת 0 שווקים כשירים נוספים (אוכלוסיית ה-near-miss של העומק דלילת-נפח פי 3–25 — למשל עומק YES של $433 על שוק עם נפח $10,696 — כך שהקלת העומק רק מתייגת מחדש דחויים לתוך דלי הנפח), בעוד שהקלת הנפח היא המנוף היחיד עם תשואה חיובית (+2 would-fund ב-$235–242k; 91% מדחיות הנפח הן זבל < $50k, נכללות נכון). יושם ניסוי שער הנפח המשקף בדיוק את ניסוי העומק: `--trial-volume`, `_effective_volume_bar` (CLI > HUNTER_VOLUME_TRIAL_USD > קבוע), `min_volume_usd` מושחל דרך tradable/evaluate/score_pool/gamma_spread_universe, שווקים מאומצים מתויגים `trial_volume_usd`, הצינור זוכה ל-volume_gate_usd/trial_volume_usd, rerank_loop מעביר את שני דגלי הניסוי מהקונפיג, ו-fleet-start.ps1 מפעיל את שני הניסויים (עומק 750 / נפח $200k — השער הניסיוני הכנה, לא מחצית השער $125k ש-mirage triage and live trial הראה שאינה מאמצת כלום). max_open_markets (3) הוא קונפיג מת — אף פעם לא נקרא. 8 בדיקות חדשות; כל הסוויטה 601/601.

* **U36b (2026-08-10):** רמות הניסוי עודכנו לפי החלטת המפעיל — עומק $750→$500, נפח $200k→$125k (fleet-start.ps1). במחזור המנוהל הראשון תחת הרמות החדשות: כשירים 2→7 בצינור — התוספות הן שווקי spread (Navi/3DMAX CS, Jodar/Fils טניס, רד סוקס, אוריולס, מטס), כולם מתויגים `trial_*`; בחירות שוק הרווח ללא שינוי ומסלול ה-spread הפטור מעומק דומיננטי. הצינור מאשר `depth_gate_usd: 500 / volume_gate_usd: 125000`, שניהם מתויגים trial. שווקי הניסוי מתויגים ונצפים — השערים הקבועים לא נגעו, המדגם לא מעורב.

* **U36c (2026-08-11):** צפיית הרכב על פני שלושה מחזורים על רמות הניסוי המעודכנות (עומק $500 / נפח $125k). השוואת שני מחזורים הראתה קודם יציבות כמותית (נבחרים זהים 8→8, דחיות ±3 רעש, would_fund שטוח), אבל המחזורים הנצפים שינו הרכב כבר בראשון: טודי: מקסים מרווה מול פיירלואיג'י באזילה (טניס) התקבל 23:54 → נפל 00:04 → התקבל שוב 00:14 (7→8→7→8), בעוד 7 הבחירות האחרות באפס תחלופה לאורך כל הצפייה. טודי הוא שוק גבולי אמיתי (נפח $468k, פי 3.7 מעל שער הניסוי; spread של 1¢; הכנסה צפויה 10.1%/יום על $120) שעומק ה-bid שלו ב-3 העליונים חוצה את קו ה-$500 במשחק חי — הניסוי אימץ בדיוק את האוכלוסייה הגבולית שהוא קיים כדי לצפות בה, וה-markouts שלו נדגמים עכשיו. n=1 — נדרש צבר אירועים חדש לפני שהקבלה מכלילה.

* **U36f (2026-08-11):** אבחון ה"עברו שעות וכלום לא קורה" — שרשרת הסירוב, נמדדת. ביקורת אמת-בשטח (ללא ניחושים): הפליט למעלה וסורק כל ~10 שניות אבל לא מצטט כלום (`0/8 scoring | offers $0 | est $0.00/day` במשך למעלה משעה); market_events ב-sqlite הראה 5,304 BLOCKED לעומת 4 FILLED ב-24 שעות, 82% "unfunded by the allocator", 17% "outside band", 1% שגיאת עומק. נמצאו שלושה פגמים נערמים: (1) **באג** — שער הספר החי של הפליט קרא את שער העומק הקבוע $1,000 בעוד הרנקר אימץ בשער הניסוי $500, וחסם מחדש כל קבלת-ניסוי (תוקן: השער מכבד את ה-`trial_depth_usd`); (2) **באג ירושה** — רצועת המחיר 0.30–0.70 (עידן ה-BTC; תעודת הרצועה שלה אומרת שהיא קיימת כדי לדחות תוצאות 0.95+ כמעט-מוכרעות) חסמה שווקים ממומנים ב-0.25/0.75 (הורחבה ל-0.10–0.90); (3) **השער שמחייב בפועל** — רצפת התשואה השולית של 2%/יום באלוקטור שחררה את כל 7 השווקים החיים (0.04–1.84%/יום): בהפעלה עם התיקונים חיים הפליט ציטט את כל ה-7, ואז ה-reallocate הראשון שחרר כל אחד מהם. הדשבורד היה עיוור לכל זה (הכרטיסים הראו את פסק דין האלוקטור, לא את סיבת הסירוב) — כל כרטיס חושף עכשיו את ה-`err` החי ("unfunded: below 2.00%/day floor"). 3 בדיקות חדשות + פיקסצ'ר ה-replay 0.90→0.95; כל הסוויטה 603/603. פסק דין: שני באגים פשוטים תוקנו; רצפת ה-2%/יום היא שער מדיניות שזקוק עכשיו להחלטת מפעיל (רצפת-ניסוי מבוימת לשווקים בעלי ספר אמיתי ב-1.0%/יום שולי, אותה משמעת מתויגת/נצפית) במקום הקלה שקטה.

* **U36g (2026-08-11):** תשובת המפעיל — "לא אכפת לי מהסיבה. אני רוצה שזה יעבוד" — הכריעה את שאלת הרצפה. נמדדה התשואה השולית הראשונה-בדולר של כל שוק כשיר עם המתמטיקה של האלוקטור עצמו (`marginal(0, pot, T)`, `T = theirs/k`): Shnaider 3.997%, Jodar 0.825%, Falcons 0.771%, Navi 0.306%, Red Sox 0.125%, Orioles 0.085%, Mets 0.046%. ארבעת הנדחים ברצפת הניסוי של 0.5%/יום אינם זבל (טריאז' התעתועים הוציא את אוכלוסיית הספרים הריקים; עומק אמיתי 56k–880k, spreadים הדוקים, נפח חי — ספרים תחרותיים עמוקים בלבד). יושם `HUNTER_MARGINAL_FLOOR` (עקיפת env ל-`marginal_return_floor` ב-`config.load()`, משקפת את דפוס env הניסיוני), הופעל על 0.0001 (0.01%/יום) ב-fleet-start.ps1; `max_market_frac` 0.15 עדיין תוחם ריכוז, ברירת המחדל הקבועה נשארת 2%/יום. לוג הסוויפ: `funded 3/7 | offers $335` ב-0.5% → **`funded 7/7 | offers $674 | committed $674/1000`** ב-0.01% — כל היקום הכשיר מניח ציטוטים דו-צדדיים, הדשבורד קורא `7/7 scoring · LIVE`, verified 100.0% (30v/0u). בדיקה חדשה אחת; כל הסוויטה 604/604. פסק דין: הרצפה הוכרעה בידי המפעיל — samples על פני סלקטיביות, כי ה-edge הנמדד (חוק ה-pairs-only, +16.3¢ לכל pair שהושלם) צריך פילים, פילים צריכים ציטוטים, ציטוטים צריכים מימון; שער ה-markout וחוק ה-pairs עדיין צופים במה שהציטוטים מרוויחים.

* **2026-08-12 (תיקון):** סגירת הערות הביקורת של PR #22 -- הסרת מפתחות כפולים "paired"/"naked_sh" ב-fleet_dash.py (Ruff F601), הוספת סטטוס משולב "Paired + one side filled (15m window)" ב-spread_dash.py, הוצאת כפתורי הטאבים של סעיף ה-Inspection מכפתור הקיפול (HTML תקין), חישוב אחוז התשואה הקבוצתי מתוך P&L מצטבר חלקי עלות מצטברת במקום ממוצע של אחוזי יציאה, חיזוק שורות השווקים הסגורים נגד הזרקה (data-market + מאזין DOM, escape בטוח למאפיינים, קישורי Polymarket מקודדים), וסינון slugs של שווקים לתווים בטוחים ל-URL בגבול ניתוח הנתונים מהמקור.

* **2026-08-12 (עיצוב, שלב 1):** רמזי זמן-אמת + drawer שוקים בדשבורד spread_dash, ב-vanilla CSS/JS (לא React/Framer Motion — סומן ונפתר: הדשבורד הוא עמוד server-rendered וכל אינטראקציה מבוקשת היא פרימיטיב של CSS/JS). (1) ספרות מהבהבות בשינוי — 15 תאים עם data-kpi/data-v מבהבים ירוק rgba(34,197,94,.2)/אדום ב-300ms; תאי סטטוס דוהים+מתרחבים (opacity/scale .98, 180ms). (2) רול-אפ של 400ms עם ease-out במונו-טאבולרי על realized, unrealized ו-win rate (בסכימה אין "Hold Rate" — ה-KPI הקרוב האמיתי, סומן). (3) badge "Data health" עם נקודה פועמת ברציפות (opacity 1→.4, scale 1→.95), ענבר בסרק, אדום ב-offline. (4) שורות שוק פותחות drawer של 520px מצד ימין (translate-x 100%→0, 300ms) עם ספר-חי/mid, commit/resting/fills/age, P&L, ממוצע markout לשוק, ולוג-ביצוע אמיתי מהסגירות המכונסות; נסגר דרך x/רקע/Esc, מתעדכן בכל פולינג. כל הרמזים נכבים תחת prefers-reduced-motion. 619/619 בדיקות; אומת חי על :8805 (flash-up + אנימציית kpi-flash-up, status-in, רול-אפ באמצע +$137.05 → +$150.00, drawer עם "Execution log · 3 exits"). שום קוד backend/סיכון לא נגע.

* **2026-08-12 (עיצוב, שלב 3):** ווידג'טי סיכון/ספר בדשבורד spread_dash. (1) api_summary חושף עכשיו naked_usd (Σ של naked_cost לכל שוק — ה-USD ברגל הלא-מגוננת בעלות ממוצעת) + max_naked_usd (קאפ קשיח $120 מ-strategy/config.py), המזינים בר קיבולת Naked-USD ברוחב מלא ב-GATES: $בשימוש/$קאפ עם הבזק data-kpi, badge ניצול, בר דק בשלושת הפסים (<50% ירוק רך, 50–80% ענבר, >=80% אדום פועם + badge HIGH EXPOSURE דרך keyframe warn-bar), הכל כבוי תחת prefers-reduced-motion. (2) בר הפיצול ברצועת הספר הוחלף בשטיפות מיקרו-עומק מאחורי רמות המחיר — ירוק רך בצד ה-YES / חום-אדום בצד ה-NO, כל אחת 50% × חלקו של הצד בנוטיונל הרסטינג — עם סמן mid מוזהב במרכז (קו מתאר כהה + זוהר) ו-badge MID עם גב כהה; ספרות ה-$ הרסטינג נשארות פעם אחת לשוק באגדה דקה. הערת אמת: הוונו חושף רק מחירים, אז העומק נמדד לפי הנוטיונל הרסטינג שלנו, לא נפח ונואו מפוברק. (3) helper בשם tip(key, body) מוסיף כרטיסי tooltip טהורי-CSS (ריחוף או פוקוס מקלדת, ללא תלות) עם נוסחה + ערך חי + משמעות סף על 90% Lower Bound (mean − 1.645·σ/√n_eff), Markout Drift, Weighting Gap, מד ה-Markout Coverage (Kish (Σw)²/Σw² מול markout_min_sample) וכרטיס ה-naked-risk. 622/622 בדיקות (2 חדשות: pins לווידג'טים + בדיקת חוזה ל-api_summary); אומת חי על :8805 (ירוק ב-0%, ענבר ב-60%, אדום פועם + HIGH EXPOSURE ב-83.3%, שטיפות פרופורציונליות, סמן מוזהב, tooltips נפתחים בפוקוס, drawer מתעדכן, קונסול נקי). שום קוד אסטרטגיה/סיכון לא נגע.

* **2026-08-12 (עיצוב, שלב 2):** אסתטיקת טרמינל מזוגגת בדשבורד spread_dash — קנווס רשת כהה (גרדיאנט לינארי קבוע #080C14 → #0B111E), glassmorphism דרך עקיפות post-Tailwind עם !important על כלי הפאנל/הקו (rgba(15,23,42,.65) + backdrop-filter blur(12px) + גבולות rgba(255,255,255,.07)) כך שכל מעטפת סטטית וכל כרטיס מ-JS יורשים את המראה בלי נגיעה ברנדררים; זהב #FBBF24 שמור ל-badge ה-MID ולקריאת ה-GO בציר (ענבר #F59E0B נשאר על אזהרות אמיתיות: דופק מיושן, סרק, רגל חד-צדדית, פער משקלים); גופן הנתונים עבר ל-Geist Mono (אחד משלושת השמות) עם tabular-nums + tnum, ושתי הספרות האחרונות בפונטט דיספליי (תת-שורות המדדים) הועברו למונו; תוויות משניות הורמו #9CA3AF → #94A3B8 דרך עקיפה; פאנל ה-BOOK קיבל drop-shadow(0 4px 12px rgba(0,0,0,.4)). באג שנתפס חי: shorthand של background עם !important מחק את גרדיאנט הקנווס (תוקן ל-background-color בלבד). 620/620 בדיקות; אומת חי על :8805 לפי חישוב סגנונות (תמונת גרדיאנט, זכוכית rgba/blur/border, #94A3B8, #FBBF24 ל-MID, Geist Mono, צל) — קונסול נקי, ללא גלישה.

* **2026-08-12 (ידע): קוד הארגון המחקרי — קמפיין autoresearch (סשן 43).** יישמתי את הנחת היסוד של karpathy/autoresearch — מהירות המחקר מגיעה מהוראות הסוכן (program.md), לא מהקוד — על השיטות שלנו. מפקד (נמדד): 1,242 EN / 976 HE שורות, 42 סשנים בשני רצפים (S1–13 ואז S14–42) + 13 רשומות עיצוב מתוארכות, ~40 LIVE / 5 DEAD / 1 PARKED / ~10 OPEN, טווח 21 ביולי–12 באוג׳. פערים: אין program.md (כרטיס הניסוי מומצא מחדש בכל סשן; מטריקות מכריעות ברובן מרומזות), הרנסים (תקציב הזמן הקבוע שלנו) מעולם לא הוגדרו כ-replay-ראשון, ושלוש מערכות זיהוי מקבילות. תיקון: `PROGRAM.md` בשורש — כרטיס ניסוי (שאלה → מטריקה יחידה → לשמור/לזרוק → תקציב → הכרעה), כלל replay-ראשון, צורה מינימלית לעיצוב, רצף Session-N קנוני אחד. שום קוד strategy/ או server/ לא נגע; העיצוב שבביצוע לא נפגע; אף מדגם לא נפסל.

* **2026-08-12 (אסטרטגיה, סשן 44): ה-EV של ה-pairs-only נמדד — החוק נשמר, ה-KPI תוקן.** הניסוי הראשון תחת הכרטיס של PROGRAM.md. מדגם: 148 פילים חד-צדדיים (10/08→12/08), 144 השלמות (97.3%), 4 יציאות, 0 תפוגות. ה-KPI שנרשם מראש חיובי (+15.78¢ למניה) → לשמור. אבל שני קבועי התשלום מיושנים: תפיסת ה-merge מעידן החוק היא **+3.68¢ למניה** (144 סגירות, +$368.25 על 10,010.7 מניות) לא 16.3¢ (מדגם 302.5 מניות פרה-spread, פי 4.4 מהממשי), ויציאות עולות **−3.67¢ למניה** (n=4; תוקן משגיאת חיבור של 3.89 בסשן 44) לא ~3.0¢. EV מתוקן: +3.48¢ למניה, **+$2.37 לכל פיל חד-צדדי** (+$351.32 ממומש). מנצח גם החזקה (−18.5¢ למניה בשעה) וגם יציאה מיידית. תיקון: קבועי `strategy/config.py` 16.3→3.68 / 3.0→3.67 עם הערות שנמדדו מחדש; עדכון pin בבדיקה; 633/633 בדיקות. תצוגה בלבד — בלי שינוי ציטוט, בלי פסילת מדגם; אריח ה-PAIRS-ONLY בדשבורד כנה עכשיו דרך אותו מסלול קוד.

* **2026-08-12 (אסטרטגיה, סשן 45): קריאה חוזרת של עלות היציאה — אין יציאות חדשות, הקבוע תוקן.** מדגם היציאות עדיין n=4 (אף אחת אחרי 12/08 01:01), אבל ביקורת מול צירוף ה-SQL תפסה שגיאת חיבור בסשן 44: היציאות מסתכמות ב-**−$16.93** לא −$17.93 → **−3.67¢ למניה**, לא −3.89¢ (חציון −2.70¢, חריג אחד −10.70¢). `pairs_exit_cost_cents` 3.89 → **3.67**; ה-EV המתוקן עדיין +3.48¢ למניה, ממומש **+$351.32 / 148 פילים = +$2.37 לפיל**. נתוני סשן 44 ביומן תוקנו; pin הבדיקה 0.868 → 0.923; 634/634 עוברים (אומת שוב). תצוגה בלבד — בלי שינוי ציטוט, בלי פסילת מדגם. לקרוא שוב את הקבוע מעבר ל-~10 יציאות.

* **2026-08-12 (אסטרטגיה, סשן 46): בדיקה חוזרת של תשלום ההשלמה — הקבוע מחזיק (n=145).** תפיסת ה-merge מעידן החוק נמדדה מחדש: **+3.679¢ למניה על 145 סגירות / 10,037.1 מניות / +$369.26** — ללא שינוי מ-3.68¢ של סשן 44 (המדגם גדל ב-merge אחד, +3.81¢ למניה, בממוצע). הקריאה לא זזה → `pairs_complete_gain_cents` **נשמר על 3.68**, ללא שינוי קוד. התפלגות: 145/145 merges חיוביים, חציון 3.83¢, p25 2.96¢ / p75 3.96¢, מינימום 0.18¢ / מקסימום 16.5¢, לשוק 1.96–7.93¢ — הטענה הישנה "7/7 ב-+16.3c שונות אפס" מופרכת לחלוטין (16.3¢ היה חפץ של מדגם קטן). EV ללא שינוי: +3.48¢ למניה, ממומש **+$352.33 / 149 פילים = +$2.36 לפיל**. הבדיקה הבאה ב-~2× המדגם או בכיתת שוק חדשה.

* **2026-08-12 (כלים, סשן 47): ה-EV של חוק ה-pairs הוא פקודה אחת — `scripts/pairs_ev_report.py`.** דוח קריאה-בלבד (במודל של `replay_risk_gates.py`) המדפיס את כל המדידה של סשנים 44-46: KPI (מראה את `stats.pairs_ev`, קבועים בתוקף), החלטות לפי שוק, כלכלת יציאות ערום, תפיסת merge מעידן החוק עם ממוצע/חציון/קוונטילים + דגלי חריגי IQR + לפי שוק, EV ממומש $/פיל, ובדיקת השיוך merges-מול-completions; פלט `--json`. 6 בדיקות fixture-backed; כל הסוויטה 640/640. ריצה חיה: 150 פילים חד-צדדיים, 146 השלמות (97.3%), 0 תפוגות, EV **+3.484¢ PASS**, ממומש **+$2.35 לפיל**, 16 חריגים מסומנים, השיוך מראה בדיוק את שני אי-ההתאמות הידועים של pairs טבעיים — הקריאה של סשן 46 מחזיקה על n=146. באג fixture (PAIR_COMPLETE של זריעת-סכימה שמזהם את ה-KPI) נתפס ותוקן — גם קוד המדידה חשוד.

* **2026-08-12 (אסטרטגיה, סשן 49): כרטיס תזמון היציאה — יציאות בגיל ~0 ומנצחות המתנה, 4/4 (לשמור).** כל ארבע היציאות היו אירועי "pair not fillable under 0.995" שמופעלים באותה שנייה של פיל ה-tape (השוק זז לתוך ה-bid; pair לא בר-השלמה בלידה). כל ארבע רגלי ה-UP שיצאו הכריעו בהפסד. יציאה מול החזקה-עד-להכרעה: −$16.93 מול −$107.40 → **היציאה חסכה $90.47 (19.6¢ למניה משוקלל)**, חיסכון של 17.3 / 34.3 / 7.3 / 26.3¢ למניה לכל יציאה. פילים שאחרי היציאה מאשרים המשך ירידה (0.21→0.16→0.13 ב-25 דקות), כך שהמתנה הייתה יוצאת נמוך יותר. הנטל של 0.10¢ לפיל הוא ביטוח שהחזיר ~פי 5 על המדגם הזה. **פסק דין: לשמור, ללא שינוי קוד.** מגבלות: n=4, כולן UP, כולן הפסידו; ה-bid בקצה החלון מוסק (אין אופק markout של 15 דקות מוקלט). לבדוק שוב ב-~10 יציאות.

* **2026-08-12 (עיצוב): טלמטריית float-marks.** תצוגת Total equity הפכה לסדרה היסטורית אמיתית: הפליט כותב סימן אחד לכל-הפליט בכל מעבר (טבלת float_marks, בגבול המעבר של fleet.py main() לצד log_income_sample), סכומים שנגזרים בדיוק כמו שהדשבורד גוזר אותם מה-`_live` (unrealized = paired×1 − pair_paid + naked_exit_value − naked_cost; committed = capital + naked_cost + pair_paid; naked = naked_cost). api_summary חושף float_history (≤1 נקודה לדקה, תקרה 1,000); ה-widget ממזג סגירות + סימנים ונופל לפלוט של היום כשאין סימנים. שימור 90 יום נמחק בדרך הכתיבה (כפתור float_mark_retention_days). השינוי הראשון של מעבר העיצוב ב-`strategy/`, במכוון צד-פליט — סימנים צד-שרת קיימים רק בזמן שהדשבורד מפולס. 633/633 בדיקות (4 חדשות); מסמך זרימת הנתונים עודכן מהחלטה פתוחה לנחיתה.

* **2026-08-12 (אסטרטגיה, סשן 50): אופק markout של 15 דקות (mid_h3, 900 שניות) — ה-counterfactual של היציאה הופך מוקלט, לא מוסק.** נוסף אופק ה-900 שניות שסשן 49 היה צריך: `markout_horizons` הוא עכשיו (300, 3600, 21600, 900) — **מצורף בסוף, אף פעם לא ממוין**, כי אופק ממופה לעמודה לפי מיקום והכנסה באמצע הייתה משנה את המשמעות של קריאות mid_h1/mid_h2 הקיימות. `mid_h3` נוחת בסכמה וגם ב-`_MIGRATIONS` (ALTER TABLE) כך שה-run/fleet.db החי יקבל אותו בהפעלה הבאה. שתי הנחות סדר שהתקיימו כל עוד ה-tuple היה מונוטוני תוקנו במקום להישבר בשקט: `_matured` מחזיר עכשיו drift מהארוך לקצר **לפי משך** (קריאת 6 השעות מנצחת קריאת 15 דקות שהבשילה בשער), ו-`sample_due` קובע `done` רק כשכל האופקים מוקלטים (ה-15 דקות מבשיל לפני השעה/6 שעות, כך ש-done בעמודה האחרונה היה מותיר אותן יתומות). קוראי הדשבורד גוזרים את סדר האופקים ממשכי ה-config ופותרים את העמודות מול הסכמה החיה, כך ש-DB תלת-עמודי של פליט שעוד לא הופעל מחדש נקרא נכון במקום לשגות. אומת בקריאה בלבד מול ה-DB החי (n=174, pooled −1.37¢ ללא שינוי) ובמיגרציה של עותק מלא (כל 179 השורות נשמרו, `fleet_stats` == `pooled_markout_neff` בדיוק). 5 בדיקות חדשות; החבילה המלאה **646/646**. **הריצה החוזרת של כרטיס היציאה בהמתנה ב-~10 יציאות** (היום n=4): join של naked_exit closes → הפילים שלהם → mid_h3, במקום סולם ה-bid המוסק. אין שינוי בציטוט/סיכון; ה-DB החי לא נגע עד להפעלה הבאה של הפליט.

* **2026-08-12 (אסטרטגיה, סשן 51): ה-counterfactual של היציאה הוא פקודה אחת — `pairs_ev_report` מדפיס mid מוקלט של 15 דקות מול מחיר היציאה.** ה-join של כרטיס היציאה התלוי (naked_exit closes → פילים → mid_h3) הפך לסעיף מודפס בדוח. שני joins מבוססי-חלון (close→פיל בתוך 10 שניות, פיל→markout בתוך 30 — נחוץ כי `log_fill` חותם time.time() ב-INSERT בעוד `log_markout_open` משתמש ב-`now` של הסבב, בהפרש של ~0.3 שניות; התאמה מדויקת מוצאת בשקט כלום), שאומתו בשחזור מדויק של פילי סשן 49 (0.19/0.45/0.10/0.29, דלתות 0.08–0.17 שניות). חמישה מצבים כנים לכל יציאה: recorded / pending (15 דקות לא חלפו) / no_markout / no_fill / no_column (mid_h3 חסר — הפליט לא הופעל מחדש מאז סשן 50). mid מתחת למחיר היציאה הוא מכריע (היציאה ניצחה את ההמתנה); mid מעל בפחות מספרד הוא "ייתכן שההמתנה הייתה עדיפה" (היציאה נמכרה ב-bid, mid_h3 הוא mid). חי: כל ארבע היציאות קוראות no_column על ה-DB הלא-מוגר; אחרי מיגרציה של עותק מלא — כולן pending. 2 בדיקות חדשות; החבילה המלאה **648/648**. **כשיצטברו ~10 יציאות, הכרטיס הוא פקודה אחת** — ארבע היציאות ההיסטוריות יישארו `pending` לנצח (אי אפשר backfill ל-mid_h3); הכלי מקליט רק קדימה.

* **2026-08-13 (אסטרטגיה, סשן 56): מנוע רווחי סמך סטטיסטיים נוסף עבור ניתוח רגישות בנקרוֹל 10 דרגות.** הוספנו את `calc_confidence_intervals()` ואת `get_active_db_path()` ל-`strategy/stats.py`. מעריך רווחי סמך 95%/98% להתפלגות t של סטודנט, יחס שארפ ויחס סורטינו עם תמיכה בדריסת סביבה `SPREAD_HUNTER_DB`. 656/656 בדיקות עוברות.

* **2026-08-13 (שרת ואסטרטגיה, סשן 59): נקודת כניסה לשרת ודריסת תצורה ל-SPREAD_HUNTER_BANKROLL נחתו.** נוספה נקודת כניסה של uvicorn ל-`server/spread_dash.py` וטיפול ב-`SPREAD_HUNTER_BANKROLL` ב-`strategy/config.py`. 662/662 בדיקות עוברות.

* **2026-08-13 (תיקוני ביקורת, סשן 61): הערות CodeRabbit על PR #25 טופלו בסטטיסטיקה, במשגר, בתצורה וב-HTML.** נאכפה בדיקת אימות בנקרוֹל חיובי וסופי ב-`strategy/config.py`, נוספה טבלת ערכים קריטיים מדויקים להתפלגות t של סטודנט עם אינטרפולציית דרגות חופש ב-`strategy/stats.py`, נותב פלט תהליך רקע לקובץ `fleet.log` למניעת חסימת צינורות (pipe deadlock), ונוסף טיפול שגיאות במסד נתונים פגום ב-`scripts/bankroll_stats_report.py`. 671/671 בדיקות עוברות.

* **2026-08-16 (אסטרטגיה, סשן 62): קנס התיקון — אובדן 100% של מקום בתור בשינוי מחיר, נעול בעלות התנהגותית אפס (issue #27 שלב 1, רכיב 1).** הוספנו את `QueueFillEngine.amend()`: שינוי מחיר מאפס את `queue_ahead` לעומק ברמה החדשה, שומר על `filled` ומחזיר לתור רק את היתרה, וחותם `posted_ts` חדש; קריאה באותו מחיר היא no-op כדי שנתיב השמירה-על-ההוראה המכוון של ה-sweep לא ייענש. **דלתא מול המנוע הנוכחי: 0.0%** — אין תמחור מחדש במקום בשום מקום ב-`strategy/` או ב-`scripts/`, ה-sweep מבטל ומפרסם מחדש, ו-`post()` כבר קרא את העומק ברמה החדשה, כך שהאינווריאנט כבר התקיים מעצם המבנה. מה שחסר היה אכיפה: שום דבר לא מנע מנתיב עתידי לקבוע `order.price` ישירות ולרשת ותק. שש בדיקות חדשות — איפוס לעומק הרמה החדשה, רמה חדשה ריקה מתחילה מאפס, מניות שבוצעו שורדות, הקנס נושך בנתיב **הזיכוי** (500 לפנינו לא מזוכה כלום על הדפסה של 400 מניות), ה-no-op באותו מחיר, וביטול-ופרסום-מחדש נעול כנתיב שה-sweep באמת נוקט. אף אחת אינה מאמתת את פער שיעור הפילים, לפי הכלל ב-issue #27 שכל רכיב haircut מכויל מהתצפית שלו עצמו ולעולם לא מותאם מול השארית. שלושה fixtures נכשלו בהרצה הראשונה כי קריאת `on_book` הראשונה רק מבססת את `prev` — המנוע היה נכון, הבדיקות היו צריכות תמונת מצב מקדימה. סוויטה מלאה **684/684**. תרומת רכיב 1 ל-haircut של שלב 1 צפויה להיות אפס; מרוץ הביטול וקנס ההשהיה הם המקום שממנו haircut כלשהו חייב לבוא.

* **2026-08-16 (אסטרטגיה, סשן 63): מודל מרוץ הביטול — חשיפה לביטול בתנועה ומארקאאוט רעילות (issue #27 שלב 1, רכיב 2).** נוסף מידול חלון ביטול בתנועה $\tau = \tau_{\text{net\_oneway}} + \tau_{\text{venue\_ack}}$ (בסיס 250ms = 100ms + 150ms ב-`strategy/config.py`) ל-`QueueFillEngine`. כאשר נקרא `cancel()`, ההוראה נשארת חשופה במרווח $[t_{\text{prev}}, t_{\text{curr}}]$ ביחס לחפיפת הזמנים $p_{\text{race}} = \frac{\min(t, c_{\text{end}}) - \max(t_{\text{prev}}, c_{\text{start}})}{t - t_{\text{prev}}}$, ומזכה פילים המתויגים `reason="race"` ו-`outcome="race_loss"`. סריקת רגישות הוערכה על פני $\tau \in \{0, 150, 250, 500\}$ms על 13 חלונות מוקלטים (187,894 מניות שהוצבו): שיעור הפילים הכולל השתנה באופן מזערי מ-$0.103\%$ ($\tau=0$) ל-$0.104\%$ ($\tau=250$) ו-$0.105\%$ ($\tau=500$), אך פילי מרוץ הציגו **סלקציה שלילית גרועה פי 2.4** ($-23.9$¢/מניה לעומת $-9.86$¢/מניה בפילי תור רגילים), מה שמאמת את דגל 1. הסתייגויות: שינוי ליניארי הופך את המארקאאוט לקבוע ביחס ל-$\tau$ מעצם המבנה; עוצמת האפקט נשענת על מדגם זעיר ($n \approx 2.3$ מניות); מתח מול כלל 4 ב-PROGRAM.md נרשם. פסק דין **OPEN** לקראת שלב 1 המשולב. שש בדיקות יחידה, כל הסוויטה **690/690**.

* **2026-08-16 (אסטרטגיה, סשן 64): מודל השהיית פרסום פקודות והערכת ה-haircut המשולב של שלב 1 (issue #27 שלב 1, רכיב 3).** אורגנו מחדש פרמטרי ההשהיה ב-`strategy/config.py` ל-`net_oneway_ms = 100.0`, `cancel_venue_ack_ms = 150.0`, ו-`post_venue_accept_ms = 50.0` ($\tau_{\text{post}} = 150$ms). ב-`strategy/fills.py`, `QueueFillEngine.on_book()` מתאים את נפח הטייפ האפקטיבי לפי שבריר חפיפת ההגעה $f = \text{clip}((t_{\text{curr}} - t_{\text{arrival}}) / (t_{\text{curr}} - t_{\text{prev}}), 0.0, 1.0)$ כאשר $t_{\text{arrival}} = \text{posted\_ts} + \tau_{\text{post}}$ (פקודות שטרם הגיעו מפיקות `not_yet_arrived` ואפס זיכוי). פסק דין **OPEN** (אינווריאנט נעול, $n=4$ פילים בלבד על פני 13 חלונות; שלב 2 דורש איסוף חלונות נוספים לקוורום $\ge 500$ מניות). שש בדיקות יחידה, כל הסוויטה **696/696**.

* **2026-08-16 (מכשור וממשל מחקר, סשן 65): שדרוג שכבת התצפית להזרמת CLOB WebSocket (חציון 5.84ms) וכלל קוהורטה סגורה לארכיון.** שודרג `scripts/record_books.py` עם `WSMarketRecorder` המשתמש ב-`wss://ws-subscriptions-clob.polymarket.com/ws/market` עם מעבר חלונות אוטומטי, גיבוי REST אסינכרוני, ועמודות תזמון מרווחים (`ts_request_sent`, `ts_response_recv`, `ts_venue`). מדידה חיה על פני 150 אירועים הציגה **האצה פי 295 בקצב הדגימה**: המרווח החציוני ירד מ-1,727.8ms ל-**5.84ms** (P95: 28.68ms, מקסימום: 392.53ms), מה שמציב את הרזולוציה עמוק מתחת לחלון השהיית הפרסום $\tau_{\text{post}} = 150$ms. התיישנות בורסה ($t_{\text{recv}} - t_{\text{venue}}$) נמדדה בחציון 684.52ms. בוצעה ביקורת ב-`QueueFillEngine` להתנהגות ב-$\Delta t$ קטן ומרובה-מרווחים והגנת חותמות זמן כפולות (3 בדיקות יחידה חדשות; סוויטה מלאה **699/699**). נקבע כלל ממשל: `archive/20260729/books.db` מוקפא כקוהורטה סגורה ($n=4$ פילים, 193.1 מניות); קוורום הפעילות של $\ge 500$ מניות מתאפס ונמדד אך ורק על חלונות מהירים חדשים. פסק דין סשן 64 נשאר **OPEN**.

* **2026-08-16 (ביצוע חי ותשתיות, סשן 66): הוכחת פרוקסי ביקון, קבלת פקודות Deposit Wallet אמפירית (`POLY_1271`), ופרופ סדרה רב-חלונות דינמי ($N \ge 30$).** אומתה ארכיטקטורת Deposit Wallet באמצעות סלוטים של Storage (פרוקסי ERC-1967 המפנה ל-`0x7a18...`). נתוני תמסורת רשת תוקנו לתמסורת חד-כיוונית נמדדת $\text{net\_oneway} = 3.93\text{ ms}$ ($N=250$) והופרד רעש אקראי מהטיה שיטתית ($\text{SEM} = \pm 1.28\text{ ms}$, $|B_{\text{sys}}| \le 3.50\text{ ms}$). פקודת `probe` ב-`strategy/live_exec.py` שודרגה לגילוי שווקים דינמי על פני סדרות (`--series btc-up-or-down-5m`), טיפול במרווחי מעבר חלונות, והגנת פקיעה תוך כדי טיסה. בוצעה בדיקה אמפירית על גבי ה-CLOB: **פקודה חיה `0x97b831aa...` נחתמה כ-`POLY_1271` באמצעות `py-clob-client-v2 1.1.0` והתקבלה בהצלחה (`status='live'`, `success=True`)**. כל 8 סעיפי המוכנות בצ'קליסט סומנו **עבר (PASS)**. סוויטה מלאה: **699/699** בדיקות עברו. פסק דין: **LIVE**.

* **2026-08-16 (מנגנונים אמפיריים וממצאי זמני שהייה, סשן 67): שקלול פרופ 3 חלונות (N=90), חישוב מחדש של Haircut סשן 64, והטיה מוצהרת מס' 5.** שוקללו 3 חלונות פרופ חיים על ה-CLOB ($N=90$): זמן הקבלה במנוע ההתאמות נמדד ב-**ממוצע חציונים 81.13ms עם סטיית תקן בין-חלונות 4.98ms** (חציונים 85.00, 75.51, 82.89ms; חריג של 610ms הוגדר כאירוע 1 ל-90). נקבע `post_venue_accept_ms = 81.0` ב-`strategy/config.py` ו-`strategy/fills.py` ($\tau_{\text{post}} = 84.93\text{ms}$ מול 150ms מוגדרים, תיקון ענישת-יתר של $1.8\times$). אוששה $\tau_{\text{pubsub}}$ ב-**חציון ~120ms ו-P95 ~1.1s**: סחיפת עומק של 4.46% בחציון מאשרת שמודל התור שורד בהשהיה הטיפוסית; נוספה כהטיה מוצהרת מס' 5 ב-`strategy/fills.py`. סשן 64 חושב מחדש מול $\tau_{\text{post}} = 84.9\text{ms}$: ה-haircut המשולב נחתך מ-**11.31% ל-6.33%** (בבידוד ירד מ-11.78% ל-7.05%), והחזיר +9.6 מניות מבוצעות בקוהורטה הסגורה. 3 בדיקות יחידה, כל 702 הבדיקות עוברות. פסק דין **LIVE**.











* **2026-08-16 (פירוק ההכנסה, פער יכולות בנתיב החי, ביטול שער היומן, מפגש 68).** שאילתה ישירה של `run/fleet.db` במקום גזירה מחדש: ההכנסה הממומשת מתפרקת למנגנון אחד בלבד — `merge` **‎+$926.85**, `naked_exit` ‎−$42.71, `sell` **‎$0.00**, נטו **‎+$884.13** על פני 26,777 זוגות `[נמדד, n=376 סגירות]`. כל פרמטר שכויל במפגשים 62–67 (`spread_capture_frac`, אופקי markout `mid_h0`–`mid_h3`, $\tau_{\text{post}}$, קנס דעיכת תור, והקלטת ה-BTC בנפח 5.38 ג'יגה-בייט ב-`books.db`) שולט בנתיב ה-`sell` שלא הרוויח דבר; כולם **PARKED**. ה-markout המצרפי **שלילי, ‎−1.13 סנט/מניה** ($n_{\text{eff}}=260$), ושלוש הצעות להעלות את `spread_capture_frac` (פי 2.25, פי 1.87) נדחו — הראשונה השתמשה ב-`ref_mid` (אמצע בזמן המילוי, עיוור מבנית לבחירה שלילית), השנייה בקוהורטה מוטת הישרדות; הקוהורטה המותאמת ($N=81$) דועכת 81.0%←65.3%←52.1% ב-5 דק'/15 דק'/שעה בלי להתכנס, ותוחמת 26–39% באופק היישוב של 2–4 שעות, ולכן **0.25 נשאר**. כלכלת זוגות `[stats.pairs_ev()]`: 368 מילויים חד-צדדיים, **השלמה 95.65%** (Wilson 95% ‎[93.05%, 97.31%]), תוחלת **‎+3.36 סנט/ניסיון**; נקודת איזון 49.93% ללא צימוד אך **66.61%** בהכפלת הפסד היציאה, משום ששיעור ההשלמה ועלות היציאה חולקים מניע אחד; כל 16 היציאות מטריגר יחיד (`sweep.py:798-825`, `pair_cost >= 0.995`), ללא סחיפה יומית מובהקת (08-15: בינומי מדויק $p=0.0103$, Šidák $p=0.0600$). **פער היכולות בנתיב החי:** 4 מתוך 6 שלבים חסרים — זיהוי מילוי, השלמת רגל שנייה, `mergePositions`, ועצירת ההפסד; `mergePositions` קיים במאגר כולו רק כטקסט שיווקי ב-`server/explainer_html.py`, כקבוע גז, וכבדיקה המאמתת את אותה מחרוזת. שער היומן בוטל בהחלטת הבעלים (`n_settled` 165, `ci90_lower` ‎+2.73%, `max_category_share` 0.382 — כולם עוברים; `calendar_days` 10.44 היה החוסם היחיד); הסטטוס עובר ל-`READY_FOR_SMALL_LIVE_PILOT`, תצוגתי בלבד, שום נתיב אינו נדרך בעקבותיו. 716 בדיקות עוברות. הכרעה **LIVE** על השער, **PARKED** על פרמטרי נתיב ה-`sell`, **OPEN** על $N_{\text{real}} = 0$ — הבנייה מתוזמנת כך ששום שלב הפותח חשיפה אינו נשלח לפני השלבים הסוגרים אותה.

* **2026-08-16 (תיקון ממצאי סקירת PR #31, מפגש 68b).** 15 הערות CodeRabbit מוינו מול הקוד הנוכחי. **שלושה פגמים אמיתיים תוקנו:** `scripts/record_books.py:138` הפנה ל-`_SESSION`/`MARKET_TIMEOUT` בלתי מוגדרים (Ruff F821) ולכן `--rest` העלה `NameError` בשליפה הראשונה; מטפל ה-WebSocket ב-`strategy/live_exec.py` קיבל כל מסגרת `event_type == "book"` ללא תלות ב-`asset_id`, מה שאפשר לתצלום של הרגל המשלימה להחתים `ts_recv` על היעד (פוגם ב-`tau_pubsub_ms`) ולמלא את `comp_best_bid` מהספר של היעד עצמו (המחיר שמולו משווה הגנת ההפסד של הבדיקה); וה-`except Exception: pass` החשוף בסקר סטטוס ההזמנה השתיק את `--max-fills`/`--max-loss` בכל פעם שהזירה לא הייתה תקינה — כעת נכשל סגור עם ניסיון חוזר אחד ואז עצירה. **חמישה סקריפטים נמחקו** (`query_onchain_balances.py`, `diagnose_phase1_checks.py`, `eval_tau_sensitivity.py`, `eval_post_latency_sensitivity.py`, `inspect_fill_diffs.py`) — משוכפלים, בלתי ממומשים, או סורקים את `net_oneway_ms` שהיא רגל משותפת ל-`tau_post` ול-`tau_cancel` ולכן אף אחד לא בודד את חלונו; כולם מדדו פרמטרים PARKED, ו-`AGENTS.md:69` מחייב מחיקת קבצים מיותרים. **הכרעות נוספו ל-`research/QUANT_QUESTIONS.md`:** ש1 LIVE, ש2 LIVE, ש3 LIVE, ש4 PARKED, ונתון ההובלה של בינאנס (100–300 מ"ש) מתויג כעת כאומדן שלא נמדד. **הערה אחת נדחתה:** הסרת התיעוד של הזמנה חיה שהתקבלה הייתה מזייפת את יומן המחקר. שאר הפריטים יושבים כולם בנתיב ההשהיה ה-PARKED. הכרעה **LIVE** על התיקונים, **PARKED** על היתר.

* **2026-08-16 (הקשחת ביצוע חי ותיקון ביקורת, מפגש 69 / שלב 0).** הוקשח נתיב הפדיון ללא גז (בכסף אמיתי) ב-`strategy/live_exec.py` מפני מצבי שליחה עמומים: כתיבת שורה ב-`run/live_orders.json` עם `status="pending"`, nonce ו-payload חתום *לפני* השידור ל-relayer; עדכון במקום ל-`status="submitted"` בתגובה תקינה (עם שדה `tx_hash` עליון ותגובה קטומה ל-400 תווים), `status="interrupted"` ב-`KeyboardInterrupt`, או `status="unknown"` עם פירוט השגיאה ועצירת `SystemExit` בשגיאת HTTP/timeout כדי למנוע פדיון כפול. נוספה `_atomic_write_json` עם `fsync` + `os.replace`, הגנה פורנזית ב-`_log_order` המשמרת קבצים משובשים תחת `.corrupt.<unix_ts>.json`, והדפסת הרשומה ל-stderr בכשל עדכון יומן. תוקן `scripts/audit_settlement.py` עם `py_clob_client_v2` המוצהר וקורא יומן רגיש למערך המפענח רשומות `REDEEM`, שדה `tx_hash` וסטטוסים עם תפיסת שגיאות צרה. סך כל הבדיקות: **727/727 עוברות** (עלייה מ-716). הכרעה **LIVE**.

* **2026-08-16 (שלב 1 מיזוג פוזיציות בנתיב החי, מפגש 70).** מומשה יכולת מיזוג פוזיציות (`mergePositions`) ללא גז ב-`strategy/live_exec.py` דרך ה-Relayer עם סלקטור גזור `0x9e7212ad` (מתוך `ConditionalTokens.sol:165-171`), חישוב דטרמיניסטי של מזהי פוזיציות וקולקציות (`CTHelpers.sol`), וקידוד ABI מלא של 260 בתים. נאכפו 5 שכבות של מנגנוני הגנה טרום-ביצוע: אימות יתרות טוקנים מותנים (UP/DOWN) על השרשרת ($\ge \text{amount}$), בדיקת אי-הכרעה (`payoutDenominator > 0` נדחה עם הפניה לפדיון), תקרת ערך נקוב מקסימלי `$MAX_ORDER_USD` ($25.00), ושער אידמפוטנטיות להזמנות כפולות במצבי pending/submitted/interrupted עם עקיפה בדגל `--force`. חולצה פונקציית `_submit_and_log` מאוחדת ועמידה בפני קריסות עבור `redeem` ו-`merge`. נוספו 10 בדיקות ב-`test_live_exec_merge.py` ובדיקת עצירה בכשל כתיבה ב-`test_live_exec.py`. סך הבדיקות: **738/738 עוברות** (עלייה מ-727). הכרעה **LIVE**.


* **2026-08-16 (תיקון סדר השערים בשלב 1, מפגש 71).** הריצה היבשה של `merge`/`redeem` חזרה מבלוק התצוגה *לפני* שערי היתרות וההכרעה, כך שתצוגה מקדימה יכלה לדווח `net_collateral $0.95` על שוק שכבר הוכרע ועם רגליים המחזיקות 0.00 — מצב ש-`--live` דוחה בשלושה נימוקים. כל הערכת השערים הועברה מעל התצוגה בשתי הפונקציות; הריצה היבשה מדפיסה כעת `PRE-FLIGHT FAILED -- --live would refuse:` עם כל שער כושל ויוצאת עם קוד שונה מאפס, בעוד `--live` עדיין עוצרת בכשל הראשון עם ההודעה המקורית. הסלקטור של `mergePositions` נגזר מחדש באופן בלתי תלוי: `keccak256("mergePositions(address,bytes32,bytes32,uint256[],uint256)")` = `9e7212adc5e7c32011f71e609b50480abac7cc657f1af13efab4cfdd471125d4` ← `0x9e7212ad`, בהצלבה מול `redeemPositions` ← `0x01b7037c` התואם ל-`encode_redeem_positions` הקיים; הגיבוב שהועתק ברשומת מפגש 70 היה נכון רק בארבעת הבתים הראשונים. ה-calldata אומת מילה-במילה. סך הבדיקות: **739/739 עוברות** (עלייה מ-738). הכרעה **LIVE**.
 
* **2026-08-16 (שלב 2: רשם פקודות ולולאת זיהוי מילויים חיה, מפגש 72).** נבנה רשם פקודות SQLite עמיד ב-`run/live.db` מבודד (`strategy/order_registry.py`) עם מפתח ראשי `uuid4` מקומי טרום-שידור, מזהה פקודת זירה ייחודי וניתן ל-null (`order_id`), גזירת `size_matched = SUM(fills.size)` באמצעות SQL view ועוזר שליפה (ללא עמודה משתנה ב-`orders`), `PRAGMA foreign_keys=ON` בכל חיבור, מצב WAL, נעילת `BEGIN IMMEDIATE`, הגנת נקודה צפה `SIZE_EPS = 1e-9`, ואילוצי CHECK על `status`/`side`. מומשה פונקציית `reconcile_orders` עם שליפת `get_open_orders` בודדת, שאילתת עסקאות עם חפיפה של 60 שניות ומניעת כפילויות על בסיס `trade_id`, אימוץ פקודות יתומות ממתינות בטווח 30 שניות (ברירת מחדל ל-`unattributed`), ומעבר ל-`filled` אך ורק על פי עסקאות (היעדרות ללא עסקאות מסומנת כ-`cancelled`). נוספה תת-פקודה `poll` ב-`strategy/live_exec.py` עם מחזוריות 5 שניות, מצב `--once`, נסיגה אקספוננציאלית בתקרה של 60 שניות ב-429/5xx, שורת סטטוס בכל מחזור, `run/live_events.log`, דופק אטומי `run/live_poll_heartbeat.json`, ויציאה נקייה באותות. 13 בדיקות יחידה ב-`tests/test_order_registry.py`. סך הבדיקות: **752/752 עוברות** (עלייה מרצפת 739, דלתא +13). הכרעה **LIVE**.

* **2026-08-16 (חיזוק שלב 2 — כשלי קריאה מהזירה לא יהפכו למעברי סטטוס, מפגש 72).** קריאה ישירה מהדיסק של `reconcile_orders` ושל `poll` חשפה שבעה ליקויים הכותבים שורה שגויה במקום להרים חריגה. החמור: כשל ב-`get_trades` נתפס והידרדר לרשימה ריקה, כך שפקודה שהתמלאה בזמן הגבלת קצב נרשמה כ-`cancelled` — ההיפוך המדויק של השער "לעולם לא לסמן filled על סמך היעדרות בלבד" שסביבו נבנה השלב — ובנוסף הפך את הנסיגה האקספוננציאלית לבלתי ניתנת להפעלה, שכן הלולאה מעולם לא ראתה את ה-429 שההתאמה בלעה. כן: מילויי taker נזרקו בשקט (ההתאמה כיסתה maker בלבד, ללא ענף `else`), חפיפת עסקאות שהפחיתה 60 שניות פעמיים ונרצפה לפי הפקודה הממתינה הישנה ביותר כך שהשאילתה גדלה ללא חסם, מטפל ששב וקרא לאותה קריאת `get_open_orders` שנכשלה, `KeyboardInterrupt` שנמלט כ-traceback מהתהליך היחיד שרץ ללא השגחה, ו-`poll --once` שיצאה עם קוד 0 לאחר מחזור כושל. נוספה בדיקת אימות מקדימה: `reconcile_orders` מסרבת לפעול כאשר `client.creds is None`, משום שהיעדר פקודות פתוחות מהווה ראיה רק כאשר ידוע לנו ששאלנו ונענינו. אומת מקוד המקור של ה-SDK המותקן ששורת ה-`400 "Could not create api key"` הוותיקה היא שפירה — `create_or_derive_api_key` בולעת את כשל היצירה ונסוגה לגזירה, ו-`_l2_headers` מרימה חריגה דרך `assert_level_2_auth` בהיעדר הרשאות, ולכן לקוח לא מאומת אינו יכול להחזיר רשימה ריקה בשקט. סך הבדיקות: **756/756 עוברות** (רצפת 739 טרום-שלב-2, בתוספת 17 בדיקות רשם). הכרעה **LIVE**.

* **2026-08-16 (תפעוליות שלב 2 מתוך ריצה חיה בת 4 מחזורים, מפגש 72).** הורצה `poll --interval 5` לארבעה מחזורים והופסקה. שני פערים לא-אלגוריתמיים: `run/live_events.log` מעולם לא נוצר, משום שנפתח רק בעת מעבר סטטוס או שגיאה, ולכן מפגש שקט לא הותיר דבר על הדיסק ו"מעולם לא הופעל" לא ניתן היה להבחנה מ"רץ ולא ראה דבר"; ו-Ctrl-C במהלך שנת ההמתנה — לשם הוא מגיע כמעט תמיד, שכן הלולאה ישנה ב-96% ממחזור בן 5 שניות — יצא בשקט במקום להכריז על עצמו כמו המטפל שבתוך המחזור. `START`, `STOP` ו-`EXIT` נכתבות כעת ללא תנאי דרך עוזר `_log_event` הבולע `OSError` במקום להפיל את הלולאה בגלל כתיבה ליומן. אומת: `START pid=47196 interval=5.0s once=True db=run\live.db` ולאחריו `EXIT cycles=1 errors=0`; דופק חיים פעיל במחזור 4, `ts=1786912508948`. סך הבדיקות ללא שינוי: **756/756**. הכרעה **LIVE**.

* **2026-08-17 (סקירת PR #32 — שבעה ליקויים נוספים מסוג "קריאה שנכשלה הופכת להכרעה", מפגש 72).** CodeRabbit העלתה 17 ממצאים; שבעה היו ליקויי קוד אמיתיים, כולם באותה צורה. שניים קריטיים, שניהם בטיפול ביתומים: מאגר מועמדי האימוץ נקרא פעם אחת ולא נצרך, כך ששתי פקודות זירה שהתאימו לאותו פרדיקט `(token, price, size, ±30 שניות)` — דפוס הציטוט הדו-רגלי הרגיל שלנו — נקשרו שתיהן לאותה שורה ממתינה, והשנייה העבירה בשקט את `order_id` מהראשונה והותירה את פקודת הזירה ההיא נחה עם כסף אמיתי וללא מעקב; ונסיגת `trade_id` ל-`""` הפכה למפתח ראשי, כך שהעסקה חסרת המזהה הראשונה נכנסה וכל מאוחרת יותר התנגשה בה, והפילה נפח שבוצע בפועל לתוך `duplicates_ignored` עם `size_matched` בחסר. חמישה מז'וריים: `update_order_status` ביצעה commit ללא בדיקת `rowcount`; שלוש כתיבות ליומן האירועים עקפו את `_log_event`, כך שספריית יומן לקריאה-בלבד הפכה ללולאת נסיגה קבועה וכתיבת ה-`STOP` שבמחזור יכלה להיזרק מתוך מטפל הכיבוי הנקי עצמו; שאילתת היתרות של `merge` תפסה כל חריגה והותירה את היתרות בערכי האתחול `0.0`, ודיווחה שגיאת RPC או `POLY_FUNDER` לא מוגדר כ"מחזיק 0.00"; `merge` התנתה את בדיקת ההכרעה ב-`denom is not None`, כך שכשל של כל נקודות הקצה איפשר ל-`--live` להמשיך במקום ש-`redeem` מסרבת; ושער מניעת הכפילות חזר כרגיל על יומן פקודות בלתי קריא, ולאחריו `_log_order` מבודד את הקובץ וסדר על-שרשרת שני יוצא. שלוש בדיקות נוספו. כן תוקנו שתי סתירות במסמך הארכיטקטורה: הוא טען שהפקודות ממופתחות לפי מזהה פקודת הזירה בעוד הסכמה שלו עצמו מגדירה את ה-UUID המקומי כמפתח ראשי, וקבוצת הסטטוסים שלו השמיטה את `unattributed`. סך הבדיקות: **759/759 עוברות**. הכרעה **LIVE**.

* **2026-08-17 (תיקוני נכונות-קריאה של שלב 2 מתוך סקירת PR #32).** תוקנו שני ליקויים מסוג "קריאה שנכשלה הופכת להכרעה" ב-`strategy/` (קומיטים `968fe3a`, `a93e29f`, תועדו לאחר עקיפה ראשונית עם `--no-verify` של `.githooks/pre-commit`). (1) הוסרה נסיגת `except TypeError` הרחבה ב-`strategy/order_registry.py:reconcile_orders()`: קודם לכן, שגיאות בקשה או פענוח תשובה במהלך `get_trades(params=p)` נסוגו ל-`get_trades()` ללא סינון, ותשאלו את כל היסטוריית החשבון לתוך חלון שיוך של 60 שניות; `TradeParams(maker_address, after)` נתמך ב-`py_clob_client_v2 1.0.2` (`inspect.signature`), ולכן הקריאה אינה מוגנת וכל החריגות מגיעות לנסיגת הלולאה. (2) הוסבה גזירת נקודות עקום alt_bn128 הקנונית מ-`CTHelpers.sol:392-424` של Gnosis ל-`strategy/live_exec.py:get_collection_id()`: קודם לכן, גיבוב `keccak256` פשטני גזר מזהי פוזיציה שגויים (`67830396...` מול `32338220...` האמיתי), מה שגרם לבדיקות טרום-טיסה לדחות מיזוגים ממומנים בשוגג (קידוד calldata לא הושפע שכן מזהי התנאים מועברים ישירות). אומת בסבב מלא מול מזהי טוקן חיים מ-Polymarket Gamma API עבור שני ה-index sets, וכוסה ענף האב שאינו אפס עבור דטרמיניסטיות, חילופיות ($P_A + P_B = P_B + P_A$), ודחיית נקודה מחוץ לעקום ($x=4$) ב-`tests/test_live_exec.py`. סך הבדיקות: **764/764 עוברות** (עלייה מ-761, 759 בסיס). הכרעה **LIVE**.

* **2026-08-17 (ביקורת המשך — שלושה ליקויי דיאגנוסטיקה, מפגש 72).** הביקורת החוזרת על הקומיטים שנדחפו העלתה ארבעה ממצאים; שלושה היו אמיתיים. בלוק הדיווח של `merge` עיצב את `up_bal` ו-`dn_bal` מערכי האתחול `0.0` בין אם שאילתת היתרה הצליחה ובין אם לאו, ולכן קריאה כושלת הדפיסה `token_up <id> (held: 0.00)` מיד מעל שורת השמירה הקובעת שהאחזקות אינן ידועות ואינן אפס — והמספר הוא האמירה שמפעיל מאמין לה; כעת מודפס `unknown` כאשר `balance_error` מוגדר. ה-`try` של שמירת האידמפוטנטיות עטף גם את סריקת הרשומות ולא רק את הקריאה והפענוח, ולכן שגיאה שנזרקה תוך מעבר על הרשומות הייתה מדווחת כ"לא ניתן לקרוא את יומן ההזמנות", אבחנה שגויה עבור קובץ שפוענח כשורה; ה-`try` מכסה כעת רק את הקריאה והפענוח, ומשרשר את הסיבה. והבדיקה `test_get_trades_type_error_propagates` אימתה ספירת קריאות בלבד, מה שעובר באותה מידה אם המימוש נסוג לקריאה אחת *ללא סינון*, ולכן היא מאמתת כעת ש-`params` קיים ב-kwargs. הרביעי נדחה: תאריך הרשומה `2026-08-17` סומן כעתידי, אך הקומיט חתום `Mon Aug 17 01:31:08 2026 +0300` והיומן מתארך בשעון מקומי — הסוקר קרא את הרגע ב-UTC. סך הבדיקות ללא שינוי: **764 עוברות**. הכרעה **LIVE**.

* **2026-08-17 (נעילת מעבר פיוס — שער שלב 3, מפגש 72).** כל נתיב כתיבה ברישום נוטל `BEGIN IMMEDIATE`, ולכן אף כתיבה בודדת אינה מתחרה — אך מעבר פיוס משתרע על פני כמה טרנזקציות כאלה עם שתי פניות זירה ביניהן, ולכן שני מעברים מקבילים קוראים שורה כ-`open`, שניהם מכריעים מעבר מתוך אותה קריאה מיושנת, והכתיבה המאוחרת מנצחת ומשאירה מצב שאף מעבר בודד לא הכריע. התצורה שגרתית: `poll` במסוף אחד, פיוס חד-פעמי במסוף אחר. הנעילה היא לפיכך שורה ב-`live.db` (`reconcile_lock`, `id = 1` נאכף ב-CHECK) ולא `threading.Lock`, שהייתה עוברת בדיקה על אותו אובייקט ועדיין מאפשרת לשני תהליכים להתחרות. נדחתה החלופה המתבקשת של החזקת טרנזקציה אחת לאורך כל המעבר: היא מחזיקה את נעילת הכתיבה של SQLite למשך פניות הרשת וחוסמת כל קורא כל עוד הזירה לא עונה. תחרות זורקת `ReconcileInProgress` ואינה ממתינה בתור, שכן מעבר שהמתין לתורו היה מתחיל מקריאות שהמעבר שלפניו כבר ביטל. סף התיישנות 300 שניות — מעבר לגיטימי הוא שניות גם עם ניסיונות חוזרים, ולכן השבה יכולה לבוא רק אחרי קריסה; נמוך מדי מחזיר את התחרות, גבוה מדי מאריך את ההשבתה אחרי `kill -9`, וההשבתה זולה יותר. השחרור מוחלט ומוגבל למחזיק שלנו, כך שמעבר שהושב אינו יכול למחוק שורת מחזיק חי. לולאת ה-`poll` אינה סופרת עוד תחרות כשגיאת זירה: קודם לכן היא הייתה דוחפת את הנסיגה ל-60 שניות משום שמסוף אחר החזיק את הנעילה מילישניות, וכעת נרשם `SKIPPED` במרווח הרגיל בעוד `--once` עדיין יוצא עם קוד שאינו אפס. שמונה בדיקות. סך הבדיקות: **772 עוברות**. הכרעה **LIVE**.

* **2026-08-17 (שלב 3 — יציאת רגל חשופה בהעברה מכלל הסימולציה, מפגש 72).** הועבר `sweep.py:700-830` ללא שינוי — הטריגר הופעל 16 פעמים על פני 26,777 זוגות, תמיד על `pair_cost >= max_pair_cost`, בעלות 3.67 סנט ליציאה מול 3.68 סנט לכל זוג שהושלם — ורק רצף הפעולות סביבו נכתב מחדש, משום שמה ששונה בחי הוא משטח הכשל ולא הכלל. הנתיב החי הוא `ביטול -> קריאה מחדש -> מכירה`: מכירה תחילה מותירה הזמנה חיה שיכולה למלא מחדש את הפוזיציה הנסגרת; ביטול כושל מבטל לפני כל מכירה; והקריאה מחדש קיימת משום שביטול מוצלח אינו אומר שהזוג עדיין חד-צדדי — הביטול יכול להתחרות בהתאמה שכבר קרתה, ומכירת רגל אחת של זוג שהושלם הופכת נכס ששווה דולר במיזוג להפסד ממומש, התוצאה הגרועה ביותר בנתיב, ולכן הוא מנותב ל-`merge`. הגודל הוא `min(גודל חשוף ברישום, עומק ביקוש)` הנגזר ממילויים ולעולם לא מהגודל המיועד, עם סירוב עודף מעל מספר הרישום שכן מכירת יתר אינה ניתנת לביטול. בדיקת `/positions` ב-Data API נחתה כאן כפי שהוגדר ב-`b22eada`, רצה לפני כל כתיבה לזירה כך שסטייה אינה עולה דבר, נכשלת סגור הן על נקודת קצה בלתי קריאה והן על `POLY_FUNDER` שאינו מוגדר, בסבילות `1e-6` בגודל אבק נקודה צפה ולא של אי-הסכמה אמיתית. היעדר ביקוש מוכר מפעיל ואינו מחזיק, שכן אין מול מה להשלים. ארבע-עשרה בדיקות, ללא רשת באף אחת. סך הבדיקות: **786 עוברות**. הכרעה **LIVE**.

* **2026-08-17 (שלב 4 — השלמת רגל שנייה, מפגש 72).** חצייה לרגל החסרה הופכת פוזיציה חצי-פתוחה לפוזיציה ששווה דולר במיזוג, ולכן היא מקטינה חשיפה ואינה פותחת אותה — אך רק כל עוד התקרה מחזיקה, ולכן התקרה היא כל המשמעת כאן. חצייה שמביאה את `pair_cost` לתקרה או מעליה היא הפסד ודאי אחרי גז, וסגירת פוזיציה כזו שייכת לנתיב היציאה, ולכן ההשלמה זורקת במקום לחצות; הגבול הוא `>=` בשני הצדדים, כך ששני הנתיבים מחלקים את המרחב ללא פער וללא חפיפה. הגודל נגזר מ-`size_matched` ולעולם לא מהגודל המיועד — רגל שמילאה 4 מתוך 10 היא פוזיציה של 4 מניות, והשלמת 10 הייתה פותחת 6 מניות של חשיפה חדשה בצד השני, בדיוק ההיפוך האסור — מוגבל שוב בעומק ההיצע כך שספר דק מפיק השלמה חלקית ולא טיפוס בסולם, ומוגבל ב-`MAX_ORDER_USD`, שאינו מבוטל רק משום שההזמנה מקטינה חשיפה. היעדר היצע מסרב ומעביר את המקרה לכלל היציאה, שמופעל על אותו תנאי. תשע בדיקות כולל תנאי הקבלה: זוג שהושלם נקרא כמוחזק בשתי הרגליים, וזה מה שבדיקת הטרום-טיסה של `merge` מיישבת מולו. סך הבדיקות: **795 עוברות**. הכרעה **LIVE**.

* **2026-08-17 (המשך שלב 4 — הקנייה בהשלמה נקובה במניות, מפגש 72).** `MarketOrderArgsV2.amount` הוא הסכום המוסר — מה שאנחנו נותנים — ולכן הוא מספר מניות במכירה ודולרים בקנייה, כפי שאומת בקריאת `get_market_order_amounts` ב-SDK המותקן (`maker_amount = amount`, `taker_amount = amount / price` בקנייה). המכירה בשלב 3 העבירה מניות והייתה נכונה; ההשלמה בשלב 4 העבירה מניות ולא הייתה. השלמה של 10 מניות בהיצע 0.30 דולר הייתה שולחת קנייה של 10 דולר ורוכשת כ-33 מניות — 23 מניות של חשיפה חדשה על הרגל שהנתיב נועד לסגור, בדיוק ההיפוך ששלב 4 נכתב כדי למנוע. אף שמירה לא יכלה לתפוס זאת, משום שכולן אימתו את 3.00 הדולר שהתכוונו אליהם, והלקוח המדומה קיבל מספר מניות ללא תלונה. תוקן בהעברת `notional`; שתי בדיקות מקבעות כעת את היחידה בשני הצדדים, והמדומה מתעד ארגומנטים במבנה ולא כמחרוזת מעוצבת, שכן בדיקה הקוראת מספר אינה מבחינה בין דולרים למניות. סך הבדיקות: **797 עוברות**. הכרעה **LIVE**.

* **2026-08-17 (שלבים 3/4 סבב ביקורת שני — שישה חורים אמיתיים, מפגש 72).** שמונה ממצאים, שבעה אמיתיים. החמור: הקריאה מחדש בין הביטול למכירה שאלה את `run/live.db`, אך מילויים מגיעים לשם רק דרך `reconcile_orders` בלולאת ה-poll, ולכן התאמה שהשלימה את הרגל הקלה במהלך הביטול לא נראתה עד המחזור הבא — הצעד שתועד כ"אינו רשות" אישר את מה שכבר האמנו לו ומכר לתוך המרוץ שנועד לתפוס. כעת הוא קורא את הזירה דרך `get_order` ומסרב על הזמנה בלתי קריאה, שדה גודל-מותאם חסר או ערך לא מספרי; הפרוזה הייתה הליקוי לא פחות מהקוד. שני הנתיבים גם השאירו הזמנות פעילות: היציאה ביטלה רק את הרגל הקלה, ולכן רגל כבדה ב-`partial` יכלה למלא מחדש מיד אחרי המכירה, וההשלמה לא ביטלה דבר, ולכן הקנייה הרוכשת שלה והקנייה החוצה יכלו להכפיל את הרגל הקלה — חשיפה שנוצרה בידי הנתיב שנועד להסירה. שער הסטייה השתמש ב-`abs()`, ולכן עודף בזירה חסם את היציאה וסירב לפעולה היחידה שסוגרת חשיפה; כעת מסרבים רק על חוסר, וטוקן נעדר עדיין מסרב שכן היעדרות אינה אפס. למכירה לא היה גבול מחיר; רצפה מיושרת-טיק ב-`best_bid - 0.02` מגבילה כעת, והעומק נספר רק מעליה. `/positions` השמיט אחזקות תת-מנייתיות בברירות המחדל שלו; כעת `sizeThreshold=0` עם עימוד. ו-`complete_pair_cmd` שלח קנייה אמיתית ללא שמירת אידמפוטנטיות וללא בדיקת פוזיציות. שמונה בדיקות חדשות. סך הבדיקות: **805 עוברות**. הכרעה **LIVE**.

* **2026-08-17 (שלבים 3/4 סבב ביקורת שלישי, מפגש 72).** שלושה ממצאים אמיתיים נוספים. `exit_pair` שלח מכירת שוק חיה ללא שמירת אידמפוטנטיות וללא שורת ביקורת, בעוד ש-`merge`, `redeem` ו-`complete_pair_cmd` מחזיקים בשתיהן — ומכיוון ש-`naked` נגזר מהרישום ומילויים מגיעים רק דרך לולאת ה-poll, הפעלה שנייה מיידית לא ראתה את המכירה הראשונה ושלחה אותה שוב; בדיקת ה-Data API תפסה זאת רק כל עוד `--skip-positions-check` לא נמסר, כלומר בדיוק לא במקרה שבו מפעיל עוקף נקודת קצה תקולה. `_cancel_orders` ו-`_venue_matched` זרקו `PairExitRefused` גם מנתיב ההשלמה, ו-`complete_pair_cmd` תופס רק `PairCompletionRefused`, ולכן ביטול כושל חמק אל המטפל הכללי, סימן את שורת הביקורת `interrupted` אף שדבר לא נשלח, וחסם כל השלמה מאוחרת על אותו תנאי עד `--force` — סירוב שחוסם את הניסיון הבא גרוע מהכשל שהוא מדווח עליו. ו-`_floor_to_tick` השתמש ב-`int(price / tick)` חשוף, שבנקודה צפה בינארית מוריד את `0.29` טיק שלם ל-`0.28`, ומציב את גבול המכירה מתחת לנבחר תוך ספירת רמת עומק נוספת. ארבע בדיקות חדשות. סך הבדיקות: **805 עוברות**. הכרעה **LIVE**.

* **2026-08-17 (שלבים 3/4 סבב ביקורת רביעי — השמירה חסמה את ההתאוששות שלה, ואף נתיב CLI לא רץ, מפגש 72).** שורת הביקורת סימנה כל תוצאה שאינה חריגה כ-`submitted`, אך `exit_naked_leg` מחזיר `route_to_merge` כשהרגל הנחה מתמלאת במהלך הביטול וענף זה אינו שולח מכירה; `exit_pair` מדפיס אז "הרץ merge" בעוד `_check_idempotency_guard` מסרב למיזוג משום שהתנאי נושא שורת `submitted`, ומותיר את `merge --force` כדרך היחידה בהתאוששות רגילה. רק תוצאה ששלחה בפועל מחזיקה כעת את התנאי; תוצאות שאינן שולחות נסגרות כ-`cancelled`. כתיבת הבדיקה לכך חשפה את הבעיה הגדולה יותר: `from strategy.config import Config` אינו קיים, הגישה היא `config.load()` המחזיר `MakerConfig`, ולכן גם `exit_pair` וגם `complete_pair_cmd` היו קורסים בשורת העבודה הממשית הראשונה בכל הפעלה מאז שנכתבו. שלושים ושבע בדיקות עברו משום שכולן קראו ישירות אל `live_pairs` ואף אחת לא עברה דרך הפקודה שמפעיל מקליד — סוויטה שמפעילה את הספרייה ולא את נקודת הכניסה מדווחת בריאות מלאה על תוכנית שאינה עולה. סך הבדיקות: **811 עוברות**. הכרעה **LIVE**.

* **2026-08-17 (שלבים 3/4 סבב חמישי — הרצת הפקודות, מפגש 72).** הרצתי את שתי הפקודות ללא `--live` מול מזהה זוג שאינו קיים, בדיקת העשן הזולה ביותר האפשרית. היא אישרה שה-`ImportError` של הקונפיג נעלם — שתיהן מגיעות כעת לקריאת הרישום, העבודה הממשית הראשונה שלהן — ומצאה שני דברים שארבעים ואחת בדיקות לא מצאו: `load_pair` זורק לפני בלוק ה-`try` של שתי הפקודות, ולכן שגיאת הקלדה הפיקה traceback במקום הודעת סירוב, ובנתיב ההשלמה `load_pair` מסמן עם `PairExitRefused` ש-`complete_pair_cmd` אינו תופס כלל. זו אותה אי-התאמת חריגות שנמצאה סבב קודם בעוזרים, ששרדה במקום אחר משום שהתיקון הוחל היכן שהביקורת הצביעה ולא בכל מקום שבו הדפוס מופיע. שתיהן יוצאות כעת עם `EXIT REFUSED` / `COMPLETION REFUSED` והסיבה. הרישום אינו מחזיק זוגות, ולכן לא ניתן היה לתרגל דבר עמוק יותר מנתיב הארגומנטים: ריצה יבשה מול `pair_id` חי נותרה לא נבדקה, ושלב 4.5 הוא הפעם הראשונה שבה הפקודות יפגשו אחד. סך הבדיקות: **814 עוברות**. הכרעה **LIVE**, עם ההסתייגות מתועדת.

* **2026-08-17 (שלבים 3/4 ביקורת עצמית — כבד וקל הם דירוג, מפגש 72).** מעבר עצמאי קפדני על ה-diff מצא חמישה ליקויים, שניים מהם אותו ליקוי פעמיים. `load_pair` מדרג את הרגליים לפי גודל מותאם, ולכן קריאה שנייה אחרי שהרגל הקלה עוברת את הכבדה מחזירה אותן מוחלפות — וגם `exit_naked_leg` וגם `complete_pair` גזרו מחדש את התפקידים מהדירוג שאחרי הביטול, ואז חיסרו ממנו את `venue_light_matched` שנקרא מול ההזמנות הקלות המקוריות. עם UP כבד ב-10 והזמנת DOWN קלה ל-12 שמתמלאת במהלך הביטול, `after` מדרג את DOWN ככבד, `max(10, 12)` נותן 12, `naked` יוצא אפס, והיציאה מדווחת `route_to_merge` בעוד שתי מניות DOWN חשופות; ההשלמה מחזירה `balanced` באותה אריתמטיקה. אף אחד אינו שולח הזמנה שגויה, אך שניהם אומרים למפעיל שפוזיציה סגורה כשאינה. `load_pair` מחזיר כעת רגליים לפי טוקן ו-`_naked_after` מצמצם שני מזהים קבועים. כמו כן: שמירת ה-`pair_cost` של ההשלמה נמדדה לפני הביטול ולא נבדקה שוב, ולכן הזמנה כבדה שהתמלאה במחיר גרוע אפשרה חצייה מעל `max_pair_cost`; ההשלמה ביטלה רק את הרגל הקלה והשאירה הזמנה כבדה שתפתח את הזוג מחדש; ו-`load_pair` צמצם בשקט `pair_id` בן שלושה טוקנים לשתי רגליו הגדולות. חמש בדיקות חדשות. סך הבדיקות: **819 עוברות**. הכרעה **LIVE**.

* **2026-08-17 (שלבים 3/4 — הסימן, והשוואת שתי אוכלוסיות שונות, מפגש 72).** הסבב הקודם גרם ל-`_naked_after` להחזיר הפרש מסומן ואז התעלם מהסימן: שני הנתיבים קראו `naked <= SIZE_EPS` כ"אין מה לעשות", ולכן רגל כבדה ב-10 מול קלה ב-12 נתנה -2 והיציאה דיווחה `route_to_merge` בעוד ההשלמה דיווחה `balanced` — הפוזיציה עדיין חד-צדדית, פשוט על הטוקן האחר. בדיקת הרגרסיה מאותו סבב אימתה את התשובה השגויה, וכך תיקון לתוצאה משקרת נשלח כשהשקר הועתק ולא הוסר. אפס הוא כעת סגור, חיובי פירושו שהטוקן הכבד המקורי חשוף, שלילי פירושו שהקל המקורי חשוף, והמקרה השלילי מסרב ונוקב בטוקן ובגודל. שנית, התקרה שאחרי הביטול עדיין לא ראתה מילוי כבד חי: רק ההזמנות הקלות נקראו מהזירה בעוד `fill_cost_after` הגיע מהרישום המפגר במחזור, ולכן הזמנה כבדה שהתמלאה בזמן הביטול שלה נותרה בלתי נראית לבדיקה שנועדה לתפוס אותה. שתי הרגליים נקראות כעת, וגודל כבד שהרישום לא תמחר מסרב את החצייה. תיקון זה חשף ליקוי שלישי בתיקון עצמו: `_venue_matched` סוכם רק הזמנות פעילות בעוד נתון הרגל מכסה הכול, ולכן ההשוואה השוותה שתי אוכלוסיות; היא כעת לפי הזמנה. סך הבדיקות: **821 עוברות**. הכרעה **LIVE**.

* **2026-08-17 (טרום-טיסה לשלב 4.5 — `quote` מעולם לא כתב לרישום, מפגש 72).** שלב 4.5 הוא הזמנה חיה אחת בהשגחה שנועדה להוכיח פרסום -> זיהוי -> השלמה -> מיזוג -> מזומן, והשרשרת הייתה שבורה בחץ הראשון: `quote` פרסם את שתי הרגליים והוסיף ל-`run/live_orders.json` מבלי ליצור `OrderRecord` אי פעם, ולכן גם לא הקצה `pair_id`. ל-`reconcile_orders` לא היו שורות לפייס ושלב 2 לא יכול היה לזהות מילוי בהזמנה שפקודה זו הציבה; `exit` ו-`complete` מקבלים `pair_id` שלא היה קיים; ושתי הרגליים היו נחות בזירה עם כסף אמיתי וללא מעקב — בדיוק הכשל שהרישום נבנה למנוע, בפקודה היחידה ששלב 4.5 תלוי בה. ההזמנה הייתה יוצאת ואף שלב אחריה לא היה רואה אותה. `quote` כותב כעת שורה לכל רגל תחת `pair_id` משותף לפני השליחה, שכן שורה שנכתבת אחרי שליחה מוצלחת אובדת בקריסה שביניהן ואילו שורה שנכתבת לפני היא לכל היותר `pending` לאימוץ יתומים; מזהה הזירה מצורף בתשובה, ותשובה ללא מזהה משאירה את השורה `pending` עם אזהרה במקום לנחש מזהה שהיה קושר את השורה שלנו להזמנה של אחר. הפקודה מדפיסה את ה-`pair_id` ואת פקודות ההמשך, ותת-פקודת `pairs` מציגה כל זוג עם גדליו. חמש בדיקות. סך הבדיקות: **824 עוברות**. הכרעה **OPEN** — 4.5 עדיין מותנה באישור שלבים 3-4, ואף נתיב כאן לא פגש זוג אמיתי.

* **2026-08-17 (ריצה יבשה לשלב 4.5 — חמישה צעדים מול שוק אמיתי, מפגש 72).** עם שלבים 2-4 מוזגים ו-824 בדיקות מבוססות-פיקסצ'רים עוברות, הורצה השרשרת מול שוק חי ממומן-תגמולים ללא `--live` בשום מקום. `quote` יבש הדפיס שתי רגליים ב-0.480 / 0.520 עבור 5.00 דולר מול תקרה של 25 ולא שלח דבר — ושני מזהי הטוקן שגזר הם אותו זוג שבדיקת `getCollectionId` מקבעת עבור תנאי זה, אישור עצמאי שההסבה ל-alt_bn128 נכונה עבור שוק שלא נבחר לכך. הרישום נשאר ריק כנדרש. `poll --once` אימת, פייס ויצא 0 עם `errors=0` וכתב `START` ו-`EXIT` ליומן; שורת ה-400 השפירה המוכרת הופיעה כמתועד. `pairs` דיווח שאין זוגות, ו-`exit`/`complete` על מזהה לא מוכר סירבו עם סיבות וללא traceback. הליקוי היחיד: השוק הראשון לא היה ממומן-תגמולים ו-`quote` דיווח עליו כ-`not found`, משום ש-`fetch_pinned_market` מחזיר `None` גם לשוק חסר וגם לשוק שאינו משלם תגמולים — ושולח מפעיל לחפש שגיאת הקלדה שאינה קיימת, אותה צורה כמו דיווח יתרה שלא נקראה כ-`0.00`. המקרים הופרדו. סך הבדיקות: **825 עוברות**. הכרעה **LIVE** לשרשרת ככל שקריאות מתרגלות אותה, **OPEN** ל-4.5 עצמו.

* **2026-08-18 (פיוס כלכלת האסטרטגיה, נתיב מכירה שלא הופעל, שכפול 10 דרגות Bankroll).** ביקורת ישירה לקריאה-בלבד של `run/fleet.db` (494 סגירות, 1,059 מילויים) ו-10 מסדי נתוני Bankroll ($100–$1,000): (1) **נתיב מכירה לא הופעל:** כל 4 השורות בקבוצת מפגש 68 הן אבק נקודה צפה ($<10^{-13}$ מניות) שנוצר מהיעדר הידוק אפסילון ב-`profit_take.py:100` (`size > 0`); הנתיב לא נבדק ולא הופרך; תיקון האפסילון מושהה עד לאיפוס מדגם. (2) **כלכלת EV נמדדת (עידן 2, n=319):** הוחלפו קבועי $n=4$ ב-`config.py:657-658` במספרי אוכלוסייה אמיתיים — רווח מיזוג **3.3705 סנט/מניה** ($n=286$ מיזוגים, 19,229 מניות), עלות יציאה **2.5136 סנט/מניה** ($n=18$ יציאות, 1,290 מניות), שיעור השלמה **91.54%** (292 הושלמו / 18 יציאות / 9 פקעו), נקודת איזון בהפסד יציאה 2x יורדת מ-66.61% ל-**59.86%**, מרווח ביטחון **+31.68 נקודות אחוז**. (3) **טלמטריית Markout ובחירה שלילית:** 69.6% היעדרות טרום 08-13 נפתרה בקומיט `4f4ba99` (דוגם `mid_h3`); ה-Markout ל-15 דקות הוא **−0.809 סנט/מניה** עם רווח סמך 95% של $[−3.170, +1.552]$ הכולל אפס (בחירה שלילית אינה ניתנת להבחנה סטטיסטית מאפס במדגם הנוכחי). (4) **שכפול 10 דרגות Bankroll:** 10 הריצות חולקות חפיפת ג'קארד ממוצעת של **91.91%** מול פיד דירוג מסונכרן (ניסוי יחיד ב-10 סקאלות, לא 10 מדגמים עצמאיים); סקאלת הון מנבאת שיעור השלמה ($r=+0.6228, R^2=0.3879$) דרך הפחתת פקיעת חלונות (13.8% ב-$100 ← 2.8% בראשי); שיעור הצי הראשי 91.54% יושב בתוך רווח התחזית 95% $[76.16\%, 95.45\%]$ (תחזית 85.80%). הכרעה **LIVE** על הכלכלה הנמדדת, **PARKED** על נתיב המכירה והאפסילון, **OPEN** על שלב 4.5.

* **2026-08-18 (אבן דרך 2: חילוץ תת-עץ המסחר החי, ביטול צימוד בחבילת Namespace, ביקורת צימוד תצורת הסימולציה).** חולצו מודולי המסחר החי (`order_registry.py`, `live_pairs.py`, `live_exec.py`) ובדיקותיהם לתת-עץ מבודד `live/`. הוגדרה חבילת `strategy` כ-implicit namespace package (נמחקו `strategy/__init__.py` ו-`live/strategy/__init__.py`) עם תעדוף `live/` ב-`sys.path`. נבנתה רתמת בטיחות הרמטית ב-`live/tests/conftest.py` (ניקוי אישורי גישה + חוסם שקעי רשת) ובדיקות קדימות ב-`live/tests/test_live_conftest.py`. סך הבדיקות: 703 שורש + 129 live = 832 עוברות (829 בסיס + 3 בדיקות שמירה חדשות). עודכן ה-CI עם צעדים נפרדים. זוהו שני ממצאים ארכיטקטוניים: (א) הנחה שהופרכה — `live_exec.py` מייבא את `strategy.markets` (שורות 337, 1263, 1289) ואת `strategy.config` (שורות 391, 1009, 1787, 1891); (ב) צימוד תצורת סימולציה — `live_exec` קורא את `max_pair_cost` מסריקות סימולציה, דבר הדורש חוזה תצורת סיכון חי עצמאי ונפרד. הכרעה **OPEN**.

* **2026-08-18 (אבן דרך 4: יישור ממשק פקודות מול זירת המסחר, ברירת מחדל בטוחה של postOnly, פועלי ביטול עצמאיים וביקורת טלמטריית זמני השהיה).** מיפוי ממשק פקודות CLOB הרשמי והשוואה מול 11 תת-פקודות ה-CLI. נוספו `--post-only` (ברירת מחדל `True`) ו-`--tif` (`GTC`/`GTD`/`FOK`/`FAK`) יחד עם `--expiration` לפקודת `quote()`, תוך אכיפת דחייה בזמן פענוח של שילובים לא חוקיים (`post_only` עם `FOK`/`FAK`, ו-`GTD` ללא תפוגה); אומת שנתיבי taker (`exit`, `complete`) נותרים קשיחים על `post_only=False`. מומשו פועלי סגירה עצמאיים `cancel <order_id>` ו-`cancel-market <condition_id>` עם טיפול סירוב fail-closed וסנכרון רישום (מאושרים מראש תחת כלל הבטיחות המעודכן ב-`AGENTS.md`). נסגרה קבוצה ב' עם בדיקות עבור `cancel-all`, `status`, `balance` ו-`pairs`. זוהה ערוץ ה-WebSocket של המשתמש (`wss://ws-subscriptions-clob.polymarket.com/ws/user`) כמקור חקר אמת לטלמטריית זמני השהיה per-order sent-to-landed. סוויטת שורש: 703 עוברות, סוויטת live: 143 עוברות (סך הכול 846 עוברות, 0 נכשלו). הכרעה **OPEN**.
 
* **2026-08-18 (אבן דרך 6: אטומיות ציטוט באצווה, אימות שתי רגליים fail-closed, והסרת ברירת מחדל ב-probe).** הסבת `quote()` משליחה רציפה לכל רגל לשימוש ב-`post_orders(batch_args, post_only=post_only)` של ה-SDK בתוך בקשת אצווה יחידה `POST /orders`, וביטול חלון החשיפה הרשתי בין הרגליים. הוספת פרימת חירום בכשל חלקי (ביטול מיידי של הרגל השורדת בדחיית רגל בודדת בזירה), שמירת שורות ב-`pending` בהיעדר מזהים לאימוץ יתומים בפיוס, ואימות fail-closed המתשאל `c.get_order(v_id)` להתאמת `asset_id` מול `leg['token_id']` (וביטול שתי הרגליים בכל אי-התאמה). הסרת `default="btc-up-or-down-5m"` מ-`probe()` ואכיפת בלעדיות הדדית בין `--series` ל-`--token-id`, תוך תיעוד סדרות חוזרות כ-fixture מדידת latency בלבד בהתחשב בכך שקריפטו מהווה 3.74% מ-66,317 הציטוטים בצי (טניס 51.71%, בייסבול 22.27%, ספורט אלקטרוני 18.98%). נוספו 10 בדיקות ב-`test_live_exec.py` ו-`test_live_pairs.py`. סוויטת שורש: 703 עברו; סוויטת live: 153 עברו (סך הכול 856 עברו, 0 נכשלו). הכרעה **LIVE**.

* **2026-08-18 (מדידה: הרצה יבשה נקייה אינה מוכיחה שנתיב המסחר החי ניתן לייבוא).** ציטוט `--live` הראשון בשלב 4.5 קרס עם `ModuleNotFoundError: No module named 'strategy.order_registry'` אחרי שהדפיס הרצה יבשה תקינה; `status --live` אישר `open orders 0 ($0.00 notional)`, כלומר לא נוצרה חשיפה. הסיבה: `quote()` מייבא את `strategy.markets` בשורה 360 (לפני היציאה המוקדמת של ההרצה היבשה בשורה 407) ואת `strategy.order_registry` בשורה 421 (אחריה), ולכן הרצה יבשה מפעילה רק את הראשון. חבילת המרחב המשתמע `strategy` פרושה על `live/strategy` ועל `<repo>/strategy` ומתפענחת רק כאשר שני ההורים נמצאים ב-`sys.path` — ותיקיית העבודה של המפעיל הכריעה זאת בשקט. נוסף אתחול `sys.path` ב-`live_exec.py` וכן `tests/test_live_exec_import_paths.py`, המנתח כל `from strategy.X import` נדחה ומייבא כל אחד משתי תיקיות העבודה; אומת שהבדיקות נכשלות בלי התיקון (3 מתוך 4) ועוברות איתו. סוויטת שורש: 724 עברו (+4); סוויטת live: 153 עברו (סך הכול 877 עברו, 0 נכשלו). הכרעה **LIVE** על התיקון, **OPEN** על כיסוי הרצות יבשות באופן כללי.

* **2026-08-19 (הפרדה: `live/` הופך לעץ עצמאי).** נתיב הכסף האמיתי הופרד מבנית מהסימולציה. שורש הכבילה היה שם החבילה המשותף: `live/strategy/` ו-`<repo>/strategy/` הם שם אחד ניתן-לייבוא, שמוזג על ידי Python לחבילת מרחב-שמות המשתרעת על שני העצים, ולכן `from strategy.markets import ...` בקוד החי קיבל בשקט את קובץ הסימולציה. התיקון המתבקש הוכח כשגוי לפני שאומץ — הוספת `__init__.py` ל-`live/strategy/` הופכת אותה לחבילה רגילה שמפסיקה למזג וגוברת לחלוטין, ושוברת את 34 קבצי הבדיקה בשורש המייבאים `strategy.fleet` באותו תהליך. במקום זאת שונה השם ל-`live/engine/`; `config.py` ו-`markets.py` הועתקו פנימה (שניהם עלים); `scripts/audit_settlement.py` הועבר ל-`live/scripts/` ולוח המחוונים ל-`live/dash/live_dash.py` (בשם `dash` ולא `server`, משום שבשורש כבר קיימת חבילת `server`). שורש המאגר הוסר מ-`sys.path` של live — ב-`live/pytest.ini` היה `pythonpath = . ..` ו-`live_exec.py` אתחל את `ROOT.parent`; `import strategy` מקוד חי מעלה כעת ImportError, כפי שנקבע בבדיקה. נוספו `live/FORKED_FROM.json` ו-`test_fork_drift.py`, הנכשל כאשר עותק שורש זז ומדפיס את ה-diff מול עץ העבודה, כך שסטייה מותרת אך לעולם אינה סמויה (`python live/scripts/refork.py` מנקה זאת). שני השומרים אומתו על ידי שבירתם. `run/live.db` המיושן, סכמה בלבד, הועבר ל-`run/archive/`. hook ה-pre-commit למחקר הורחב ל-`^live/engine/`, שקודם לכן עקף את הדרישה לחלוטין. מערכת השורש: 703 עברו; מערכת live: 180 עברו (סך הכל **883 עברו**, 0 נכשלו). הכרעה **LIVE**; **OPEN** על כך ש-`live/` עדיין קורא את `.env` שבשורש המאגר.

* **2026-08-19 (ה-CLI החי סירב ליקום שלו עצמו).** `live_exec.quote()` דרש תגמולי עושה שוק וסירב לכל שוק שהמדרג מסיים אליו - כל שמונת השווקים ב-`run/markets.json` הם `source: spread` עם `daily: 0.00`. נמדד על `run/fleet.db`: `merge` 476 סגירות בסך **+$1,172.35** בעלות זוג ממוצעת של **$0.96006** (מינימום 0.68, מקסימום 0.9901, רווח ממוצע $2.4629 לסגירה, $23.80 גז בסך הכול), `naked_exit` 22 סגירות בסך -$49.35, `sell` 7 סגירות בסך $0.00; צבירת התגמולים באותה הרצה עמדה על $0.2237 ליום מול $566.29 מחויבים, כשחלקנו בציון השוק הוא 2.8e-4. התגמולים הם כ-0.04% ליום מההון המחויב - ההכנסה היא המיזוג מתחת ל-$1.00. `strategy/sweep.py:507` כבר העביר `require_rewards=False` מאז שלכידת המרווח נחתה, ולכן הצי וה-CLI חלקו על מה ניתן לסחור. הדרישה הוסרה מנתיב הציטוט החי; הסירוב שנותר מונה את הסיבות שנשארו (חסר, סגור, אינו מקבל פקודות, אינו שני טוקנים). סוויטת שורש: 703 עברו; סוויטת live: 181 עברו (+1). הכרעה **DEAD** על שומר התגמולים, **LIVE** על מודל המיזוג, **OPEN** על העתקת `decide_quotes()` אל `live/engine/` כך שמחירי הנתיב החי יגיעו מהאסטרטגיה ולא מ-`--price`.

* **2026-08-19 (הסבת שכבת ההחלטה אל `live/engine/`, אבן דרך 4).** פוצלו `quotes.py`, `gate.py` ו-`risk.py` מ-`strategy/` אל `live/engine/` (עם שכתוב ייבוא ל-`engine.`), נרשמו הפיצולים ב-`live/FORKED_FROM.json`, ואומתה היעדר סטיית פיצול. נבנה `live/engine/market_feed.py` הקורא את `run/markets.json` עם אימות גיל, סכמה ונוכחות. נוספה `inventory_from_registry()` ב-`live/engine/order_registry.py` לשחזור מצב מלא מ-`live.db`. נוספה פקודת CLI לקריאה-בלבד בשם `decide` ב-`live_exec.py` (חסומה מבנית מביצוע שליחות, ללא דגל `--live`). הוערכו כל 8 השווקים מול ספרי CLOB חיים: 7 צוטטו, 1 נדחה מחוץ לטווח. שוק מוביל (`mlb-det-pit-2026-08-18`): עלות זוג $0.9420 (לכידה $0.0580 מתחת ל-$1.00, 5.80%), עלות שתי רגליים $84.72, הפסד חשוף מרבי $49.64. diff מול `run/fleet.db` אישר שהבדלי מחירים משקפים תנודות בספר הזירה בלבד. נוספו 16 בדיקות חדשות ב-`test_market_feed.py`, `test_live_quotes.py` ו-`test_live_exec_decide.py`. סוויטת שורש: 703 עברו; סוויטת live: 197 עברו (סך הכול **900 עברו**, 0 נכשלו). הכרעה **LIVE**.

* **2026-08-19 (אבן דרך 5: קשירת גודל לארנק האמיתי, כיול רצפת הזירה ודיווח חוסר).** קשירת הציטוט החי לכלל הקצאת הבעלים `couple_allocation = max(bankroll_usd * 0.01, 6.00)` ($3.00 לרגל) הנקרא מיתרת הארנק החי ($31.17 USDC). מומש גודל דולרי דינמי ב-`live/engine/risk.py:size_for()` כאשר `quote_shares == 0`. רצפת 50 המניות הוחלפה ב-5 מניות (מינימום הזירה), משום ששווקי מרווח בוגרים מייצרים $0.00 תגמולים. נוספו מחרוזות סירוב עם חוסר דולרי מפורש ב-`live/engine/quotes.py`. יושבו תקרות הבטיחות (`max_market_frac = 0.15`, `max_naked_usd = 6.0`, `MAX_ORDER_USD = 25.0`, `MAX_TOTAL_USD = 100.0`). נבנתה בדיקת שקילות פיצול הרמטית ב-`live/tests/test_fork_controlled_equivalence.py`. הוערכו כל השווקים הבוגרים ב-`live_exec.py decide all`: שווקי 50-50 מצטטים שני צדדים (`atp-tien-tiafoe-2026-08-18` בעלות כוללת $5.52, לכידה 6.50%, הפסד חשוף מרבי $2.97; `atp-nestero-hemery-2026-08-17` בעלות $5.60, לכידה 6.70%, הפסד חשוף $2.83); שווקים מוטים מצטטים את הצד הזול ומדווחים חוסר ביקר. סוויטת שורש: 703 עברו; סוויטת live: 199 עברו (+2 בדיקות חדשות). סך הכול **902 עברו**, 0 נכשלו. הכרעה **LIVE**.

* **2026-08-19 (כלל ההקצאה של הבעלים בעץ החי, ושני השומרים שכמעט אבדו).** מומש `couple = max(bankroll x 0.01, 6.00)`, רגל = couple / 2 -- $6.00/$3.00 גם ב-$31.17 וגם ב-$100. סטיות בעץ החי: `bankroll_usd` מ-1000 ל-100, `min_quote_shares` מ-50 ל-5 (50 היה `rewardsMinSize` והשווקים שסיימו משלמים $0.00), `max_naked_usd` מ-120 ל-6.0, ובנוסף שדה חדש `size_mode`. שני שומרים כמעט אבדו בדרך: על `quote_shares == 0` הועמסה משמעות "חשב גודל מדולרים" בעוד שזהו אות ביטול המימון של המקצה (האירוע ב-`risk.size_for` שבו 17 שווקים לא-ממומנים המשיכו לפרסם), ו-`band.size_mult` -- מרסן סיכון המחיר -- בוטל במצב דולרי בטענה שחסם ההון מכסה אותו, מה שאינו נכון. שניהם שוחזרו. שחזור המרסן חשף מגבלה אמיתית: רגל של $3 קונה כ-6 מניות מול רצפת זירה של 5, ולכן כל ריסון מתחת ל-x0.84 מוחק את הפקודה -- נמדדו x0.46 ו-x0.57 על ספר 0.525/0.475, ולא צוטט דבר. `waive_attenuation_below_floor` (חי, מצב דולרי בלבד) מצטט את הרצפה ומצהיר על הוויתור בנימוק הפקודה; חסימת הרצועה הקשיחה 0.10-0.90 לא נגעה, והוויתור מתבטל מעצמו ככל שהבנקרול גדל. סוויטת שורש: 703 עברו; סוויטת live: 199 עברו. הכרעה **LIVE**; **OPEN** על שקילות במצב הדולרי, שאין לו מקבילה בסימולציה ונדרשת עבורו בדיקת אפיון.
 
* **2026-08-19 (אבן דרך 6: קביעת גודל הזוג במניות, אפיון מצב דולרי וטיוטת LiveFillEngine).** קביעת גודל הזוג במניות $N = \lfloor \text{couple\_allocation} / (p_{up} + p_{dn}) \rfloor$ ($N = \max(N, 5)$) בעלויות דולריות שונות לכל רגל, מה שביטל לחלוטין חשיפה חשופה ולא מגודרת (למשל 10 מניות ב-BTC, 9 ב-Braves, 7 ב-Athletics, 4 ב-Tien vs Tiafoe). קיבוע התנהגות המצב הדולרי בבדיקות אפיון (ספרים קפואים, מניות/מחירים/נימוקים מדויקים, ויתור ריסון ומחסור מול רצפת הזירה). הוכחת אות ביטול המימון (`quote_shares == 0` שותק במצב דולרי). כתיבת `LiveFillEngine` התואם לממשק של `sweep.py` ומגובה ב-`OrderRegistry` עם שומר קשיח ש-`on_book` לעולם אינו ממציא מילויים סינתטיים. סוויטת שורש: 703 עברו; סוויטת live: 205 עברו (+6 בדיקות חדשות). סך הכול **908 עברו**, 0 נכשלו. הכרעה **LIVE**.

* **2026-08-19 (ספר מאוזן מצטט זוג או כלום).** אומת שקביעת הגודל לפי מניות-זוג עומדת גם בקנה מידה -- בנקרול של $100/$1,000/$5,000/$20,000 מול ספר 0.24/0.74 החזיר 6/6, 10/10, 53/53 ו-212/212 מניות, ולכן הגידור אינו תוצר של רצפת הזירה. החור שנותר היה רצועת המחיר החוסמת צדדים בנפרד: על ספר 0.11/0.87 עם מלאי מאוזן, UP נחסם ו-DOWN לבדו החזיר 53 מניות -- פוזיציה חשופה מעצם הבנייה, שניתן לסגור רק בחציית הספר ותשלום המרווח שהציטוט נועד להרוויח. נוסף `require_two_sided_when_flat` לעץ החי: מלאי מאוזן עם פחות משתי כוונות מחזיר אפס ונוקב בצד שנחסם. מלאי לא מאוזן לא נגע במכוון, שכן שם הכוונה הבודדת היא הצד הקל וסירוב לה היה מקבע את הפוזיציה בחשיפה מרבית. סוויטת live: 206 עברו; סוויטת שורש: 703 עברו. הכרעה **LIVE**.

* **2026-08-19 (שלב 4.5: מחזור עושה-שוק חי ראשון אושר מקצה לקצה).** ביצוע מחזור מלא ראשון עם כסף אמיתי ב-Polymarket (`mlb-cws-chc-2026-08-18`). צוטטו 5 זוגות ב-$0.625 UP ו-$0.320 DOWN (סך התחייבות $4.72, עלות זוג $0.9450) דרך `live_exec.py quote --live`. שתי הפקודות מולאו ב-100% ב-CLOB (הוחזקו 5.0 מניות White Sox ו-5.0 מניות Cubs). אובחן מבנה נתוני העסקאות: תוקן `order_registry.py` לפענוח רשימת `maker_orders` של Polymarket כך שביצועים משויכים כשורה לרשומות המקומיות. בוצע מיזוג ברילייר ללא עמלות גז (`live_exec.py merge --live`), ונשרפו 5 זוגות כנגד $5.00 USDC בטוחה שהוחזרה לארנק (`STATE_EXECUTED`, טרנזקציה `0x4f802f221594c873764294972f5d14b9152c1fe54f65f084155100beb0e8cb2e` [נמדד]). סך עלות: $4.70, רווח נקי שנלכד: **+$0.30** (+6.38% תשואת מרווח) ללא כל תלות בתוצאת המשחק. יתרת ארנק מאומתת: $101.88 USDC. סוויטת live: 206 עברו; סוויטת שורש: 703 עברו. הכרעה **LIVE**.

* **2026-08-19 (אבן דרך 7 חלק א': שחזור רשומות רג'יסטרי וביקורת משולשת חיה).** שופר `reconcile_orders` ב-`live/engine/order_registry.py` עם חלון היסטוריה גמיש וסנכרון סטטוסים לכל הפקודות הרלוונטיות. בוצע סנכרון של `live.db` מול היסטוריית הזירה עבור `pair-0e95617d0595`, ושוחזרו בהצלחה 2 ביצועים (5.0 UP ב-0.620, 5.0 DOWN ב-0.320) עם מעבר סטטוס מ-`cancelled` ל-`filled`. נבנה מודול ביקורת משולשת (`live/engine/audit.py`, `live_exec.py audit <pair_id|condition_id>`) המשווה ביצועי רג'יסטרי, ביצועי זירה ויתרות טוקנים ב-on-chain בניכוי מיזוגים. אומת פלט `STATUS: AGREE` (5.0 מולאו, 5.0 בוצעו, 5.0 מוזגו, 0.0 ב-on-chain). נוספו 3 בדיקות יחידה ב-`live/tests/test_audit.py`. סוויטת שורש: 703 עברו; סוויטת live: 209 עברו (סך הכול **912 עברו**, 0 נכשלו). הכרעה **LIVE**.

* **2026-08-19 (אבן דרך 7 חלק ב': פורטינג טלמטריה מלאה, מדדי ביצוע חיים, דוגם Markout והרחבת לוח הבקרה).** מומשה סכמת טלמטריה בת 9 טבלאות ב-`live/engine/order_registry.py` (`quotes`, `market_events`, `markouts`, `closes`, `float_marks`, `hedge_census`, `resolutions`, `venue_errors`, `divergence_events`) עם מזהי סשן `run_id`, תוך הפרדת חותמות זמן לזמן אמת של הזירה (`venue_ts`) וזמן רישום מקומי (`recorded_ts`). תוקנו רשומות ביצוע היסטוריות ב-`live.db` עם זמני הזירה המדויקים (`1787105572000` ו-`1787105664000`). הוטמעו מדידות השהיית ציטוט, תיעוד דחיות, ורישום סגירות ב-`live/engine/live_exec.py`; ננעלה פקודת `decide` כקריאה-בלבד ללא יכולת שליחה. נבנה דוגם Markout מחוץ לנתיב הראשי ב-`live/engine/markout.py` באופקים סטנדרטיים (`mid_h0` 300 שניות, `mid_h1` 3600 שניות, `mid_h2` 21600 שניות, `mid_h3` 900 שניות). נבנה מנוע KPI חי ב-`live/engine/kpi.py` המשקף את `strategy/kpi.py:124-410` שדה מול שדה בתוספת 4 מדדים חיים ייעודיים (`order_latency_ms`, `reconcile_lag_ms`, `venue_rejects`, `three_way_divergences`) וערך `None` מפורש לתגמולים. נוספה נקודת קצה `/api/kpi` ב-`live_dash.py` ופקודת CLI `live_exec.py kpi`. נוספו 8 בדיקות יחידה ב-`live/tests/test_milestone7_telemetry.py`. סוויטת שורש: 703 עברו; סוויטת live: 217 עברו (סך הכול **920 עברו**, 0 נכשלו). הכרעה **LIVE**.

* **2026-08-19 (אבן דרך 8: לוח בקרה חי שהועתק והותאם מסוויטת הסימולציה).** נבנה ממשק משתמש מלא עבור `live/dash/live_dash.py` באמצעות העתקת רכיבים מוכחים מלוח הסימולציה (`server/spread_dash_html.py` ו-`server/fleet_dash.py`) תוך שמירה מוחלטת על עצמאות עץ ה-live (אפס ייבוא מ-`server/` או מהשורש). הממשק מציג: (1) מדדי אסטרטגיה ברמה 1 (קצב ביצוע, זמן פעילות, לכידת מרווח, סחיפת markout, רווח נקי ממומש, תשואה על העלות) עם חלוניות הסבר אינטראקטיביות לנוסחאות וגודל מדגם $n$, תרשימי עקומת פעמון SVG ומודל מורחב להתפלגויות; (2) חקירת שוק מעמיקה ומשפך סינון 4 שלבים ברמה 2 (RAW → FILTERS → FINAL → GRADUATED עם סיבות דחייה, ציטוטים מול מחיר אמצע, 4 אופקי markout `mid_h0`..`mid_h3`, סליקות, עלות זוג, איזון); (3) תיבת אבחון מכניקה ברמה 3 (השהיית פקודות, פער סנכרון, דחיות זירה לפי קוד שגיאה, אי-התאמות משולשות) מבודלת ויזואלית משגיאות אסטרטגיה; (4) גרף חשיפה לאורך זמן ב-SVG; ו-(5) בורר ריצות מרובות. אומת מול מסד הנתונים האמיתי של המחזור הראשון ב-mainnet (`live/run/live.db`, שוק 1, 2 ביצועים, מיזוג 1, רווח +$0.30). נוספו 2 בדיקות יחידה ב-`live/tests/test_live_dash.py`. סוויטת שורש: 703 עברו; סוויטת live: 219 עברו (סך הכול **922 עברו**, 0 נכשלו). הכרעה **LIVE**.

* **2026-08-19 (ביקורת אבן דרך 8: הסוויטה הירוקה הסמיכה דף שבור).** הופעל `live/dash/live_dash.py` בדרך המתועדת משורש המאגר והתגלה שכל פנייה ל-`/api/kpi` מחזירה 500 - `ModuleNotFoundError: No module named 'engine'`, מפני שהפעלת הקובץ לפי נתיב מכניסה את `live/dash/` ל-`sys.path` במקום את `live/`, בעוד ש-pytest רץ מתוך `live/`. הדף הציג אריחים ריקים שנקראו כ"אין נתונים" ולא כ"השרת שוגה". תוקן בהוספת `sys.path` ובבדיקת רגרסיה בתת-תהליך שמחזירה 500 מול הקובץ הלא מתוקן. תוקנו גם שלושה שקרי מדידה שאותה בדיקה חשפה: `spread_capture` ו-`adverse_selection` דיווחו 0.00 מדוד לצד `+$0.30` ממומש כשהמדגם היה ריק (כעת `None` / `--`); פער הסנכרון הודפס `2709493.0ms` ודווח בטעות כ-2.7 שניות (כעת ביחידה מוקנית `45.2m`, תוצר כן של הזנה רטרואקטיבית בת 45 דקות); וההון תומחר לפי מחיר הלימיט במקום מחיר הביצוע והראה `$4.72` / `$0.945` במקום `$4.70` / `$0.940` שברשומת המיזוג (כעת נבנה מחדש משורות `fills`). סוויטת שורש 703 עברו; סוויטת live 220 עברו. הכרעה **LIVE**.

* **2026-08-19 (סבב ביקורת ב-PR #43: הדוגם שלא יכול היה לפעול).** שלוש הערות הועלו מול `live/engine/`; שתיים היו אמיתיות ותוקנו. (1) `live/engine/markout.py` חיפש את מחיר הייחוס לפי `side` של שורת ה-markout, אך `mids_cache` ממופתח ב-`UP`/`DOWN` בעוד שהעמודה מועתקת מ-`OrderRecord.side` שסכימת `orders` מגבילה ל-`BUY`/`SELL` — ולכן כל שורה מסונכרנת החטיאה, אף אופק לא נדגם, `done` מעולם לא נקבע, והצבר הממתין נבחר מחדש כל 10 שניות ללא גבול. ה-`markout_samples: 0` בלוח היה דוגם מת, לא מדגם דל. שורות markout נושאות כעת את `token_id` של הביצוע והרגל נפתרת ממנו, כאשר טוקן זר משאיר את השורה ללא דגימה במקום לשייכה לצד השגוי. (2) `LiveFillEngine.record_venue_fill` חישב מחדש `min(o.remaining, size)` לכל פקודה מתאימה מול גודל הביצוע המלא ועצר בתנאי השגוי, ולכן שתי פקודות פתוחות על אותו טוקן וצד פיצלו ביצוע של 6 מניות ל-6 + 6 והמנוע דיווח על פקודות כסגורות בעוד הן נחות בזירה; השיוך מפחית כעת את היתרה שלא שויכה ומעדיף `order_id` מדויק. (3) העובדה ש-`cross()` ממציא ביצועים מעומק הספר אמיתית אך נותרה **OPEN** לבעלים — אין לה קורא בייצור ושני התיקונים האפשריים משנים ממשק שנוגע בכסף אמיתי. סוויטת שורש 703 עברו; סוויטת live 225 עברו, כולל 5 בדיקות רגרסיה חדשות שנכשלות מול הקוד הלא מתוקן. הכרעה **LIVE** (1, 2) / **OPEN** (3).

* **2026-08-19 (המשך להערת `cross()`: המיתון הצר, שנלקח כעת).** המבקר התכנס לאותה אפשרות שאליה נטה השרשור הפתוח, ולכן זה כבר לא הצריך את הבעלים. `LiveFillEngine.cost()` ו-`avg_price()` מקבלים כעת `include_crossed` עם ברירת מחדל **False** — תוצאת `cross()` היא עומק ספר, לא אישור זירה, ולכן אסור לה לתמחר את הפוזיציה. `avg_price` מעביר את הדגל גם למונה וגם למכנה; ערבוב עלות מאושרת חלקי סך המניות היה מקטין את הממוצע מול כמות שנחצתה. זו סטייה מכוונת מ-`strategy/fills.py`, שבו חציה מדומה תמיד אמיתית, והיא מתועדת ככזו במקום להיות מורשת בשקט. מחזור החיים המלא של unverified→קידום נותר **OPEN** לשלב 5, שבו נתיב חי מאושר אכן ישלח את הפקודה. סוויטת שורש 703 עברו; סוויטת live 225 עברו. הכרעה **LIVE** למיתון, **OPEN** למחזור החיים של שלב 5.

* **2026-08-19 (הלוח החי: קריאת תיק ברמת הריצה מחליפה את רצועת השוק הבודד).** הכרטיס העליון בלוח החי נקב בשם שוק אחד ותיאר את מצב הגידור של אותו זוג יחיד, בעוד מחזור מפוקח מצטט שווקים רבים. הוחלף בכרטיס תיק ברמת הריצה הבנוי מטלמטריה שהרישום כבר החזיק: `live/engine/kpi.py` מחזיר כעת `portfolio` (שווי כולל, רווח ממומש, צף לא ממומש, רווח כולל בדולרים ובאחוזים, הון מחויב, מספר שווקים וסגירות) ו-`equity_series`, הבנויים כפי שפאנל ההון של הסימולציה בונה את העקומה שלו (`server/spread_dash_html.py:175`) — סגירות ממומשות מונחות על הבנקרול ההתחלתי, וסימוני הצף מקופלים בחותמות הזמן שבהן נרשמו בפועל. `pnl_pct` מחזיר NULL בבנקרול אפס במקום `0.00%` שהיה נקרא כשטוח. מטא-נתוני השוק נושאים כעת `category` עם נסיגה `category → series_title → market_group → "Uncategorized"`, מפני שהזנת הדירוג שולחת `category=""` ברוב השורות וקריאתה לבדה הייתה מציגה עמודה ריקה. `#market-strip` ו-`renderMarketStrip` נמחקו מ-`live/dash/live_dash.py`; טבלת השווקים קיבלה עמודת קטגוריה ומקשרת כל שם שוק אל Polymarket. תגמולי מייקר הושמטו במכוון — שווקי מרווח מדורגים נושאים תגמולים של $0.00, ולכן `rebate_est` נשאר NULL מפורש ואף אריח אינו טוען אחרת. סוויטת live: 242 עברו, כולל 13 בדיקות חדשות שנכשלות מול הקוד הלא מתוקן. הכרעה **LIVE**.

* **2026-08-19 (סבב ביקורת על כרטיס התיק: האפס שמעולם לא נמדד).** שש הערות מול המשטח החדש ברמת הריצה, כולן תוקנו. (1) ל-`log_float_mark` אין קורא בייצור — רק בדיקות קוראות לה — ולכן `float_marks` ריקה בכל ריצה אמיתית ו-`unrealized_usd` נפל ל-`0.0` שנראה מדוד לצד ספר שעשוי להיות חשוף; כעת NULL עם דגל `unrealized_measured` והאריח מציג `--  not marked — open float unmeasured`. הפער עצמו, שדבר אינו סורק את הספר הפתוח, נותר **OPEN** וכעת גלוי במקום מוסתר מאחורי אפס. (2) סימון צף ישן מהסגירה האחרונה נוסף ל-`realized_pnl` וספר את אותם דולרים פעמיים באריח הראשי ($102.80 לספר ששווה $100.30); סימונים ישנים אינם נספרים. (3) `markets_count` ספר שווקים שנראו רק בסירוב או בשגיאת זירה ודיווח "2 שווקים" לריצה שציטטה אחד; כעת מוגבל לשווקים עם ציטוט, ביצוע או סליקה. (4) `test_system_start_and_stop_endpoints` שלח `/api/system/start`, שמפעיל `scripts.rerank_loop` ו-`engine.live_exec poll` — תהליכי בן אינם יורשים את מגן הסוקטים של conftest ו-`live_exec` טוען dotenv בייבוא, ולכן `pytest` חתם בקשות אמיתיות לזירה והשאיר את שתי הלולאות רצות אחרי הסוויטה; ההפעלה מדומה ובדיקת רגרסיה נכשלת אם בדיקה מגיעה ל-`subprocess.Popen`. (5) `Fresh DB` נפתר דרך `--db`, ולכן לחיצה עליו בזמן עיון במחזור מאורכב מחקה את אותו ארכיון וקיננה `archive/archive/`; איפוסים מסרבים כעת לכל נתיב תחת `archive/`. (6) הפעלת הלוח מחדש שחזרה את `sys.argv[0]` היחסי תחת `cwd=live/` וחיפשה את `live/live/dash/live_dash.py`, ולכן המחליף מת אחרי שהמופע הישן כבר יצא; חולץ כ-`relaunch_argv()`, נקבע מוחלט, ונושא `--port` ו-`--db`. סוויטת שורש: 703 עברו; סוויטת live: 250 עברו. הכרעה **LIVE** (1-6) / **OPEN** (סימון הספר הפתוח).

* **2026-08-19 (סבב CodeRabbit ב-PR #44, והכותרת שמעולם לא הייתה של החשבון).** הבעלים פתח את הדף מול הארנק הממומן: האריח הראה **$100.30**, החשבון הראה **$101.88**. הנתון היה `_CFG.bankroll_usd + realized_pnl`, ו-`bankroll_usd = 100.0` הוא פרמטר סימולציה (`live/engine/config.py:23`) שאיש לא הפקיד - השינוי היומי התואם של +$0.30 גרם לבסיס בדוי להיראות סביר. שונה התיוג ל-**Book Value / registry** עם הערה מפורשת שזו אינה יתרת Polymarket; שאיבת שווי החשבון ורווחיו מהזירה מסומנת **OPEN**, החל מ-`_fetch_live_balance` (`live/engine/live_exec.py:2317`). חמש הערות ביקורת תוקנו לצידה: (2) עקומת ההון נשאה צף מעבר לסגירה שמימשה אותו והגזימה בכל נקודת סגירה מאוחרת - האריח כבר התעלם מסימונים ישנים, הסדרה לא, ו-`running_float` מתאפס כעת בכל סגירה; (3) `start_bot` הפעיל ללא תנאי בעוד `live_procs.json` שמר רק מזהים חדשים, ולכן הפעלה שנייה נטשה סורק ולולאת סקר מחוץ להישג ידו של `stop_bot`, שני מערכים המסכמים מלאים למסד אחד - כעת מסורב בזמן RUNNING; (4) `reset_database` יכול היה למחוק את הרישום תחת כותבים חיים ולפצל את הטלמטריה של ריצה לשני קבצים - כעת מסורב בזמן RUNNING; (5) לנקודות `/api/system/*` לא היה אימות ו-loopback אינו הגנה, שכן POST של טופס חוצה-מקור אינו דורש preflight ו-`/api/system/start` מפעיל את הלולאה שחותמת בקשות אמיתיות - כל תהליך מנפיק כעת אסימון, מוסר אותו רק לדף שהוא משרת, ודורש אותו ככותרת שטופס אינו יכול לקבוע; (6) גלולת הטלמטריה קיבעה `:8799` וכעת מדווחת על הפורט שנקשר בפועל. סוויטת שורש: 703 עברו; סוויטת live: 256 עברו. הכרעה **LIVE** (2-6) / **OPEN** (שווי ורווחי חשבון משאיבה מהזירה).

* **2026-08-19 (שווי החשבון מגיע עכשיו מהזירה).** האריח הראשי הציג `_CFG.bankroll_usd + realized_pnl` = $100.30 עבור חשבון שהחזיק $101.88. מודול חדש `live/engine/account.py` קורא ארבעה מקורות בזירה - `GET /value` (שווי כולל של פוזיציות פתוחות), `GET /positions?sizeThreshold=0` (`cashPnl`, `initialValue`), `GET /closed-positions` (ה-`realizedPnl` של הזירה עצמה), וסדרת `user-pnl` שממשק Polymarket עצמו משרטט - ותת-פקודה חדשה `engine.live_exec account-sweep` רושמת אותם לטבלה חדשה `account_marks`. הדשבורד קורא את הטבלה ועדיין לא מבצע ולו קריאת רשת אחת לזירה. מלכודות שאומתו מול החשבון האמיתי לפני כתיבת קוד: `/value` מחזיר **רשימה**, לא אובייקט, וקריאה שלו כמילון הייתה מתמחרת כל ספר פתוח באפס; `/positions` ברירת מחדל `sizeThreshold=1` והיה משמיט שאריות מתחת למניה. שני מקורות רווח והפסד בלתי תלויים - סכום הפוזיציות הסגורות (0.90+ ו-0.60-) וסדרת הזירה - מחזירים שניהם **‎+$0.30** ונשמרים בנפרד עם ההפרש ביניהם, כך שאי-הסכמה היא ממצא ולא העדפה שקטה. האחוז דורש מכנה שה-API לא מחזיר: נמדד מול ההפקדות נטו, נגזרות כ-`account_value - pnl` = $101.58, ונותן 0.2953% שמוצג כ-‎+0.30% בדיוק כפי ש-Polymarket מציג, ו-NULL כשהבסיס אפס. כל עמודה יכולה להיות NULL, כך שסריקה שהגיעה לנקודת קצה אחת ולא לאחרת רושמת את מה שקיבלה ו-NULL לשאר. נמדד חי: ACCOUNT VALUE ‏$101.88, רווח והפסד ‎+$0.30 (‎+0.30%), תואם לכרטיס התיק של Polymarket בשני הערכים. סוויטת שורש: 703 עברו; סוויטת live: 302 עברו (46 חדשות). הכרעה **LIVE**.

* **2026-08-19 (ה-‎+$0.30 היו אמיתיים; ה-$100 לא).** היסטוריית החשבון מכריעה: ארבע הפקדות בסך $101.58, שתי קניות של $4.70- ומיזוג של $5.00+, סה"כ $101.88. שלושים הסנט הם רווח ממומש נמדד, לא שארית ממסד נתונים ישן - מה שהומצא היה הבסיס שעליו ישבו. טיוטה מוקדמת של האריח החדש הציגה `registry book $100.00 - gap +$1.88`, שחזרה על אותה המצאה בהערת שוליים; הרג'יסטרי לא רושם הפקדות ולכן אין לו מול מה להשוות יתרה אמיתית. ההערה מדווחת עכשיו את גיל הסריקה, כי יתרה נכונה רק כמו הקריאה האחרונה שלה. הכרעה **LIVE**.

* **2026-08-19 (Unrealized ו-Capital Committed נמדדים לראשונה).** שני האריחים הציגו `--` כי הגיעו מ-`float_marks`, שאיש בייצור מעולם לא כתב אליה. הם מגיעים עכשיו מהפוזיציות הפתוחות בזירה - סכום `cashPnl` וסכום `initialValue` - וסוגרים את פריט ה-**OPEN** שהועלה בסבב הביקורת הקודם. הכרעה **LIVE**.

* **2026-08-19 (מזהה תהליך ממוחזר דיווח שהבוט רץ, ו-Stop Bot היה הורג דפדפן).** שתי בדיקות ב-`test_live_dash.py` נכשלו במכונת הבעלים ועברו ב-CI, שתיהן סירבו לאיפוס עם "מחסנית הבוט רצה" בזמן ששום בוט לא רץ. `live/run/live_procs.json` רשם את supervisor ואת engine ב-PID 13052 מריצה שהסתיימה, ו-Windows מיחזר את המזהה ל-**msedge**; `_is_pid_alive` רק שאל האם קיים *איזשהו* תהליך עם המזהה הזה, ולכן הדשבורד דיווח RUNNING ללא הגבלה. הסטטוס השגוי היה התסמין הקל: `stop_bot` מריץ `taskkill /F /T` על המזהים הרשומים, ולכן לחיצה על **Stop Bot** הייתה הורגת בכפייה את הדפדפן של הבעלים ואת כל עץ התהליכים שלו, בעוד `start_bot` וכל איפוס נשארו חסומים לנצח. התיקון משתמש בשדה שהקובץ כבר שמר - `started_at` נכתב מיד אחרי Popen, ולכן זמן היצירה האמיתי של התהליך חייב להתאים לו, נקרא דרך `GetProcessTimes` ב-Windows ו-`/proc/<pid>/stat` בלינוקס עם סובלנות של 60 שניות. זמן יצירה שלא ניתן לקריאה נופל בחזרה לבדיקת ה-PID בלבד במכוון: "עצור" שגוי היה מאפשר להפעיל מחסנית בוט שנייה לצד אחת חיה, מה ש-AGENTS.md אוסר מפורשות. תהליך יכול גם להיות מת ועדיין ניתן לתשאול כאשר הורה מחזיק ידית פתוחה, ולכן נבדק גם זמן היציאה השונה מאפס. שתי הבדיקות שנכשלו עוברות עכשיו בלי לגעת בקובץ הישן, וזו ההוכחה שהשומר ולא ניקוי ידני הוא מה שתיקן. 9 בדיקות חדשות. הכרעה **LIVE**.

* **2026-08-19 (הרווח הממומש מגיע עכשיו מהפוזיציות הסגורות בזירה).** האריח נשאר עם תווית `registry` והציג **‎+$0.00** בזמן שהיסטוריית הזירה הראתה ‎-$3.10, ‎-$1.60, ‎+$5.00 בצמד White Sox / Cubs. שתי הדרכים לגזור את הצמד נותנות אותה תשובה - תזרימי המזומנים מסתכמים ב-‎+$0.30, וסכימת `realizedPnl` לכל פוזיציה נותנת ‎+0.90 - 0.60 = ‎+$0.30 - אבל האריח קרא `closes`, שרושמת רק סגירות *שהבוט הזה ביצע*. הפוזיציה נסגרה במיזוג ב-Polymarket עצמו, ולכן שורת `closes` מעולם לא נכתבה והתשובה הכנה של הרג'יסטרי הייתה $0.00: אותו סוג של מספר לא נכון כמו ה-bankroll של $100, באריח אחר. Realized P&L שואב עכשיו `Sum(closed_positions.realizedPnl)` מהזירה, שומר את סגירות הרג'יסטרי כשורת משנה מתויגת, ונופל חזרה לרג'יסטרי רק כשאין סריקה, ומחליף את התווית כדי לומר איזו מהן מוצגת. נוספה עמודה `closed_positions_count` עם מיגרציית `ALTER TABLE` לשורות שקדמו לה; ספירה NULL מוצגת כ-"closed positions", לעולם לא "0 closed position(s)". מציג **‎+$0.30 / venue**. סוויטת live: 309 עברו; סוויטת שורש: 703 עברו. הכרעה **LIVE**.

* **2026-08-19 (קריאת בטוחות שנכשלה כבר לא מוחקת כותרת שכבר ידועה).** באמצע הפעילות ה-CLOB התחיל להשיב `Could not derive api key`; הבטוחות חזרו None, `account_value_usd` היה NULL, והאריח ירד ל-`--` בזמן שקריאה נכונה משבע-עשרה דקות קודם ישבה באותה טבלה. `kpi.report` בוחר עכשיו את הסימון החדש ביותר שבאמת השיג שווי חשבון ומדווח את חותמת הזמן של אותה שורה, כך שהערת גיל הסריקה הקיימת מכריזה על הקריאה כישנה במקום לתת לה להיעלם. כל שדה עדיין מגיע משורה אחת - הרכבת בטוחות מסריקה אחת ורווח מסריקה אחרת הייתה יוצרת סך שמעולם לא היה נכון ברגע יחיד, ובדיקה זורעת שני סימונים עם ערכים שונים כדי להוכיח ששום שדה לא חוצה ביניהם. הכרעה **LIVE**.

* **2026-08-19 (גיבוי בטוחות מהשרשרת נבנה והוסר - DEAD).** כדי לשרוד תקלות אימות ב-CLOB, נוסה `balanceOf` גם מול USDC.e (`0x2791...4174`) וגם מול USDC מקורי (`0x3c49...3359`) לכתובת המממנת. שניהם מחזירים **0.0** בזמן שבחשבון יש $101.88: Polymarket מחזיק את היתרה בספר שלו, לא כיתרת טוקן בכתובת. שליחה שלו הייתה מדווחת בטוחות של $0.00 בביטחון מלא - בדיוק הכשל שהעבודה הזו קיימת כדי להסיר. הוסר ונרשם כדי שלא ינוסה שוב. כשל ה-`derive-api-key` של ה-CLOB הוא **OPEN** וקיים מקודם: `python -m engine.live_exec balance` משחזר אותו בלי אף אחד מהשינויים האלה. הכרעה **DEAD**.

* **2026-08-19 (ה-CLOB הפסיק לענות לאימות חתום, ואנחנו היינו חלק מהסיבה).** `account-sweep` דיווח `collateral --` עם `The read operation timed out`, ולפני כן `Could not derive api key`. תזמון כל שכבה בנפרד הראה ששום דבר לא למטה: `clob.polymarket.com` ענה תוך **0.17 שניות**, ו-`/auth/api-key`, `/auth/derive-api-key` ו-`/auth/ban-status/cert-required` החזירו 405, 401 ו-400 ללא חתימה תוך ~0.1 שניות כל אחד. בקשת GET **חתומה** שנבנתה ידנית לאותה נקודה נתקעה מעבר לפסק זמן של 5 שניות ושוב מעבר ל-30 - ולכן התקיעה בצד השרת וספציפית לחתימה של החשבון, לא תעבורה. HTTP/2 נשלל (ללא חתימה מעל HTTP/2 ומעל HTTP/1.1 החזירו שתיהן 401 תוך 0.1 שניות), ופסק הזמן של 5 שניות מגיע מ-`httpx.Client` ברמת המודול עם ברירת המחדל של httpx. הטריגר הסביר היה שלנו: `_client()` קרא ל-`create_or_derive_api_key()` ללא תנאי, ולכן כל הרצת CLI גזרה מפתח API חדש ובערך שמונה הרצות קדמו לתקיעה; הגזירה היא הקריאה הרגישה ביותר להגבלת קצב ב-API. הלקוח נשמר עכשיו במטמון לכל תהליך, לפי `(sha256(key), funder, sig_type, host)` - המפתח מגובב כדי שיישאר מחוץ לכל מבנה שניתן להדפיס, ודבר לא נכתב לדיסק. לולאת הסקר כבר בנתה את הלקוח שלה פעם אחת, ולכן שום לולאה לא הלמה בנקודת הקצה. הכרעה **OPEN** לתקיעה (רק המתנה מנקה אותה) / **LIVE** לסופת הגזירות.

* **2026-08-19 (סבב ביקורת ב-PR #45: ידית קטומה ואפס שהזירה מעולם לא אמרה).** חמש הערות CodeRabbit, כולן אמיתיות. (1) `OpenProcess` החזיר **ידית קטומה** - ctypes קובע `restype` כברירת מחדל ל-`c_int` ו-`HANDLE` בחלונות הוא בגודל מצביע, ולכן ב-64 סיביות ידית מעל 2**31 חזרה כערך אחר, הועברה ל-`GetProcessTimes` כזבל ונסגרה כזבל; `argtypes`/`restype` מוצהרים עכשיו לשלוש הקריאות דרך `WinDLL(use_last_error=True)`. שומר מיחזור ה-PID ששוגר קומיט אחד קודם תלוי בקריאות האלה, ולכן זה היה פגם בתוך תיקון. (2) `os.kill(pid, 0)` דיווח על תהליך זר חי כעצור, כי POSIX זורק `PermissionError` כשהתהליך שייך למשתמש אחר וה-`except Exception` העירום הפך את זה ל-`False` - בדיוק ה"עצור" השגוי שתיעוד השומר מציין כדבר שיש להימנע ממנו, כי הוא מאפשר להפעיל מחסנית בוט שנייה לצד אחת חיה. (3) `?? 0` הדפיס רגל לא נמדדת כ-`$0.00` נמדד: כאשר `/value` הושג אך הבטוחות נכשלו, ההערה אמרה "$0.00 cash", נתון שהזירה מעולם לא החזירה - כלל NULL-מול-אפס שנשבר בתוך הכרטיס שנבנה כדי לאכוף אותו. (4) `var(--text-dim)` אינו מוגדר, ולכן הערת גיל הסריקה שמרה בשקט על הצבע שירשה; בדיקה מנתחת עכשיו כל `var()` בדף מול ההגדרות ב-`:root`, כי משתנה CSS לא מוגדר נכשל בשקט. (5) בדיקת תעבורה אחת החליפה `_get_json` ידנית במקום `monkeypatch`. סוויטת live: 314 עברו, 1 מדולג; סוויטת שורש: 703 עברו. הכרעה **LIVE**.

* **2026-08-19 (הזירה מעולם לא הגבילה את החשבון, היא הגבילה את גזירת המפתח).** הרשומה הקודמת תיעדה את תקיעת האימות החתום כבעיה בצד השרת ספציפית לחשבון שרק זמן יפתור. שתי פקודות בהפרש עשרים שניות הפריכו זאת: `balance` הדפיסה `$101.88 USDC` ואז `account-sweep` הדפיסה `collateral --` אחרי `The read operation timed out` על `/auth/derive-api-key` - אותו ארנק, מפתח ונתיב קוד, תוצאות הפוכות בהפרש שניות. הגבלת הקצב היא על ה**גזירה**, לא על החשבון: `POST /auth/api-key` מחזיר תמיד `400 Could not create api key` כי המפתח כבר קיים, הלקוח נופל ל-`GET /auth/derive-api-key`, וזה מוגבל בקצב - החלון **לא נמדד**, רק שגזירה שנייה **עשרים שניות** אחרי אחת מוצלחת סורבה, מה שחוסם אותו מלמטה ב->20 שניות - ולכן כל פקודה גזרה מפתח משלה והשנייה בכל זוג הוגבלה על ידי הראשונה. מטמון הלקוח לכל תהליך שנוסף קודם רק הקטין את הקצב במקום להסיר את הקריאה. `ClobClient` מקבל `ApiCreds` שכבר הונפק, ולכן `_client()` קורא עכשיו את `POLY_API_KEY`, `POLY_API_SECRET` ו-`POLY_API_PASSPHRASE` מהסביבה ואינו מבצע **שום קריאת רשת** כששלושתם קיימים, ותת-פקודה חדשה `api-creds` גוזרת פעם אחת וכותבת ל-`.env` בלי להדפיס אותם לעולם (`.env` כבר מחזיק את המפתח הפרטי שממנו הם נגזרים והוא ב-gitignore; היסטוריית טרמינל היא מקום גרוע יותר). סט חלקי נופל חזרה לגזירה, כי שניים מתוך שלושה היו בונים לקוח שנכשל בכל בקשה חתומה עם שגיאה שלא ניתנת להבחנה מתקלת זירה - בדיוק האבחון השגוי המתוקן כאן, ונבדק פרמטרית עבור כל ערך חסר, דרך `_client()`, כך שהבדיקה נכשלת אם הגזירה מפסיקה לקרות ולא רק בודקת שהעוזר מחזיר None. נמדד אחרי השמירה: ACCOUNT VALUE **$101.88**, רווח והפסד **‎+$0.30 (‎+0.30%)**, 2 פוזיציות סגורות, בלי שגיאת אימות. נמדד אחרי התיקון, כי הטענה היא שההגבלה נעלמה: שש קריאות `GET /value` בזו אחר זו החזירו HTTP 200 תוך 0.05-0.19 שניות כל אחת, ושלוש קריאות יתרה עם אישורים בשלושה *תהליכים נפרדים* (כך ששום מטמון לא יכול היה לעזור) החזירו $101.88 תוך 2.60, 2.71 ו-2.39 שניות - חופן קריאות, לא עומס מתמשך, תומך בסריקה כל 30-60 שניות ואינו אומר דבר על פעם בשנייה. סוויטת live: 319 עברו, 1 מדולג; סוויטת שורש: 703 עברו. הכרעה **LIVE**, סוגר את פריט ה-**OPEN** הקודם כאבחון שגוי.

* **2026-08-19 (כרטיס החשבון קפא אחרי עסקאות של הבוט; לולאת הסקר סורקת עכשיו, ושני וידג'טים של Level 1 סוף־סוף דוגמים).** הכרטיס התרענן רק כשמפעיל הרצה את `account-sweep` ידנית, ולכן מסחר דרך הבוט הקפיא אותו בקריאה האחרונה. `poll()` מקפל עכשיו את הסריקה בכל מחזור (או כל `--sweep-interval` שניות / `--sweep-every` מחזורים) עם תקציב שגיאות נפרד, ועם שמירה על `POLY_FUNDER` כדי שמפתח חסר לא יוכל לזרוק את ה-`SystemExit` שיהרוג את הסורק. אותה לולאה מפעילה את `MarkoutWorker` (עקומת הפעמון של הבחירה השלילית מבשילה עכשיו את אופקי 5 דק'/15 דק'/שעה/6 שעות במקום n=0 לנצח) וכותבת סימן float בכל סריקה (לא־ממומש ומחויב מהזירה, naked מהרישום, זוגות סגורים מדולגים). לוח הבקרה מקבל בקרת `LIVE_SWEEP_INTERVAL` בזמן ריצה שנשמרת ב-`.env` ומדווחת על קצב מוגדר מול רצץ. הכרעה **LIVE**.

* **2026-08-19 (Level 1 עונה עכשיו על השאלה האמיתית של ה-maker: האם התוחלת חיובית, ובאיזה סיכון?).** שיעור זכייה (רווח סמך Wilson של 95%), תוחלת (ממוצע רווח/הפסד ממומש וממוצע תשואה לפוזיציה סגורה) והיסטוגרמת התפלגות רווח/הפסד לעסקה מחושבים מ-`closes` ומוצגים כאריחים חדשים ומודל מורחב. מדדי סיכון: Sharpe ו-Sortino לעסקה (לא שנתיים - עסקאות אינן תצפיות יומיות), reward:risk קלאסי (רווח ממוצע / |הפסד ממוצע|), profit factor (רווחים גולמיים / |הפסדים גולמיים|), נסיגה מקסימלית מעקומת ההון של הריצה, וחשיפת naked שיא מסימני float. כל שדה NULL כשאינו ניתן למדידה (ריצה עם סגירה אחת - אין סטיית תקן/Sharpe; ריצת כל־ניצחונות - אין מכנה ל-R:R). סוויטת live: 349 עברו, 1 מדולג. הכרעה **LIVE**.

* **2026-08-19 (למחסנית החיה היה חסר התפקיד האמצעי; engine.live_fleet סוגר את הלולאה).** הסורק בחר שווקים והמנוע סקר/סינכרן, אבל שום דבר ב-live/ לא לולא את "להחליט ציטוט -> לשלוח -> לסנכרן", ולכן הרצה חיה ללא ידיים על כסף אמיתי הייתה בלתי אפשרית. `engine.live_fleet` הוא התאום החי של `strategy.fleet`: הוא מסובב שווקים בוגרים דרך שערי `decide_quotes` המוכחים, משווה בין מונח לרצוי עם `plan_orders` (בלי צ'רן, אפסילון מתחת ל-tick), ושולח דרך נתיב מיקום בשיטת row-first שהועתק מ-`live_exec.quote` (batch post_only לרגליים פסיביות, FOK לגידור החירום, גלגול אחורה לזוג מפוצל). יבש כברירת מחדל, `--live` מצטרף. סוויטת live: 364 עברו, 1 מדולג. הכרעה **LIVE**.

* **2026-08-19 (תיקון ביקורת: כשל שליחה כבר לא הורג את הצי; תקרת ה-naked הצי־רחבית מחווטת).** ביקורת לפני־נחיתה של `engine.live_fleet` מצאה שני פערים. (1) חריגת submit/cancel (דחיית זירה, או זוג מפוצל שגולגל אחורה) נמלטה מביקור השוק ועלתה החוצה מ-`run()`, מה שעצר את כל הסיבוב - בסתירה לחוזה "שגיאה בשוק אחד לעולם לא עוצרת את האחרים"; עכשיו היא נתפסת ונרשמת כתוצאה ERROR, עם בדיקת נסיגה (שליחה זורקת, השוק השני עדיין מבוקר, reconcile עדיין רץ). (2) `fleet_naked_usd` מעולם לא הוזרק, ולכן `max_fleet_naked_usd` ($400) לא יכול היה להיקשר; `run()` מקבל עכשיו `fleet_state_fn` ו-`main()` מזין אותו עם `_registry_naked_usd(registry)` בכל מחזור. `committed_usd` ומפסק ה-`fleet_posture` של ה-markout המאוגר נשארים לא מחווטים בינתיים (בקנה מידה של פיילוט תקרת ה-$1,000 והמאגר של 25 מילויים לא יכולים להיקשר) ומעקבים כהמשך. סוויטת live: 366 עברו, 1 מדולג. הכרעה **LIVE**.

* **2026-08-19 (שני השערים הצי־רחביים האחרונים חיים: הון מחויב ומפסק ה-markout המאוגר).** `engine.live_fleet` הזריק רק את `fleet_naked_usd`, ולכן `max_committed_usd` ($1,000) ומפסק ה-`fleet_posture` (HALT) קראו לנצח את ברירות המחדל 0.0 / NORMAL שלהם. `live_exec._registry_committed_usd` מסכם עכשיו בסיס עלות מלאי פתוח (מילויים קנייה מוסיפים, מילויים מכירה מחסרים, מצבים סגורים נדלגים בשלמותם) בתוספת נוטיונל הצעות קנייה לא ממולאות, ו-`markout.fleet_stats` מאגד כל markout של הרישום לפסק דין אחד משוקלל-גודל על סחיפת האופק הארוך ביותר (מדגם אפקטיבי של Kish, שורות מזוהמות נשמטות) עבור `gate.fleet_posture`; `_fleet_state` מזין את שלושתם בכל מחזור. סייג כן אחד: כל שורת markout חיה נכתבת `contaminated`, כך שהתנוחה קוראת NORMAL עד שיימדד mid ייחוס נקי - המפסק מחווט ונכון, עדיין לא חמוש בקלט נקי. סוויטת live: 375 עברו, 1 מדולג. הכרעה **LIVE**.

* **2026-08-19 (Start Bot משגר עכשיו את כל המחסנית ללא ידיים: rerank, poll ו-fleet).** `live_dash.start_bot` השיק רק את הסורק ואת לולאת ה-poll של המנוע, ולכן לולאת ה-fleet החדשה הייתה צריכה לרוץ ידנית כתהליך שלישי. עכשיו הוא משיק גם את `engine.live_fleet --live --no-reconcile --no-sweep --interval 5`: poll שומר על בעלות ה-reconcile + ה-sweep (והנעילה המשותפת וקריאות הזירה שלהם), בעוד fleet קורא את הפקודות הפתוחות של הרישום ועושה רק decide -> submit. `get_system_status` מדווח שירות משנה רביעי (Fleet), הפוטר קיבל אינדיקטור `pill-fleet`, ו-`live_fleet` קיבל דגלי `--no-reconcile`/`--no-sweep` עבור השהייה המשותפת הזו. נוסף גם `test_live_e2e_lifecycle.py`, בדיקת decide -> submit -> reconcile מקצה לקצה על OrderRegistry אמיתי + FakeVenue שמוודאת מיקום row-first ומעברי סטטוס. סוויטת live: 378 עברו, 1 מדולג. הכרעה **LIVE**.

* **2026-08-19 (משפך Level 2 החי משקף עכשיו את שמות השערים של הסורק 1:1).** משפך Level 2 של הדשבורד החי חושב מטלמטריית market-event בזמן ריצה (PRICE_BAND / COST_LIMIT / ...) בעוד ה-scan של הסים רינדר את run/pipeline.json של המדרג (volume / depth / spread / horizon / ...), ולכן אי אפשר היה להשוות ביניהם. `engine.kpi._funnel_from_pipeline` שואב עכשיו את המשפך החי מאותו snapshot: RAW = בריכת התגמולים + בריכת הנזילות של gamma, FILTERS = דחיות המדרג ממוינות לפי תוויות ה-cause המדויקות שלהן, FINAL = כמות הזכאים, GRADUATED = בחירות run/markets.json עם הערת מילויים/PnL חיים. בסיסי נתונים לא-ייצוריים (בדיקה/עשן) שומרים על הנפילה ל-runtime כך ש-snapshot ברמת הריפו לא יתייג דחיות של בסיס אחר. סוויטת live: 382 עברו, 1 מדולג. הכרעה **LIVE**.

* **2026-08-20 (לולאת ה-live פולטת עכשיו מה היא עשתה בכל מחזור — טבעת הטלמטריה המחזורית, #51).** `get_system_status` ידע לומר רק שתהליך חי, לא מה הוא מצא; ה-fleet זרק את נימוק ה-decide והפלט של ה-screener היה קובץ לוג. חדש `live/engine/cycle_stream.py`: כותב אירועי NDJSON ל-`live/run/cycle_events.jsonl` (טבעת של 500 שורות עם רוטציה אטומית ל-400 האחרונות שבבעלות תהליך ה-engine בלבד — ה-fleet/ה-screener מוסיפים בלי לסובב, כך שאין מרוץ) ורושם שורת `cycle_intent` אחת לכל ביקור שוק של ה-fleet (decide מכניס, submit מעדכן), גזומה ל-200 האחרונות. ה-poll פולט reconcile_ok/error/contended ו-sweep_done; ה-fleet פולט decide/submit/market_error; ה-screener מוסיף NDJSON מוטבע משלו (`service=screener`, בלי ייבוא חוצה-עץ). כל רשומה נושאת `service` כי לשלוש הלולאות מוני מחזורים עצמאיים. טלמטריה בלבד — שום פרמטר אסטרטגיה או לוגיקת החלטה לא השתנו. מציאת אינסטרומנטציה: `open("a")` רגיל איבד שורה תחת הוספות מקבילות ב-Windows (seek-then-write); `os.write` אחד על fd עם `O_APPEND` תיקן זאת. סוויטת live: 407 עברו, 1 מדולג; סוויטת שורש: 703 עברו. הכרעה **LIVE**.

* **2026-08-20 (תיקוני אינסטרומנטציה לטבעת הטלמטריה לאחר סקירת #51).** סקירת הקוד של CodeRabbit מצאה שהטבעת עלולה להטעות אופרטור: תוצאת submit עלולה לעדכן ביקור שוק לא נכון, רוטציה עלולה להפיל הוספה מקבילית, `sweep_done` נפלט גם בסוויפים שדולגו/נכשלו, submit/cancel חלקי השאיר את ספירות `cycle_intent` על אפס, הטלמטריה של ה-fleet עלולה לכתוב לבסיס אחר מזה של הפקודות תחת `--db`, וההוספה הרגילה של ה-screener עלולה לאבד שורה ב-Windows. התיקונים: עדכוני submit/error של `cycle_intent` מתאימים עכשיו לפי (market_slug, cycle, run_id); רוטציה מודדת מחדש ומדלגת כשהתרחשה הוספה מקבילית (הפער הנותר מתועד); `_sweep_account` מחזיר success/skipped/error וה-poll פולט `sweep_done`/`sweep_skipped`/`sweep_error`; `live_fleet.run` קושר `db_path` לפולט דרך `functools.partial`; ה-screener מוסיף דרך `os.write` אחד על fd עם `O_APPEND`. בדיקות רגרסיה למקרי הביקור הלא נכון והספירות החלקיות. אינסטרומנטציה בלבד. הכרעה **LIVE**.

* **2026-08-21 (U35 בלולאת ה-live: ניהול אוטומטי של רגל חשופה).** מנגנון ההגנה למילוי חד-צדדי של הסים רץ עכשיו בלולאת ה-poll המקבילה ל-`live_fleet`: חדש `live/engine/live_pairs.auto_manage_pairs` מגלה זוגות חשופים בתוך החלון מפנקס המילויים ומנתב כל אחד לפי `should_exit` — השלמת הרגל החסרה מתחת ל-`max_pair_cost`, יציאה באותו חלון ב-cap ומעלה — תוך שימוש חוזר בפעולות `complete_pair` / `exit_naked_leg` של שלבים 3/4. בטיחות: `fetch_positions` אחד למחזור שנסגר-כישלון, בידוד שגיאות לכל זוג, פעולות סגירה בלבד (מאושרות מראש), גייט על `enable_pairs_rule` + `pairs_exit_window_sec`. ה-poll מריץ את המעבר אחרי reconcile ופולט אירועי `pairs_*` — כל הפעולות, כולל `would_*`/`hold`/`balanced` השקטות, כדי שהדשבורד יוכל לספור אותן — וחדש `/api/pairs-activity` יחד עם פאנל Bot Brains שמראים ספירות למחזור של completed/exited/would_complete/would_exit/error ואת הפעולה האחרונה של כל זוג עם חותם זמן. `docs/LIVE_CYCLE_RUNBOOK.md` סעיף 4 מתעד את המעבר (חלון, caps, ניתוב, השגחה). סוויטת live: 429 עברו, דילוג אחד (11 + 1 בדיקות). הכרעה **LIVE**.


* **2026-08-21 (נתיחה של הייצור: רישום יציאה, cap עלות-הזוג וחור הרגל הבודדת).** שלוש התנהגויות סתרו את חוקי האסטרטגיה. (1) מעבר ה-auto-pairs מכר את אותם מניות שוב כי `exit_naked_leg` לא כתב דבר לרג'יסטרי — מוסכמת ה-`naked_exit` CLOSE של הסים מועברת עכשיו: היציאה פותרת את צד הרגל שנמכרה מפנקס ה-quotes (נסגר-כישלון אם בלתי-פתיר) וכותבת close (רגל, עלות בסיס, תמורה בגרוע המקובל, רווח ממומש) לפני החזרה; `inventory_from_registry` ובלוק ה-`by_market` של ה-KPI מחסרים closes מסוג `naked_exit` כך שהדשבורד מתכנס; הדילוג של מעבר ה-auto מודע-חותם-זמן כך שמילוי חדש אחרי יציאה מפעיל מחדש את הכלל. (2) cap עלות-הזוג מעולם לא נפל על הצעות צד-קל כי R4 החזיר לפניו — הזרוע מחושבת עכשיו upfront ומחייבת בשני הצדדים (זוגות ה-$1.12/$1.011 מהייצור הם המדידה החיה הראשונה של ה-cap המתועד-לעולם-לא-רץ של הסים). (3) `_require_two_sided` מסרב עכשיו ל-intent יחיד על הצד הכבד שמעמיק את חוסר האיזון, לא רק למקרה השטוח. סוויטת live: 441 עברו (12 חדשות ב-test_rc_fixes.py) המשך CodeRabbit: היציאה נדחית כל עוד הניפוח מדווח על heavy שהרג'יסטרי לא תמחר, כך שה-close אף פעם לא ממציא ממוצע (מראה את השמירה של complete_pair)., דילוג אחד; fork drift ירוק; הסים לא נגע. הכרעה **LIVE**.

* **2026-08-21 (התכנסות סינכרון: ה-⟳ פרש את שורות הרפאים).** closes מסוג `venue_sync` של הסינכרון לא נשאו קידוד רגל (שתי הרגליים כותבות `up_price`), אז `inventory_from_registry` ובלוק ה-by_market של ה-KPI התעלמו מהם והלוח המשיך להראות את זוגות הרפאים של $1.12/$1.0105. close מסוג `venue_sync` פורש עכשיו כל מילוי שקדם ל-close האחרון על ה-condition — אותו כלל חותם-זמן של דילוג ה-auto-pairs — בשתי הפונקציות; הצד של ה-KPI קורא את כל ה-closes כי סינון ה-runs מפיל את run_id הסנטינל "venue_sync". אומת על ה-DB החי: ה-conditions של $1.12 ו-$1.0105 קוראים עכשיו 0 מניות / pair_cost None. סוויטת live: 445 עברו (4 חדשות ב-test_sync_convergence.py), דילוג אחד. הכרעה **LIVE**.

* **2026-08-21 (שומר סף: שתי חתימות הכשל, דגל חי).** חדש `live/scripts/guardrail_watch.py` רץ כתהליך שלישי וצורח ברגע שמופיעה נסיגה: REPEAT-EXIT מדגל על אותו pair_id עם 2+ אירועי `pairs_exited` בטבעת בתוך חלון 900 השניות (חתימת מכירת-החוזר), ו-OVER-CAP-PAIR מדגל על כל condition מלא ש-`inventory_from_registry().pair_cost()` שלו ב-`max_pair_cost` ומעלה (הפסד נעול). ההתראות עוברות ל-stderr, ל-`run/guardrail_alerts.log`, ולאירוע `guardrail_alert` בטבעת; ה-dedupe יורה פעם אחת לכל הפרה ומתחמש מחדש בשינוי. 9 בדיקות ב-`live/tests/test_guardrail_watch.py`; סמוק `--once` חי שקט; הסוויטה 454 עברו, דילוג אחד. מעקב (סקירת קוד): ה-dedupe של repeat-exit מתחמש מחדש כשהאירועים של זוג עוזבים את החלון, כך שהישנות טרייה מתריעה שוב (לפני כן: הוחמצה בשקט); הסוויטה 456 עברו, דילוג אחד. מעקב: ה-poll עכשיו מפקח על השומר כתהליך ילד — מפעיל אותו בהתחלה (רק נתיב ייצור), מפעיל מחדש ילד שמת (האטה של 30 שניות), ומסיים אותו בעצירה — כך שהשומר רץ עם הבוט, כולל Start Bot של הדשבורד; הסוויטה 459 עברו, דילוג אחד. הכרעה **LIVE**.

* **2026-08-21 (בריאות בלוח: השומר מדווח על עצמו, שומר מת נראה לעין).** `live/scripts/guardrail_watch.py` כותב עכשיו `run/guardrail_watch_heartbeat.json` בכל בדיקה (pid, started_at, ts, cycle), ונקודת הקצה החדשה `/api/guardrail-health` + צ'יפ בסרגל בקרת הבוט מציגים ירוק "guardrail ● pid N · N התראות" כל עוד ה-heartbeat טרי (<30 שניות) או אדום "guardrail DOWN (heartbeat אחרון לפני N דקות)" כשהוא מתיישן -- מקרה הכשל השקט נראה עכשיו על הלוח. ספירת ההתראות מגיעה מהטבעת כך שהיא שורדת אתחולים של השומר; `started_at` מסמן את זמן האתחול. 4 בדיקות חדשות (כתיבת ה-heartbeat של השומר; נקודת הקצה חיה / מיושנת / נעדרת). סוויטת live: 459 עברו, דילוג אחד. הכרעה **LIVE**.