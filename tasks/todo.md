# Todo — Issue #291

- [x] T1 — Ranker `--out-dir` isolation (`scripts/filter_markets.py` + `tests/test_filter_markets_out_dir.py`)
- [x] T2 — Loop flag forwarding (`scripts/filter_loop.py` + `tests/test_filter_loop.py`)
- [x] T3 — Shadow feed override (`trader_loop._market_specs(path)` + `shadow_run --markets-path` + tests)
- [x] T4 — Menu `shadow-trial` launch + trial manifest (`scripts/spread-hunter-menu.ps1` + `tests/test_menu_shadow_trial.py`)
- [x] T5 — Manifest-driven resume replay (Menu R, All-runs loops)
- [x] T6 — Architecture note (`docs/agents/architecture.md`) + readiness suite green
